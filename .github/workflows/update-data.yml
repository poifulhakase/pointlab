name: Update Market Data

on:
  schedule:
    # 木曜 18:30 JST (09:30 UTC) — 投資主体別（木曜17時公表から1.5h後）
    - cron: '30 9 * * 4'
    # 金曜 18:00 JST (09:00 UTC) — 信用残高・裁定残高（金曜16:15公表から1.75h後）
    - cron: '0 9 * * 5'
    # 土曜 09:00 JST (00:00 UTC) — 金曜失敗時のバックアップ
    - cron: '0 0 * * 6'
  workflow_dispatch:  # 手動実行も可能

jobs:
  fetch-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fetch latest market data
        run: npm run fetch-data

      - name: Check for changes
        id: diff
        run: |
          git diff --quiet public/data/ || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push updated data
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/
          git commit -m "chore: update market data [skip ci]"
          git push

      # Vercel は GitHub 連携により、push をトリガーに自動デプロイ
