# handover.md — 直近の作業引継ぎ

> セッションをまたぐ「直近状態」の正。作業完了時に**先頭の「現在の状態」を更新**し、古い分は「履歴」に1〜2行で畳む。
> 背景・経緯の深掘りは `~/.claude` メモリ（第1〜25セッションの詳細ログ）。

最終更新: 2026-08-14

---

## 🔴 ユーザーの残作業（2026-08-09 時点・未完了）

疑似トレード（ロボ口座）は**実装が一式そろっていて、動かすのに必要なのは下記だけ**。
コード側でできることは残っていない。

- [x] **① Anthropic API にクレジットをチャージ** … ✅ 2026-08-09 完了（$20・残高 $19.97）
  🔵 ぽいロボ側の見積もりは**約1,000円/月**（1日1回・営業日20日・`claude-opus-5` / effort `high`）。
  🔵 AutoFBA のハカセAI と同じアカウントなら残高は共用。

- [x] **①b 月間の支出上限と自動リロード** … ⏸ **一旦やらない**（2026-08-10 ユーザー判断）
  🔵 **月間上限は $10 のまま**。ぽいロボ＋AutoFBA を合わせると月内に頭打ちする可能性はあるが、
  　 まず実際にいくら使うかを見てから決める（見積もりは約1,000円/月）。
  🔴 **頭打ちになると判断が静かに止まる**（Actions は `continue-on-error` なので落ちずに素通りする）。
  　 通知が来ない日が続いたら、まず残高と上限を疑うこと → https://console.anthropic.com/settings/limits

- [x] **② GitHub Secrets を3つ登録** … ✅ 2026-08-10 完了・Actions 上で疎通確認済（run #134）
  手動実行で `robo_dry` にチェックを入れると `--dry --force` の空撃ちができる（`fetch-data.yml` の
  workflow_dispatch 入力・`e3173ec`）。判断ステップは平日19:30のスケジュール実行だけの条件なので、
  これが無いと本番初回まで配線を検証できなかった。
  🔵 #134 で `ANTHROPIC_API_KEY`（LLM判断が成立）・`CHATWORK_ROOM_ID`（ログで `***` にマスク）とも通過を確認。
  🔴 このとき出た `画像の取得に失敗: Unexpected end of JSON input` は鍵の問題ではなくコードの穴で、`811f660` で修正済
  　 （Chatwork はファイルが1枚も無いルームの `/files` に 204 No Content を返す。`res.json()` が空文字で落ちていた）。

  登録した3本（内容の控え）:
  - `ANTHROPIC_API_KEY` — Claude API（判断と保有画面の読み取りに使う）
    🔴 **ぽいロボ専用に新しく1本発行する**（2026-08-10 ユーザー決定）。AutoFBA のハカセAI と
    **鍵は分けるが残高は共用**（残高はアカウント単位なので追加チャージは不要）。
    分ける理由＝①ぽいロボがいくら使ったかを鍵ごとの使用量で検証できる（見積もり約1,000円/月の答え合わせ）
    ②将来この鍵を失効させてもハカセAIが巻き添えで止まらない。
    発行: https://console.anthropic.com/settings/keys → Create Key（名前は `poirobo` 等）
    🔵 AutoFBA の鍵は**本番サーバーの `.env` にしか無い**（ローカルの `backend/.env` には入っていない）。持ち出さない。
  - `CHATWORK_API_TOKEN` — 通知の送信と画像の受け取り
  - `CHATWORK_ROOM_ID` — 宛先。値は `~/.claude` メモリ `reference_stock_calendar_chatwork_room` にある
  - 🔵 未設定でも他のデータ更新は動く（`continue-on-error` にしてある）

- [x] **②b APIキーを発行したら、まずローカルで空撃ちする** … ✅ 2026-08-10 完了
  `node scripts/robo-trade.mjs --dry` が通過。営業日判定→価格(66,306円)→需給12項目→イベント(8/12 米CPI)→
  LLM判断 `hold / 確信度58%` まで一気通貫。理由・「この判断が外れるとき」とも日本語で充填されており
  プロンプト側の手直しは不要。Chatwork 未設定のため画像なしで続行（想定どおりの分岐）。
  🔴 **鍵をコマンドに直接書かない**（シェル履歴と画面＝作業ログに残る）。
  リポジトリ直下に `.env.local` を作って、そこに1行だけ置く（`.gitignore` の `.env*.local` で除外済み）:
  ```
  ANTHROPIC_API_KEY=sk-ant-...
  ```
  ```
  # 鍵なしで配線だけ確認（済・2026-08-09 に実施して通っている）
  node scripts/robo-trade.mjs --dry --no-llm --force

  # 鍵ありで「AIの判断そのもの」を目視する（書き込み・通知はしない）
  node scripts/robo-trade.mjs --dry
  ```
  🔵 `.env.local` は `scripts/loadLocalEnv.mjs` が読む。Actions には存在しないので影響しない
  　 （Secrets が `process.env` に入る／既存の環境変数は上書きしない）。
  🔵 起動時に出るのは**読み込んだキー名だけ**で、値は表示されない。
  🔵 `--dry` は口座もログも書かず、Chatwork へも送らない（送る内容を画面に出すだけ）。
  🔵 `--force` は休場日でも走らせる指定。平日に試すなら要らない。
  🔴 ここで**理由と「この判断が外れるとき」が日本語で埋まっているか**を見ること。
  　 空だったり英語だったりしたらプロンプト側の問題なので、本番に上げる前に直す。

- [x] **③ チャート撮影の自動化** … ✅ 2026-08-10 完了（`aef6c1f` 他）
  - Playwright ＋ Chromium 導入済み。**ログインは不要になった**（公開チャート `INDEX:NKY` 日足を撮る）
  - タスクスケジューラ `poirobo-capture-chart` に**平日16:00**で登録済み
  - 休場日は撮らない／中身の無い画像は投稿しない
  - 🔴 **残り＝`.env.local` に `CHATWORK_API_TOKEN` と `CHATWORK_ROOM_ID` の2行**（ユーザーが貼る）。
    無いと「撮れているが投稿できない」で止まる。**GitHub Secrets は Actions 専用で撮影側には届かない**
  - **初回の自動撮影は 2026-08-12(水) 16:00**（8/11 は山の日でスキップ）

- [ ] **③b BB・MACD を載せるか**（保留・2026-08-10）
  公開チャートなのでインジは載らない。欲しくなったら TradingView にログインして
  保存レイアウトを**現物 `INDEX:NKY` に直して保存**し、`.env.local` に `ROBO_CHART_URL` を足す。
  🔴 保存レイアウトは `NK1!`（SGXの先物）のままなので、直さないと**画像は先物・数値は現物**になる。
  🔵 そもそも**画像が判断に効いているかは未検証**なので、急がなくてよい。

- [ ] **旧③ ローカルに Playwright を入れ、TradingView に初回ログイン**（→ 上記③で不要になった）
  🔵 **調べた（2026-08-10）＝ふだんのChromeでは TradingView にログイン済み**（アカウント「ポイントラボ」・
  　 保存チャート `ecEzo0V0` が開く）。ただし**Chrome拡張ごしの撮影では、価格ヘッダーは写るのに
  　 チャート本体（ローソク足）が白いまま写らなかった**（キャンバスが 300x150 の初期サイズのままで描画されない）。
  🔴 したがって**撮影は Playwright が正解**。ブラウザ内部で描画完了を待って撮るのでキャンバスも写る。
  　 ふだんのChromeにログイン済みでも**Playwright側は別のプロファイル**なので、下の初回ログインは省略できない。
  ```
  npm install -D playwright
  npx playwright install chromium
  npm run capture-chart -- --login     # ブラウザが開くのでログイン→Enter
  ```
  🔵 撮影しなくても判断は動く（数値だけで続行する）。急がなくてよい。

- [ ] **④ 毎営業日の運用**（③が済んでから）
  引け後に `npm run capture-chart`（チャート撮影＋自動投稿）＋
  **売買した日だけ**、松井証券の保有株式（残高照会）を Chatwork に投稿
  → **翌営業日 08:30 JST** に Actions が実保有へ同期・判断・記録・通知（2026-08-10 に19:30から移動）。
  🔵 保有画面は判別のため、ファイル名に `chart` を**含めない**こと（含むとチャート扱いになる）。
  🔵 売買していない日は投げなくてよい（同期は起きず、ロボ口座はそのまま）。

- [x] **⑤b トレード記録の学習（A＋B）＝器は完成**（2026-08-14 実装・下の「現在の状態」参照）
  🔵 残っているのは**貯まるのを待つ**ことだけ（毎日の自動実行で勝手に埋まる）。
  以下は着手時のメモ（設計の意図は変わっていない）。
  🔴 **判断のしかたには触らない**。あとから記録を読んで評価する後付け処理だけを作る
  （いま30トレードで「AIは決定論より上手いか」を測っている最中なので、判断器を変えると分母が壊れる）。
  - **A. 結果の紐付け**：判断ログに「その後どうなったか」を追記（翌営業日の寄付き→1日後/5日後のリターン・方向が当たったか）
  - **B. 確信度の答え合わせ**：確信度の帯ごとに「言った確率 vs 実勝率」を集計
  🔴 **いまのログ（`robo_logs/*.json`）には結果が入っていない**（`input`/`output`/`validation` のみ）＝
  　 このままでは後から学習させようにも材料が無い。だから先に作る。
  🔵 学習そのもの（過去の判断をプロンプトに渡す／人が教える）は**30トレード後に2号機として並走**させる。
  　 学習なしの記録が残っているので、学習が効いたかどうかも比較で測れる。
  🔵 ぽいロボ本体では同じ検算で「確信度が自信過剰かつ反転」を発見している（第25セッション）。

- [ ] **⑤ 読み取り精度の確認（未実施）**
  🔴 キャプチャの読み取り精度は**まだ測っていない**。実物を1〜2枚投げて答え合わせすること。
  機械チェック（桁誤り・数量過大・対象2銘柄以上）は入れてあるが、実測はしていない。

- [ ] **④ 判断ポイント（先の話）**
  - 30トレード貯まったら Go/No-Go（期待値>0 かつ 対照群を上回る）。週次シグナルなら**約7ヶ月**先
  - LLM のコストを実測して `effort` を `high` のままにするか決める（見積もり約1,000円/月）

---

## 🟢 現在の状態

**ブランチ**: `main`。**第47 まで push 済み＝本番反映済み**（2026-08-16 / `3ac7a86`）。
未コミットなし・origin と一致。

### 2026-08-16(47): アプリの点検 → 見つかった穴をふさぐ（通知APIの認証・脆弱性0件）

ユーザー依頼「このアプリに問題点がないか調査」→ 見つかったものを**その場で対応**した。
**tsc 0 / lint 0 / テスト 492件・36ファイル 全green（+9）／build OK**。push 済み `3ac7a86`。

#### 🔴 見つけた穴：予約通知APIが実質だれでも叩けた

`api/send-booking-email.js` と `api/send-booking-push.js` は **Origin ヘッダの一致しか見ていなかった**。
Origin はブラウザの外（curl など）からは自由に名乗れるので、防御になっていない。
さらに**宛先を本文の `userEmail` からそのまま取っていた**ため、

- **任意のアドレスに「ぽいロボ」名義のメールを送れる**（＝迷惑メールの踏み台・ドメイン評判が落ちる）
- プッシュも**任意の uid に任意の文面**を送れる（uid が分かれば他人の端末に通知が出せた）

状態だった。予約の作成／キャンセル（`create-booking` / `cancel-booking`）は最初から
idToken 検証＋レート制限があり、**通知の2本だけが取り残されていた**。

#### 直し方（`api/_bookingAuth.js` に共通化）

| やったこと | 中身 |
|---|---|
| ① 認証 | `Authorization: Bearer <idToken>` を Admin SDK で検証（後方互換で body.idToken も見る） |
| ② 🔴 宛先の引き直し | **本文を信じず**、`bookings/{id}` から宛先・氏名・日時を取る |
| ③ 権限 | `request` / `cancel_user` ＝本人、`confirm` / `cancel_admin` ＝管理者だけ |
| ④ レート制限 | uid あたり 60秒10通（`_ratelimit.js` を流用・1操作で最大2通のため少し多め） |
| ⑤ 再発防止 | `src/utils/__tests__/bookingNotifyAuth.test.ts` 9件。**「本文の userEmail は無視される」を固定** |

- 🔵 例外は**管理者からの一言**だけ本文を採用する。キャンセルAPIはこれをドキュメントに書かないので、
  拾わないと文面から消えてしまうため（送れるのは管理者だけなので安全）。
- フロント（`bookingApi.ts`）は `sendBookingEmail` / `sendBookingPush` が **idToken を付けて送る**ように変更。

#### 依存の脆弱性 13件 → **0件**

| | 前 | 後 |
|---|---|---|
| firebase-admin | 13.10.0 | **14.2.0** |
| uuid（gaxios / teeny-request 経由） | 古いまま moderate 6件 | `overrides` で **11 系に固定** |
| xlsx | 0.18.5（npm版・修正なし・high） | **作者の公式配布 CDN 0.20.3**（修正版） |

- 🔴 **xlsx は npm レジストリ版が放置**されている（Prototype Pollution / ReDoS が未修正のまま）。
  作者は自分のCDNでしか新版を配っていないので、`package.json` が
  `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` を直接指している。
  **取得元が npm ではない**ので、CI の `npm ci` が落ちたらまずここを疑う。
  使っているのは `scripts/fetch-jpx.mjs` など**サーバー側だけ**（ブラウザには入らない）。
- 🔵 `overrides` と xlsx の理由は `package.json` の `//overrides` / `//xlsx` に書いてある。

#### 点検して**問題なし**だったもの

- ソースマップは本番に出ていない（`.map` は index.html にフォールバック）
- ソースに秘密情報の混入なし（Firebase の web apiKey は**クライアントに出る前提**の値。守るのは `firestore.rules`）
- 残る無認証API＝`booking-ics` / `rainviewer-weather-maps` / `stocks-daily` / `youtube-rss`。
  読み取り専用か、取得先ホストが固定。踏み台にならない
- `public/data` 16ファイルすべて最新

### 2026-08-16(46): 表示速度（起動時の転送量を4割減らした）

ユーザー依頼「表示速度改善」→ 計測してから直した。push 済み `277c1e1` / `7cd2737`。

🔴 **最初の描画はもともと速かった**（FCP 188ms・DCL 85ms）。遅さの正体は**描画後の転送量**だったので、
「初期表示に要らないものを後ろへ下げる」方向で直した。

| | 前 | 後 |
|---|---|---|
| **最初の2秒の転送量** | 1,297KB | **776KB（−40%）** |
| 合計転送量 | 1,463KB | 1,237KB |

- **A. 画像**＝`icon-192.png` 43→16KB、`logo.svg` を最適化（sharp / svgo）。
  🔴 svgo は `public/icons.svg`（スプライト）を**41バイトに破壊した**ので対象外にしてある。触らないこと。
- **B. Firestore同期をアイドルまで遅らせた**（`useFirebaseSync.ts` の `whenIdle`）
  ＝firestore(291KB)＋メモ(169KB) が**4.4秒後**に移動。裏タブなら `visibilitychange` まで待つ。
- **C. モーダルの遅延マウント**（`{poiroboAlertModalOpen && …}` / `{noteDate && …}`）
  ＝開くまで読み込まない。
- **D. メモの月別分割は見送り**（判断）。169KB が初期ロードから外れて優先度が下がった一方、
  データ構造の移行リスクの方が大きい。🔵 **500KB を超えたら再検討**。

### 2026-08-16(30): 答え合わせを push し、成績タブに出した

**テスト 476件・34ファイル 全green（+4）／tsc 0／build OK。** 🔴 判断のしかたには触っていない。

1. **第29（判断の答え合わせ）を push**（`b848de8`）＝本番デプロイ＋毎営業日の自動実行に
   「答え合わせ」ステップが入った（`continue-on-error`・**LLMを呼ばないので費用ゼロ**）。
2. 🆕 **エンジン＞成績タブに答え合わせを表示**（ユーザー判断＝「n<30 は暫定と明記して出す」）。

| 追加したもの | 中身 |
|---|---|
| `src/utils/roboCalibration.ts` | `robo_calibration.json` の読み込みと型／`PROVISIONAL_MIN=30`・`isProvisional()` |
| `RoboAccountPanel` の `CalibrationBlock` | 成績タブの PERFORMANCE と YOUR CALL のあいだに1枠 |
| テスト4件 | `src/utils/__tests__/roboCalibration.test.ts`（文言そのものも検査） |

**画面の中身**＝①1営業日後／5営業日後の 件数・勝率・平均確信度・平均損益
②確信度の帯（言った確率 vs 実勝率・差は負＝自信過剰で色を変える）
③影の2通り（チャートのみ／数値のみ）の n と勝率。

- 🔴 **5営業日後の n が30件に満たないあいだは「暫定 n=X / 30」バッジ**＋
  「**ここを見て設計を変えないでください**」を本文に出す。小さい標本の勝率を毎日見ると
  偶然のブレを実力と読み違えるため（ぽいロボ本体では自信過剰を**52週貯めてから**見つけている）。
- 🔴 **上の PERFORMANCE（建玉の損益）とは別の物差し**である断りを同じ枠に置いた。混ぜて読むと誤る。
- 🔵 帯は5営業日後が主。まだ0件の時期は**先に埋まる1営業日後**を出す（どちらを見ているかは必ず書く）。
- 🔵 プレビューモードでは出さない（`fetchRoboCalibration` が null を返す＝ダミーを作れない数字なので）。
- 🔵 データを取り直した（8/14 ぶんを反映）＝判断4件・**採点できたのは5日後で0件**（1日後 n=2・勝率100%）。
  影は 8/14 が1件目。

🔵 残りは**貯まるのを待つ**だけ（毎営業日の自動実行で勝手に埋まる）。

3. 🔴 **地下室（デイトレード／スイングトレード）を「結果だけ」にした**（ユーザー指示）。
   説明の地の文を落とし、**表と数字だけ**にする。両ファイルの先頭コメントにも書いたので、
   今後この2ページに数字を足すときは**表に1行足すだけ**にして、解説を書かないこと。

| ページ | 落としたもの | 残したもの |
|---|---|---|
| デイトレード | 地下室の説明リード／「指数で測ると勝てて実物で消える」の解説3段／型の内訳の前置き／「なぜ日中は伸びないのか」／「これから」 | 結論1行・測ったこと6行・型の内訳5行・🆕「そのほかの実測」5行（1321寄→引、オーバーナイト vs 日中、寄り天率、指数の始値、5分足の保存） |
| スイング | リード文／結論の補足／最大DDの解説／「なぜ引けで建てるのか」／TEVの現在地／「これから」 | 結論1行・分かっていること5行・まだ分かっていないこと4行（短文化）・🆕「数字」4行・「30トレード貯まるまで設計は動かさない」1行 |

🔵 免責（研究の記録であり推奨ではない）は**両ページとも残してある**。

4. 🆕 **地下室2ページを結論ベース＋ダイナミックな見た目に作り直した**（ユーザー指示）。
   共通部品を新設し、両ページを同じ形にした。

| 追加したもの | 中身 |
|---|---|
| `src/components/basementKit.tsx` | `VerdictHero`（判定スタンプ）／`BigStat`＋`StatGrid`（特大の数字）／`JudgeList`（判定一覧）／`BasementHead`／`BasementKeyframes` |
| `src/components/basementHooks.ts` | `useInView`／`useCountUp`／`reduceMotion`（🔵 部品と分けたのは fast refresh の警告を出さないため） |

- **形は3つだけ**＝①判定スタンプ（結論を言い切る・背景に巨大な × / ○ が流れ込む）
  ②特大の数字3枚（見えたところで0から駆け上がる）③判定一覧（行が順に出て、強さのバーが伸びる）。
- 🔴 **デイ＝「日計りの根拠は、見つからなかった。」／スイング＝「ルールとしては成立している。AIが上回るかは未判定。」**
  を先頭で言い切る。以降は数字だけ。
- 🔵 動きは**スクロールして見えたものだけ・一度きり**（波動の書と同じ作法）。
  `prefers-reduced-motion` では動かさず最初から結果を出す。画像は使わず CSS と SVG のみ。
- 🔵 1日の型は出現率を行の地に敷いた（数字を読まなくても比が見える）。

### 2026-08-16(39): Believe を5枠2列に。ロボットの図・余白・配色を仕上げ

相談の続きで枠を増やし、見た目を仕上げた。

- **枠は5つ**（相談で追加）＝ファナック（動く/工場）・ハーモニック（動く/関節）・
  キオクシア（記憶）・アドバンテスト（考える/検査）・フジクラ（つなぐ/光）。
  🔵 独占の強さ＝アドバンテスト（テスタ実質2社）＞ ハーモニック（減速機50%）＞ ファナック（CNC標準）
  ＞ キオクシア（NAND寡占だが価格は市況）＞ フジクラ（**5枠で最も薄い**と明記）。
- **PCは2列**カード（銘柄が増えたため）。スマホは1列。
- 🆕 **4層の図にロボットの線画**（頭＝考える／胸＝記憶／破線の配線＝つなぐ／関節＝動く）。
  値段が付いた層は鈍色、**まだ付いていない「動く」だけシアンに光る**。
  🔴 ぽいロボのドット絵（`PoiroboPixel`）でも試したが、**手足が短く印の置き場が窮屈**で戻した（ユーザー判断）。
- 🔴 **日経との対比は全廃**（ユーザー指示）＝素の12ヶ月・3ヶ月だけ。ヘッダーの日経表示も削除。
- 🔴 **文章を大幅に削減**＝見立ては1項目1行、WHY/CONFIRM/BREAK は「詳しく」に畳む。
  末尾は**免責1行＋UPDATED だけ**（DROPPED の表示は外し、データには記録として残す）。
- 🔵 **余白を全体に拡大**（特にスマホ）＝見出し30px・3行、株価38px、チャート180px、カード間隔26px。
  カードは1枚ずつ立ち上がる。
- フッターを**研究室配色（ネオン）**に（`CalendarHeader` の `isLab` に `momentum` を追加）。
- データは**即時反映**（`checkUpdatedAt`）＝枠を入れ替えても古い銘柄が残らない。

### 2026-08-16(45): 右下タブを 主力 / 候補 / 局面 / 検索 に統一

- 周期画面の内部タブ（セクター／個別）を外に出し、Believe のタブと**1本の並び**にした。
- 🔴 名前は**全部2文字の名詞**でそろえた（品詞と字数がバラつくと並びが読みにくい・ユーザー指摘）。
  主力＝5枠／候補＝監視銘柄／局面＝景気循環のどこか／検索＝銘柄を探す。
- 🔵 **検索はスマホのみ**（PCは3列とも見えているので、押しても変化しないタブになるため）。

### 2026-08-16(44): 切り替え口を右下だけに整理／カレンダーのセクター入口を削除

- 🔴 **地下室の画面内の切り替えを全部やめた**（ユーザー指示＝切替口が多すぎた）。
  消したもの＝**端の矢印**・**下中央の現在地**・**ページ末尾の「隣の部屋」カード**。
  残したのは**右下のサブメニュー（デイ / スイング）**と、見た目を占領しない**スワイプ／キーボード ← →** だけ。
- 🔴 **カレンダー左サイドバーのセクターローテーションの円形バナーを削除**（ユーザー指示）。
  入口は **Believe の右下タブ「サイクル」に一本化**。`Sidebar` の `onOpenSector` と
  App の `handleOpenSector` も削除した。

### 2026-08-16(43): サイクル（セクターローテーション）を Believe のタブに入れた

- 右下のサブメニューを **主力 / その他 / サイクル** の3つにし、**サイクル＝セクターローテーション**（ViewMode `sector`）を割り当てた。
  🔵 カレンダー左サイドバーのバナーからの入口は従来どおり残っている（入口が2つになっただけ）。
- 🔵 これまで**スマホでしか出していなかったサイクルのバー**を、PCでも出すようにした
  （3画面を行き来するタブなので、PCで消えると戻れない）。スマホは区切りのあとに セクター / 個別 が続く。

### 2026-08-16(42): Believe を会員限定に／切替を右下のサブメニューへ統一

- 🔴 **Believe と その他の監視銘柄を会員限定に**（ユーザー指示）。
  作法は地下室と同じ＝**入口（研究室のメニュー）は全員に見せ、🔒会員限定の札を付け、中身に鍵をかける**。
  非会員が押すと会員案内、直接その view が復元された場合も `CommunityLockScreen`。
  🔵 判定は `canViewMemberPages`（管理者は従来どおり全部見える）。
- **画面の切り替えを右下のサブメニューに統一**（他画面と同じフローティングバー）
  - Believe＝**主力 / その他**（`momentum` / `watch`）。枠内のリンクカードは廃止。
  - 地下室＝**デイ / スイング**（`daytrade` / `swing`）。
    🔴 部屋の状態は **App の ViewMode を単一情報源**にした（内部stateから移動）。
    タブ・左右スライド・端の矢印・「隣の部屋」カードが常に一致する。
  - どちらもバーはサイバー配色（`isBelieveNeon` を追加）。

### 2026-08-16(41): 監視銘柄ページ・レンジ枠・Chatwork通知

- 🆕 **その他の監視銘柄（別ページ・ViewMode `watch`）**＝Believe の枠の最後に入口カード。
  フィジカルAIのサプライチェーン**19社**を数字＋1年のミニチャート（200日線つき）で並べる。
  並べ替えは「200日線に近い順」（既定）／12ヶ月が強い順／高値から遠い順。
- 🆕 **レンジ枠（歴史的サポート狙い）**＝2432 DeNA / 3793 ドリコム / 3825 リミックスポイント。
  🔴 **会社の中身は見ない**（値動きが規則的なレンジに見えるという理由だけ・ユーザー指示）。**15年の週足**で見る。
  🔴 サポートの物差しを**途中で直した**＝当初は「15年の絶対安値からの距離」だったが、
  **何年も前に一度だけ付けた暴落安値は実際には機能しない**（ドリコムは15年安値から+136%で永遠に光らない）。
  → **直近5年の安値の下位15%＝何度も止まっている帯**をレンジ下限とし、**そこから+10%以内**をサポート圏にした。
  チャートに下限の帯（±5%）を描き、**5年で何回止まったか**も出す。
  🔵 実測＝ドリコム 下限413（+12.1%・51回）／リミックス 169（+30.2%・50回）／DeNA 1,496（+69.5%・54回）。
- 🆕 **Chatwork通知**（`npm run notify-watch`・Actions に同梱）＝
  **200日線付近（±5%）／サポート圏に入った銘柄と、そこから外れた銘柄**を、**変化があった日だけ**送る。
  状態は `public/data/poirobo_watch_state.json` に保存して差分を取る。
  🔴 **知らせるだけ**（採否も入れ替えも自動ではやらない）。
- **200日線付近の見せ方を強化**＝太い枠・左6pxの帯・呼吸する光・大きいバッジ。該当しない枠は少し沈める。
- 🔵 現在の該当＝ファナック(+0.55%)・安川電機(+2.62%)・NTN(+2.64%)・ディスコ(+2.55%)。

### 2026-08-16(40): 仕上げ（文言・ロボットの動き）と、資料3点の更新

- 見出し＝**「ロボットが、第4次産業革命を起こす。」**（旧「フィジカルAIが、〜」）。
  基準の文言＝**「世界で独占があるか」**。4層の絵の見出し行は削除。
- 🆕 **ロボットが動く**＝手足が関節を軸に振れる／頭が上下する／配線を信号が流れる。
  `prefers-reduced-motion` で全部止まる。
- **資料3点を更新**
  - システム仕様書（研究室＞資料＞システム仕様）＝「Believe / 第4次産業革命」の節を全面書き直し
    （5枠・選定基準・4層の絵・2列カード・日経対比の全廃・DROPPED・即時反映・ドット絵を戻した経緯）
  - 説明書（ManualView）＝**Believe の章を新設**（選び方／画面の見方）。章は8つに
  - この引継ぎ資料（第36〜40）

### 2026-08-16(38): Believe の枠を決めた（相談の結果）＋ 選定基準は「独占」

🔴 長い相談の結論。**枠は3つ**。安川電機は一度入れて**外した**（記録も残す）。

| 枠 | 銘柄 | 独占（＝選定基準） |
|---|---|---|
| A案：工場の自動化 | 6954 ファナック | CNCが事実上の世界標準（スイッチングコストの壁） |
| B案：人型・関節 | 6324 ハーモニック | 精密減速機で世界シェア約50%（材料・熱処理・加工のノウハウ） |
| C案：AIの記憶 | 285A キオクシア | NANDは世界5〜6社の寡占。🔴 価格サイクル型で**賭けの型が違う**（β3.23） |
| ~~見送り~~ | 6506 安川電機 | サーボは競合が最多（中国Inovance首位）＝**独占が薄い**／値動きもファナックとほぼ同じ |

- 🔴 **選ぶ基準はひとつ＝独占があるか**。独占が無ければ台数が増えても値下げ競争で終わる（＝「単価の崩壊」）。
  画面の冒頭とカードの `MOAT / 独占はあるか` に明記した。
- 🆕 **AIの4層の絵**（`LayerGap`）＝考える/記憶/つなぐ/動く の代表銘柄の対日経12ヶ月を毎日並べる。
  現状＝アドバンテスト +189%／キオクシア +2001%／フジクラ +160%／**ファナック −16.6%**。
  「AIは来ている。ロボットだけが、まだ来ていない。」を1枚で言う絵。
  🔴 絵の下に「**『まだ来ていない』は『これから来る』を意味しない**」も併記。
- 🆕 `DROPPED`（見送った枠の記録）＝外した理由を残す。後から「外して正解だったか」を検証するため。
- 🔵 相談で確認した事実は見立ての本文に出典つきで反映済み（8/3のファナック −15.7%、中国サーボのシェア、
  配膳ロボPuduが日本8割、キオクシアの1ヶ月7割安、Optimus Gen3が全身140基・2026年後半量産）。

🔴 次の相談＝**秋の3イベント**（安川2Q 10月上旬／ファナック2Q 10月下旬／Optimus Gen3の量産）を見て枠を見直す。
カレンダーに置くかは未決。

### 2026-08-16(37): 画面名を「Believe（第4次産業革命）」にし、3枠目にハーモニックを入れた

🔴 ユーザーとの長い相談の結果。**銘柄選びは毎回相談して決める**（機械は測るだけ・自動で入替えない）。

- **画面名**：`MOMENTUM / 中長期銘柄` → **`Believe / 第4次産業革命`**（見出しも「フィジカルAIが、第4次産業革命を起こす。」に）。
  🔵 内部識別子（ViewMode `momentum`・ファイル名 `MomentumStocksView.tsx`）は据え置き。
- **枠は3つ。仮説のラベル付き**（後から「どちらの層に価値が落ちたか」を答え合わせするため）
  - 6954 ファナック＝**A案：工場の自動化**（CNCの世界標準・まだ値段が付いていない）
  - 6506 安川電機＝**両取り**（サーボは産業用にも人型にも要る・時価総額が小さく伸びしろ）
  - 6324 ハーモニック＝**B案：人型・関節**（精密減速機 世界シェア50%・Optimus Gen3は全身140基）
- 見立てに `lane` / `laneLabel` / `markedOn`（観測開始日 2026-08-16）を追加。画面にチップで出る。

**相談で確認した事実（出典つきで見立てに反映）**
- ファナックは8/3に −15.7%（受注は拡大も利益がコンセンサス未達）。安川も下振れ。
- 中国：サーボは中国Inovanceが中国市場首位（15.9%）、安川2位。ただし**産業用ロボットの世界シェアは日本勢6割**、精密減速機はハーモニックが世界50%。
- 配膳ロボットの日本シェアは**Pudu（中国）が約8割**＝安価帯はすでに中国。
- キオクシアは6/22の112,700円→7/30に36,020円（**1ヶ月で約7割安**）。44兆円はピーク時の話。
- 🔴 **AIの4層のうち、値段が付いていないのは「動く側」だけ**（対日経12ヶ月＝キオクシア+2001%／アドバンテスト+189%／古河電工+274%／フジクラ+160%／**ファナック −16.6%**）。

🔴 次に相談すること＝Optimus Gen3の量産（2026年後半）で**部品表に誰が入るか**を見てから枠を見直す。
キオクシア・電線株を「ものさし」として画面に置くかは未決。

### 2026-08-16(36): 中長期モメンタム銘柄（研究室＞MOMENTUM）を新設

🔴 きっかけ＝ユーザーの「ファナックと安川電機をぽいロボのメイン銘柄にしたい」。
話し合って**役割を「観測」に決めた**（売買対象にするのは30トレード貯まってから）。

- 🔴 **ロボ口座には入れていない**。判断器も売買対象も触っていない＝いま測っている 2/30 件の分母は無傷。
- 研究室のメニュー **DATA の上**に「MOMENTUM / 中長期銘柄」を新設（PC・スマホ共通・ViewMode `momentum`）。

| 追加したもの | 中身 |
|---|---|
| `scripts/fetch-poirobo-stocks.mjs` | Yahoo から 6954/6506 と日経の日足 → `public/data/poirobo_stocks.json`（`npm run fetch-momentum`・Actions に組込） |
| `src/utils/poiroboStockCalc.mjs` | β・相関（日付で突き合わせ）／1〜12ヶ月リターンと対日経の相対／12-1モメンタム／52週の位置／姿勢。テスト7件 |
| `src/utils/poiroboStockThesis.ts` | **見立て（人が書く）**。仮説 → CONFIRM（効いていれば出る数字）→ BREAK（崩れる条件）の形 |
| `src/components/MomentumStocksView.tsx` | 画面。巨大な社名の透かし・特大の株価・全幅チャート（200日線＋期間6ヶ月/1年/2年）・相対リターンのバー・52週メーター |
| `src/hooks/useMotion.ts` | `useInView` / `useCountUp` / `reduceMotion` を共通化（地下室の `basementHooks` はここへの再輸出に変更） |

🔴 **API は増やさない**（Vercel の Functions 枠が上限）。Actions で取って静的JSONで配る既存の作法に乗せた。

**初回の実測（2026-08-14 時点）**
```
6954 ファナック 6,435円  β1.32 相関0.72  12ヶ月 +50.7%（対日経 −16.6%）  52週高値から −27.5%
6506 安川電機   5,431円  β1.65 相関0.75  12ヶ月 +70.7%（対日経 +3.4%）   52週高値から −31.4%
```
🔵 **2銘柄とも日経の増幅器**（β1.3〜1.7）。ただし**直近3ヶ月は日経に大きく負けている**（−33.7% / −34.2%）。
　 ここは隠さずそのまま画面に出している。

🔴 次に判断が要ること＝**C案（影で並走）をやるか**。ロボ口座の bull/bear/hold をこの2銘柄に写して
仮想成績だけ記録すれば、判断器を触らずに「ETFで取るのと個別で取るの、どちらが良かったか」を同じ物差しで比べられる。

### 2026-08-16(35): 画面名を改称（シールド→ブンセキ／エンジン→ロボ口座）

🔴 ユーザー指示。**表示名だけ**を変え、内部識別子・保存キー・ファイル名は据え置き
（★2026-08-09 の入れ替えと同じ理由＝保存済みレポートの中身が別画面のものとして出てしまう）。

| | 旧 | 新 |
|---|---|---|
| 需給分析（ViewMode `quant`） | シールド | **ブンセキ**（アイコンも盾→**棒グラフ＋虫めがね**） |
| 疑似トレード（ViewMode `shield`） | エンジン | **ロボ口座**（アイコンはロボットのまま） |
| ブンセキの先頭タブ | シールド | **需給** |
| ロボ口座の先頭タブ | ロボ口座 | **口座** |

- 🔴 **画面名と同じ名前のタブを無くした**（どちらを指しているか読めなくなるため）。
- 直した表示名＝フッターナビ（`CalendarHeader` の `MAIN_VIEWS`・新 `AnalysisIcon`）／
  ブンセキ左パネルの見出しと COPY ボタンの絵（`EnginePanel`）／説明書の章（`ManualView`）／
  利用規約・免責（`LegalModal`）／ぽいロボとは（`PoiroboAboutPanel` の兵装名 ANALYSIS・ACCOUNT）／
  戦略プレイブックの道具紹介／需給プロンプトの Role「ぽいロボ ブンセキ」（`enginePrompt.ts`）／
  システム仕様書（改称の節を追加。★日付つきの過去記述は当時の名称のまま）。
- 🔵 テストも文言に追随（`engineTabs.test.ts` は先頭タブが「口座」であることを固定）。
- 🔵 実画面で確認（ナビのアイコンとツールチップ・ブンセキ画面の見出しとタブ）。

### 2026-08-16(34): 地下室を「左右にスライドする1画面」にした

🔴 ユーザー指示＝「右上での切り替えじゃなくてページ内で左右スライド」「ダイナミックに」。
第33で入れた**ヘッダーの切替ボタンは廃止**し、部屋を横に並べて滑らせる形にした。

| ファイル | 役割 |
|---|---|
| 🆕 `BasementRooms.tsx` | 地下室の**枠**（壁・電球・ヘッダー・スライド・操作）。App はこれ1つを出す |
| `DaytradeResearchView.tsx` / `SwingResearchView.tsx` | **部屋の中身だけ**（`DaytradeRoom` / `SwingRoom` を named export）。枠は持たない |
| `basementKit.tsx` | `BasementRoomSwitch` を削除。`BasementBackdrop` に `jolt`（電球が大きく揺れる）を追加 |

- **操作は4通り**＝①指／マウスのドラッグ（**軸を最初の動きで決める**ので縦スクロールを邪魔しない・
  端は引っぱりを重くする）②画面端の矢印（PC・行き先の部屋名つき）③キーボード ← →
  ④ページ末尾の「隣の部屋」カード。下中央に現在地（押しても移れる）。
- **ダイナミックにした点**＝トラックは 620ms の弾む曲線で滑り、**いない部屋は 0.94 倍に縮んで暗く沈む**
  （隣を覗いている見え方）。移動のたびに**電球が大きく揺れ**（`bsmt-jolt`）、**光が横に走る**（`bsmt-wipe`）。
  🔵 部屋の中身は**画面に入って初めて**判定スタンプ・数字の駆け上がりが動く（`useInView` のまま）＝
  滑り込んだ瞬間に部屋が起きる。🔴 `prefers-reduced-motion` では全部止まる。
- 🔴 App は `view === 'daytrade' | 'swing'` のどちらでも `BasementRooms` を出し、`initial` で最初の部屋を決める
  （DATA のカードから来た側が開く）。**壁は1回だけ描かれる**ので、移動しても部屋の外は動かない。
- 🔵 実画面で確認済み（スイング→デイのスライド・端の矢印・現在地の更新）。会員ゲートの一時解除は戻してある。

### 2026-08-16(33): 地下室をひと続きにする／資料カードの並び替え

🔴 ユーザー指示3件。

1. **地下室の部屋どうしを行き来できるようにした**（スイング → デイ → スイング）。
   - `basementTheme.ts` の **`BASEMENT_ROOMS` が単一情報源**（key・表示名・短縮名・記号・判定の短文）。
     部屋を増やすときはここに1行足すだけで、切替も「隣の部屋」も追従する。
     🔵 `key` は App の ViewMode と同じ文字列（`daytrade` / `swing`）にしてあるのでそのまま渡せる。
   - **ヘッダーに切替（`BasementRoomSwitch`）**＝いま居る部屋を琥珀色で示し、押すともう一方へ。
     スマホは「デイ／スイング」の短縮名＋パンくずの副題を省いて詰める。
   - **ページ末尾に「NEXT ROOM」カード（`BasementNextRoom`）**＝隣の部屋の記号・名前・判定の短文を出す。
     読み終わりがそのまま次の入口になる。
   - App 側は `onSwitchRoom={(k) => setViewWithTransition(k)}` を渡すだけ。
2. **DATA＞研究資料の並び順**＝ぽいロボは **戦略プレイブック → 説明書**、
   未来ガジェットは **波動の書 → タイムマシン → PER市場温度計**（`NoteView.tsx` の `ARTICLES` の順がそのまま画面の順）。
3. 🔵 **実画面で確認済み**（スイング→デイ→スイングの往復・並び順とも）。会員ゲートは一時的に外して確認し、**戻してある**。

### 2026-08-16(32): 地下室の背景を作り込む（部屋そのものを描く）

🔴 ユーザー指示＝「背景をもっと地下室感」。`BasementBackdrop`（`basementKit.tsx`）を新設し、
両ページの背景を**1枚の部屋**にした。旧 `concreteStyle` / `BULB_GLOW` は役目を終えたので削除。

重ねているもの＝①コンクリートの地色（上が暖かく足元が沈む）②粒子（feTurbulence の粉っぽさ）
③打ち継ぎの目地＋その影 ④壁のシミ3か所 ⑤**吊り下げた裸電球**（右上・ゆっくり揺れる／
コード・ソケット・フィラメントまで SVG）と**そこから落ちる光の筋**（縁をぼかして空気に散らす）
⑥光の中を舞う埃5粒 ⑦ビネット ⑧床との境。

- 🔴 **読みやすさを優先**＝カードは半透明を濃いめに直し（`basementColors.card`）、`blur` と影を足して
  壁の目地が文字の裏を通らないようにした。ヘッダーは不透明色をやめて `basementVeil()`（半透明＋blur）。
- 🔵 画像は使わない（CSS と SVG のみ）。揺れ・埃・明滅は `prefers-reduced-motion` で止まる
  （`.bsmt-lamp` / `.bsmt-glow` / `.bsmt-dust` は index.css に既存のものを再利用）。
- 🔵 **ローカルで実画面を確認済み**（dev サーバー＋プレビューリンクで両ページを目視）。
  🔴 会員限定ゲートを一時的に外して確認したが、**確認後に戻してある**（`NoteView.tsx` は無変更）。

### 2026-08-16(31): カレンダーの月次イベント帯から決算関連を全削除

🔴 ユーザー指示＝「1Q決算発表（中旬まで）のような決算関連の月毎の帯は全て削除。仕様からも実装からも消し、
空いたエリアは詰める」。

- `src/utils/earningsSeason.ts` → **`src/utils/monthBands.ts` に改名**（決算を扱わなくなったため）。
  消したのは「1Q/中間/3Q/本決算ピーク」4件と「1Q決算発表（中旬まで）」1件＝**8月は帯そのものが消滅**。
  残したのは日付が決まっている指数イベントだけ（日経平均の定期入替・MSCI定期レビュー・TOPIX浮動株比率見直し）。
- 🔴 **帯が無い月は枠ごと出さない**（月・週・日の3ビューとも）。旧実装は `visibility: hidden` で
  高さを確保していたので、空の帯のぶんカレンダーが狭いままだった。詰めた結果、月ビューは
  グリッド（`flex:1`）がその高さを吸う。
- システム仕様書（研究室＞資料）の「月次イベント帯」節も同時に直した（決算を載せない・空の月は出さない）。
- テスト 476件 全green／tsc 0／lint 0／build OK。

### 2026-08-14(29): 判断の答え合わせを作った（⑤b-A ＋ ⑤b-B）

**テスト 472件・33ファイル 全green（+16）／tsc 0／build OK。** 🔴 判断のしかたには一切触っていない。

これまで判断ログ（`public/data/robo_logs/*.json`）には `input`/`output`/`shadows` しか無く、
**「その後どうなったか」が入っていなかった**＝後から学習させようにも材料が無かった。そこを埋める器を作った。

| 追加したもの | 中身 |
|---|---|
| `src/utils/roboOutcome.mjs` | 採点の純関数（`computeOutcome` / `calibrationBins` / `summarize`）。テスト16件 |
| `scripts/robo-outcome.mjs` | 実行本体。ログに `outcome` を書き足し、`public/data/robo_calibration.json` を出す |
| `npm run robo-outcome` | 手で走らせるとき（`--dry` で書き込みなし） |
| Actions | 「判断の答え合わせ」ステップを**判断の後**に追加（`continue-on-error`・**LLMを呼ばないので費用ゼロ**） |

**測り方**（`basis` にも書いてある）
- 起点＝**判断した日の終値**。執行が引成（MOC）なので**実際の約定基準と同じ**。
  🔴 引継ぎの当初案は「翌営業日の寄付き」だったが、8/12 に執行が変わったので実態に合わせた。
  寄り基準で見直したくなったとき用に `next_open` も残してある。
- 測る対象＝**日経225の終値**（1321/1570/1571/1357 は倍率が違うだけで方向は同じ）。
- horizon＝**1営業日後・5営業日後**。`hit` は方向の当否、`edge_pct` は向きから見た損益率（ベアは符号反転）。
- 🔴 **hold は当否を付けない**（何もしなかった日を勝率に混ぜない）。引き分けは勝ちにしない。
- 🔴 **これは「判断が短期で当たったか」であって建玉の損益ではない**（実際は損切り・トレーリングまで持つ）。

🔵 **影の判断（チャートのみ／数値のみ）も同じ物差しで採点する**＝30件貯まったときの3通り比較の材料になる。
　 旧「`compare-shadows.mjs` を作る」構想はこれに統合した（`variants` に3通り並ぶ）。
🔵 **何度走らせても同じ結果**。5営業日後がまだ来ていないぶんは `null` のままで、日が経つと勝手に埋まる。

**いまの中身（3件・まだ判断材料にならない）**
```
2026-08-10 hold      確信度42% → 1日 +0.83%      / 5日 未確定
2026-08-12 bull 1321 確信度56% → 1日 +1.16% ○   / 5日 未確定
2026-08-13 bull 1321 確信度58% → 1日 +0.45% ○   / 5日 未確定
```
🔴 **採点できた判断は 5日後で0件**。**30件貯まるまで比較しない**（途中で乗り換えない）。

🔵 画面には出していない（JSONと実行ログまで）。成績タブに出すかは貯まってから決める。

**🔴 次のセッションで判断が要ること**
1. ✅ **push するか** → 2026-08-16 に push 済み（第30）。
2. ✅ **成績タブに出すか** → 2026-08-16 に「n<30 は暫定と明記」で表示（第30）。
3. 🔵 **8/13 の判断（1321 14口）の約定が入ったら**、`outcome` と口座の損益が別物であることを
   一度見比べておくと、以後の読み違いが減る（`outcome` は5日固定・口座は手仕舞うまで）。

### 2026-08-14(28): 状態の点検と、システム仕様書の全文突合

**点検の結果＝壊れているところは無し。**

- **口座の配線は生きている**＝`--dry --no-llm --force` で通し確認。8/13 の判断（open 1321 14口）が
  `account.pending` に積まれており、**今日 8/14 の実行で引け値 70,730 円で約定する**見込み（評価額 1,007,164 円）。
  🔵 8/12 の注文が建っていないのは不具合ではなく、`29da51b`「発注期限（15:25）を過ぎた判断は口座に積まない」で
  意図的に落としたぶん。ポジション未保有・確定トレード0件は想定どおり。
- **影の判断（3通り）の記録はまだ0件**＝仕組みを入れたのが 8/13 の実行より後なので、
  `public/data/robo_logs/*.json` に `shadows` がまだ入っていない。**今日の実行が1件目**。30件まで比較しない。
- **テスト 456件・32ファイル 全green／tsc 0／build OK**。ローカルと origin は一致・未コミットなし。
- データは 8/13 ぶんまで（本日ぶんは 19:30／21:30 の取得回で入る）。

**🔴 システム仕様書（研究室＞資料＞システム仕様）を全文、実装と突き合わせて8件是正した。**
名称入替（8/09）・タブ再編・ロボ口座化・周期の独立ページ化を経て、**画面の構成を書いた箇所が実装と食い違っていた**。

| 直したところ | 実装（正） |
|---|---|
| 概要「シールド＝シールド/周期の2タブ」 | **シールド / 環境 / 現物 / 先物 の4タブ**（`QUANT_TABS`） |
| 概要「エンジン＝4タブ・保有ポジション管理」 | **ロボ口座 / 成績 / 履歴 の3タブ**（`ENGINE_TABS`）・**管理者のみ表示** |
| 周期（セクター）の記載なし | **独立ページ**（ViewMode `sector`）・入口はサイドバーのバナー |
| 「エンジンビュー」節まるごと（NK225データ／画面構成／AIプロンプト） | 2026-08-09 に**機能ごと削除済み**（節を削除し、削除の事実と保持データだけ残した） |
| フッタータブ順が3か所で食い違い | **カレンダー / チャート / シールド / エンジン / 研究室**（`MAIN_VIEWS` が単一情報源） |
| `poical-nk-futures-price-v4` | **v5**（ma25_dev 追加でスキーマ変更） |
| AIヒントの対象ファイルと行番号（QuantView 1479 等） | **`CyberAiLaunch.tsx` の `AI_LINKS` 1か所**（Claude「ぽいロボ推奨」も追加済み）。🔴 行番号は書かない運用に変更 |
| 資料カード一覧・説明書の説明 | NoteView の実際の分類（ぽいロボ／地下室／基礎／インジケーター／イベントドリブン／未来ガジェット／管理メニュー）・**説明書は18枚スライド** |
| URL `/stock-calendar` | 正は **`/calendar/`**（vite の base。旧URLでも同じ画面は開く） |

🔵 概要に「最終突合 2026-08-14」を追加＝**★日付つきの項目は当時の記録**で後から上書きされることがある、と読み方を明記した。

🔴 **次に判断が要ること（変更なし）**＝影の判断が30件貯まったら比較する。それまで設計は動かさない。

### 2026-08-13 の総括（27c〜27n）

**画面**＝説明書を18枚のスライドに／地下室は鍵つきで全員に見せる／**波動の書を全5巻＋索引（73点）に**
（巻零・理＝ダウ理論＋線／巻一・灯＝ローソク＋窓／巻二・形／巻三・波＝エリオット＋一目／巻四・力＝出来高）。
図は画像を使わずSVGで、スクロールして見えたものだけ動く。

**研究**＝一目均衡表を26年で測って**当たらない**ことを確認（型はt<2・水準論は「前の山と同値」に負ける）。
🔵 ただし**検証結果は画面に出さない**＝波動の書は読み物、検証は地下室、という住み分けにした。

**仕込み**＝図鑑の型を機械判定できる条件（`atlasRules.mjs`）に落とし、26年から拾って正解ラベル化。
図鑑をカタログJSONに書き出してAIに読ませる `atlas-read.mjs` まで通した。
🔴 いずれも**判断には入れていない**。影で記録して測ってから決める。

**運用**＝チャート画像をチャートだけにし、出来高が出る先物（SGX:NK1!）に変更。
自動実行の時刻をカレンダーの「システム」層に出した（🖥 撮影だけPCが要る）。

🔴 **次に判断が要ること**＝影の判断（本番／チャートのみ／数値のみ）が30件貯まったら比較する。
それまで設計は動かさない。

### 2026-08-13(27n): 影の判断を2本走らせる（材料を減らすと成績はどうなるか）

- 🆕 `scripts/shadowDecide.mjs`＝同じ日に**3通り**の判断を出して記録する。
  ①本番（需給＋価格＋チャート）②チャートのみ ③数値のみ。**建てるのは①だけ**
- 🔴 影は口座・注文・通知に一切触らない。失敗しても本番を止めない（例外はここで飲む）。
  `--no-shadow` / `ROBO_SHADOW=0` で止められる
- 🔵 きっかけ＝「チャートだけで判断させたら現行に勝つのか」（ユーザーの問い）。
  🔴 ②は過去に遡って再現できない（当時の画像が無い）ので、貯める以外に測る道がない。
  **30トレード貯まるまで比較しない**（途中で乗り換えない）
- 初回の空撃ちで3通りが割れた＝本番 hold／チャートのみ open 1570 5口／数値のみ hold。
  記録は `data/robo_logs/<日付>.json` の `shadows` に入る
- API費用は判断1回ぶんが3倍（1日1回なので実額は小さい）

### 2026-08-13(27m): 波動の書をAIに食わせる（🔴 影で記録するだけ・判断には混ぜない）

- 🆕 `scripts/export-atlas.mjs`＝画面（`ChartPatternPanel.tsx`）を唯一の情報源として
  `data/atlas/catalog.json` を書き出す（**73点**＝巻零12/巻一23/巻二16/巻三16/巻四6）。
  カタログを手書きしないのは、図鑑を直したのにAIには古い説明が渡る、というズレを作らないため
- 🆕 `scripts/atlas-read.mjs`＝チャート画像＋カタログ → 見えた型を JSON で返させる。
  初回実測OK（日足で赤三兵70%/N波60%/ダブルトップ45%/大陰線65%/ダウ第六原則60%・in 10k/out 1.9k tokens）
- 🔴 **判断には入れない。影で走らせて記録するだけ**（ユーザー指摘＝型を足すと成績が悪くなる可能性がある）。
  🔵 貯めた結果が良ければ、そのとき初めて `robo-trade.mjs` に入れて正とする
- 🔵 測るものを2つに分ける＝①**読み取りの正しさ**（`atlas-detect.mjs` の機械判定と突き合わせ）
  ②**型の効き**（地下室で先のリターンを測る）。①が悪ければプロンプト、②が悪ければ型そのものの問題
- プロンプトの要点＝図鑑に無い名前を作らせない／**何も無ければ空配列**（図鑑を渡すと何か見つけたくなる）／
  価格の数値は読ませない／出来高が無い画像では巻四を挙げさせない

### 2026-08-13(27l): カレンダーに「システム」層を出す（自動実行の時刻）

- 🆕 `src/utils/roboSchedule.ts`＝自動実行の時刻を1か所に。週・日ビューの時間帯にブロックで出る。
  サイドバーの「システム」で切替（旧「ぽいロボの動き」）（既定オフ・localStorage 保存）
- 平日＝**15:00 AI判断**（引成なので15:25までに発注）／**16:00 チャート撮影**／16:30 5分足アーカイブ／
  19:30・21:30 データ更新。土曜＝09:00 週次集計。🔴 休場日は出さない
- 🔴 名前は「システム」（旧「ぽいロボの動き」）。**管理者と会員だけに出す**（`canSeeSystem`＝
  `isAdminUser || isMember`）。中の運用の話なので地下室と同じ扱い。既定はオフのまま
- 🔵 **ブロックを押すと「何をしているか」が出る**（BadgePopup・PCが要るものは冒頭に🖥の断り）
- 🔴 **撮影だけ色が違う（黄）＝PCが起動していないと動かない**。日ビューには「🖥 PCを開けておく」も出す。
  それ以外は GitHub Actions なのでPCの状態に関係ない
- 🔴 `roboSchedule.ts` は**実際の設定の写し**。時刻を変えるときは fetch-data.yml /
  archive-intraday.yml / タスクスケジューラと両方直すこと。テスト4件つき

### 2026-08-13(27k): ロボに渡すチャート画像をチャートだけにする

- 🔵 **ノイズを外す**（ユーザー指示）＝右のウォッチリストと左の描画ツールをCSSで消し、
  **中央のチャート領域だけを切り出して**撮る（消すだけだと右に余白が残る）。撮って目視確認済み
- 🔴 **シンボルを INDEX:NKY → SGX:NK1!（日経225先物）に変更**。現物指数には出来高データが無く、
  出来高が描けなかった。巻四（出来高の型）を読ませる以上、出来高なしの画像では意味が半分になる
- 🔵 手撮り（デスクトップアプリ）も同じ方針＝ウォッチリストを畳み、**ローソク＋出来高だけ**にする。
  MACD は外す（ピクセルから正確に読めず、下段を占領してローソクが小さくなるため）

### 2026-08-13(27j): 図鑑の型を機械が判定できる条件に落とす（AI読み取りの下ごしらえ）

- 🆕 `scripts/atlasRules.mjs`＝**巻一（1本もの6種＋窓）の数値条件を1か所に**。
  実体/ヒゲ/値幅は「直近20本の中央値との比」で見る（年代で値幅の桁が違うため絶対値は使わない）。
  🔴 しきい値は**先に決めた**もの。当たるように後から動かさない
- 🆕 `scripts/atlas-detect.mjs`＝26年から型を機械で拾い、`data/atlas/n225.json` に書き出す。
  🔵 これは**AIにチャート画像を読ませたときの答え合わせ（正解ラベル）**に使うためのもの。
  人手でラベルを付けずに精度を測れるようにする
- 実測（日経225・6,361営業日）＝大陽線772/大陰線679/十字線527/コマ512/カラカサ315/トンカチ236。
  窓は1,385回で**88%が120本以内に埋まる（中央値4本）／173回は埋まらない**
- 🔴 分類は「その他の窓」を必ず持つ（当初これが無く、放れの窓が1,029回と過大に出ていた）。
  尽きの窓の「5本以内に100%埋まる」は**定義そのもの**（走った先の窓＋5本以内で埋め戻し）なので発見ではない
- 🔵 残り＝巻二（形）の条件化。6種は `analyze-chart-patterns.mjs` に実装済みなので、
  そこから atlasRules へ移すのが次の一歩

### 2026-08-13(27i): 波動の書を全5巻に（ダウ理論・窓・出来高・線・ダイアゴナル・ハーモニック）

- 🆕 **巻零・理（ダウ理論）＝トップの既定タブ**。6原則（織り込み／3つの波／3段階／相互確認／
  出来高で確認／転換の合図まで続く）＋ **線の引き方6種**（支持・抵抗・役割転換・トレンドライン・
  チャネル・ラインのだまし）。🔵 トレンドの定義が決まらないと他の巻が「何の」形か言えないので巻頭
- 🆕 **巻一に窓5種**（普通・窓埋め・放れ・中間・尽き）。`CandleFigure` に `gapAt` を足し、
  前の足との空きに帯を敷く。絞り込みに「窓」を追加
- 🆕 **巻三に推進の変形4種**（エンディング／リーディングダイアゴナル・延長・フェイラー）と
  **ハーモニック4種**（AB=CD・ガートレー・バタフライ・クラブ）
- 🆕 **巻四・力（出来高6型）**＝順行／息切れ／出来高を伴う放れ／だまし／セリングクライマックス／
  閑散に売りなし。🔵 この書で**値段以外を見るのはここだけ**。`VolumeFigure`（上に値動き・下に棒）
- 🔵 索引は全5巻ぶんに拡張（54点）。スマホは2列・タブは説明を落として横に収める

### 2026-08-13(27h): 波動の書を図鑑に寄せる（通し番号・絞り込み・索引）

- 🆕 **通し番号**＝各カードに `No.01` … 巻ごとの連番。図鑑は番号で引けることが要点
- 🆕 **分類チップ**＝巻一は `1本/2本/3本`（既存）、巻二に `反転/継続` を追加
- 🆕 **絞り込み**＝巻一は 1本/2本/3本以上、巻二は 天井/底/継続。件数つきの丸ボタン（`FilterBar`）
- 🆕 **索引タブ（索引・目）**＝3巻の全型（18+16+8＝42点）を図だけのサムネイルで一覧。
  押すとその巻へ移動し、絞り込みを「すべて」に戻して、その項目まで smooth scroll ＋ 展開（`jump`）
- 🔵 巻三のカードも他と同じ**表は1行・詳しくはタップ**に統一（`WaveCard` に `detail` を追加）
- 🔵 図鑑の見た目は動きも含めて統一＝タイルは hover で浮いて光り、絞り込みチップも選択で光る

### 2026-08-13(27g): 巻の並べ替え・スクロールで図が動く・一目均衡表を測って巻三に追加

- 🔵 **巻の順を「短い足あと順」に**＝巻一・灯（ローソク足18型）／巻二・形（16種）／巻三・波。既定は巻一
- 🔵 **図はスクロールして見えたときに動く**（`useInView`＋IntersectionObserver・threshold 0.2・一度きり）。
  全部が一斉に動くと目の置き場が無いため（ユーザー指摘）。カードは `PatternCard`／`CandleCard`／
  `WaveCard` に切り出し、`WaveFigure`・`CandleFigure` は `show` を受け取る
- 🆕 **一目均衡表の波動論を巻三に追加**（エリオットと同じ「波の数え方」なので巻は分けない・ユーザー判断）。
  🔵 **エリオットと同じ流れ**＝全体像を1枚（N波＋目標水準）→ 波の型 I/V/N/P/Y → 水準論（V・N・E・NT）→ 時間論（9・17・26）
- 🔴 **載せる前に26年で測った**（`scripts/analyze-ichimoku.mjs` 新規・日経225・6,361営業日）。結論＝**当たらない**。
  🔵 ただし**この結果は画面には出さない**（2026-08-13 ユーザー判断）＝波動の書は読み物、検証は地下室でやる住み分け。
  スクリプトは残してあるので走らせれば再現できる
  - 型（三役好転・雲抜け・転換/基準クロス）… 基準比は小さく **t はすべて2未満**。パラメータを
    (9,26,52)→(7,22,44)/(12,30,60) と振ると符号が入れ替わる。🔴 **三役逆転（売り）の後に20日で
    +1.08〜+1.25%**と基準より上げており、合図と逆に動いている
  - 時間論 … 9日±1 は 23〜26%（一様11.5%）と多いが、間隔の分布は5日が最頻で単調減衰＝
    **「9が特別」ではなく短い波が多いだけ**。17日・26日は一様以下
  - 水準論 … 誤差中央値は NT/N 3.5%・V 4.4%・E 7.1%。**「前の山と同値」と置くだけの予想が 1.8%** で最も近い
  - 🔴 t は**基準（全日平均）との差**で計算している。0との差で測ると株が上がった分まで「効いた」になる

### 2026-08-13(27f): 波動の書に「灯（ローソク足の型18）」

- 🆕 **灯（のちに巻一）**＝1〜3本で読む型を18種（`CANDLES`）。1本＝大陽/大陰/十字/カラカサ/トンカチ/コマ、
  2本＝包み/はらみ/かぶせ/切り込み/毛抜き天井/毛抜き底、3本以上＝明けの明星/宵の明星/赤三兵/
  三羽烏/三空踏み上げ/上げ三法
- 🔵 **酒田五法のうち 三川・三空・三兵・三法 はここ**。残る「三山」は三尊そのものなので巻一に置いてある
  （同じものを別の粒度で見ている、という整理）
- 図は `CandleFigure`＝0-100 の価格座標を上下反転して実体＋ヒゲを描く。陽＝緑・陰＝青・十字＝白線、
  1本ずつ順に灯る。カードの作りは巻一と同じ（タップで見つけ方）
- 🔵 ローソクは**成立日が機械的に決まる**ので、巻一と同じ土俵で26年を測れる。
  🔴 実測はまだ入れていない（`scripts/analyze-candles.mjs` は未作成）

### 2026-08-13(27e): 波動の書に「巻二・波（エリオット波動）」／説明書は1枚1トピックに

- 🆕 **波動の書を2巻構成に**（`ChartPatternPanel`）。**巻一・形**＝従来の16種／**巻二・波**＝エリオット波動。
  🔴 **巻を分ける理由**＝形はネックライン割れ等で機械的に定義でき26年ぶんを測れたが、
  波は**数え方が一意に決まらない**（同じチャートで人により第3波／第1波と割れる）。
  同じ棚に並べると「どちらも同じ根拠」に見えるため隔てた
- 巻二の中身＝全体像（推進5波＋修正3波・線が描かれる演出）／**三つの掟**（2波は始点を割らない・
  3波は最短にならない・4波は1波の高値に重ならない）／**修正の型3種**（ジグザグ・フラット・
  トライアングル）／目安（オルタネーション・フィボナッチ・チャネル）
- 🔵 巻二に置いていた**立場の断り書き2つ（巻頭「この巻の立場」・巻末「なぜ数えないのか」）は削除**
  （2026-08-13 ユーザー判断）。図鑑として読む邪魔になるため。巻を分けている事実自体が区別になっている。
  🔴 判断に使っていないという設計は変わらない（コード先頭のコメントに残してある）。巻二に ? は出さない
- パンくず・画面タイトルも「波動の書」に統一（ページ内の16種の説明は据え置き）
- 🔴 **説明書は1枚＝1トピックに割り直した**（7章→**18枚**）。章の中のタブ解説も1枚ずつ。
  章まるごと1枚だとスライド内でスクロールが出て「めくる」と「スクロール」が混ざるため（ユーザー指摘）。
  本文は1カラム（横に切れない）。目次は章単位のままで、章の1枚目へ飛ぶ。ヘッダーとドットに
  章内の `n / m` を表示

### 2026-08-13(27d): 説明書をスライド化・地下室は鍵つきで公開・「波動の書」

- 🆕 **説明書（`ManualView`）を1章＝1枚のスライドに**（戦略プレイブック／タイムマシンと同じ作法）。
  🔵 ただし説明書は「調べもの」でもあるので、**PCの目次は残し、押した章へ一発で飛ぶ**。
  背景に巨大な章番号（01〜07）がスライドイン・進捗バー・`1 / 7` カウンタ・章末に NEXT 予告。
  矢印／キーボード（↑↓・Space・PageUp/Down・Esc）／スワイプ。
  🔴 スワイプは**章内スクロールが端に着いた向きにだけ**送る（長い章が読めなくなるため）
- 🔵 **地下室は隠さず、鍵をかけて見せる**方針に変更（ユーザー判断）。カードは全員に表示し、
  非会員にはすりガラス＋`🔒 会員限定`。中身のアクセス条件は従来どおり（管理者・会員のみ）。
  存在すら見えないと「無い」のと同じで、入る動機にならない
- 「波動探知機」→ **波動の書**（カード名のみ。ページ内は据え置き）。
  🆕 専用バナー `ScrollThumb`＝**チャート自身が筆で書き残した巻物**の見立て。和紙・巻き軸・
  にじみ・落款「波」・縦書きの題箋。墨の一筆（押し目→上昇→天井→下げ）が 2.6 秒で書かれていく。
  🔴 ここも画像は使わず CSS と SVG のみ。`prefers-reduced-motion` で止まる
- 地下室ページの `Head` / `P` を module scope へ（レンダーのたびに別コンポーネント扱いになっていた）。
  これで `npm run lint` はエラー0

### 2026-08-13(27c): 画面まわり（地下室・波動探知機・フォーメーションの実測非表示）

- 🆕 **DATA＞研究資料に「地下室」を新設**（ぽいロボの直下）。まだ人に見せる形になっていない
  研究を置く場所。カード＝**デイトレード**（今日の検証記録）／**スイングトレード**（近日公開）。
  🔴 表示は全員・**中身は管理者と会員のみ**（`memberOnly`／2026-08-13(27d)で「隠す」から変更）
- 専用バナー `BasementThumb`＝コンクリートの目地・吊り下げた裸電球（ゆっくり揺れて瞬く）・
  舞う埃・階段。🔴 画像は使わず CSS と SVG だけ。`prefers-reduced-motion` で止まる
- ページ（`DaytradeResearchView`）も同じ配色（暖色＋薄暗い地）。上から電球の光が落ちる
- 🆕 **スイングトレード**のページも開けるようにした（`SwingResearchView`）。
  この時間軸は**材料も検証も既にある**ので「何が分かっていて、何がまだ分からないか」の1枚にした。
  🔴 いちばん大事な数字として**最大DD −46%**を独立の節にしている
  （判断の精度より「この谷を通り抜けられる金額で張ること」が効く）。
  🔴 **30トレード貯まるまで設計は動かさない**と明記（途中で変えると比較に使えない）
- 地下室ページの配色は `basementTheme.ts` に切り出して共有（ページが増えても色がバラけない）
- 「フォーメーション分析」→ **波動探知機** に改名（カード名のみ。ページ内は変更なし）
- フォーメーション16種の「日経225・26年で測った結果」は**非表示**（ユーザー判断）。
  t<2 が多く判断材料にならないため。🔴 データ（`measured`）は残してあるので戻せる

### 2026-08-13(27b): 確信度の較正をやり直した → **五分五分（52%以下）は出さない**

**きっかけ**＝「材料を増やさずに確信度を較正し直す」（ユーザー判断）。測ったら別の問題が出た。

**分かったこと**

前回（2026-06-10）の圧縮（傾き0.5→0.3・上限70）は**反転を解消していた**（確信度と勝敗の相関 +0.35）。
ただし副作用として、**38週中36週が50-59%帯に潰れ、確信度が実質「常に53%」の定数**になっていた。
🔴 **情報を持たない数字をパーセントで見せるのは、ズレているより質が悪い**（読む人は意味があると思う）。

| 確信度帯 | n | 実勝率 |
|---|---|---|
| 50-52% | 14 | **21%** |
| 53-55% | 20 | 60% |
| 56-60% | 2 | 50% |
| 61%以上 | 2 | 100% |

50-52%帯の中身は |TEV| が 27〜92 とバラバラで、**シグナルが弱いから確信度が低いわけではなかった**（12/14が bear）。
Fisher 正確検定 **p=0.021**。

**やったこと＝五分五分ゲート**

確信度 **52%以下なら `tev_lowConviction=true`＝シグナルを出さない**（`LOW_CONVICTION_MAX`）。

🔵 **これはデータへのフィッティングではなく設計の整合**。確信度50%＝「五分五分」と自分で言っているのに
売買シグナルを出すのは矛盾している。だから根拠の順序は「原理として正しいか」が先、数字は裏付け。

- シグナル 38 → **24週**（−37%）／全体勝率 **47% → 63%**
- 較正のズレ 50-59%帯 gap −9.1 → **+4.1**（ほぼ整合）
- 見送った14週の方向は実勝率21%＝**捨てたほうが良かった**
- 🔴 **63%は同じデータで閾値を決めた in-sample の数字**。実運用ではこれより低く出る。
  見送った週も方向は `shadow_signal` に記録し続けるので、標本が増えたら測り直せる

**触った場所**（式は単一情報源に集約したまま）

| ファイル | 変更 |
|---|---|
| `src/utils/tevCore.mjs` | `LOW_CONVICTION_MAX` / `tev_lowConviction` を追加。**符号（方向）は変えない** |
| `scripts/backtest-tev.mjs` | 五分五分は neutral 扱い。`shadow_signal` / `shadow_win` を記録 |
| `src/utils/engineExport.ts` | `tev_analysis.low_conviction` を出力 |
| `src/utils/enginePrompt.ts` | 〜52%は「五分五分／見送り」の帯。この帯では方向を根拠にした売買の話を書かせない |
| `src/components/BacktestPanel.tsx` | 見送った週数と「もし出していたら」の勝率を画面に出す |

🔴 **踏んだ落とし穴**＝`backtest-tev.mjs` の `computeTEV` は tevCore の結果を**詰め替えて**返しており、
新しい項目を足し忘れたため**ゲートが黙って無効**になっていた（最初の再実行で数字が1つも変わらず気づいた）。
詰め替えのある場所にはコメントを残した。

**他のバックテストへの波及＝なし**（システムv1〜v4・トレンドフォロー・ロボ対照群は価格ベースでTEVを使わない）。
ロボ対照群を再実行した現在値（Go/No-Goの基準）＝
**ETF実データで 72トレード・勝率46%・CAGR 13.09%・最大DD −46.15%**。

### 2026-08-13(27): 🔴 判断に渡す材料が2つ欠けていた（明日の本番実行で実害が出るところだった）

`--dry` の実走で発見。**自動テストでは出ない類**（外部の画像と外部APIが絡むため）。

**【1】チャートを毎日どちらか片方しか見ていなかった**

撮影側（`capture-tradingview.mjs`）は毎日**日足と週足の2枚**を上げているのに、
受け取り側（`fetchLatestImages`）が「最初に見つけた1枚」しか返していなかった。
そのため **8/12は週足だけ・8/13は日足だけ**を見て判断していた。

- → 日足・週足を別々に返し（`chartDaily` / `chartWeekly`）、**両方**をLLMへ渡す
- → 画像の直前に名札（`【日経平均 日足チャート】`）を入れる。並べて渡すと取り違えるため
- → プロンプトにも役割を明記（**日足＝短期の形／週足＝大きな流れ**）
- 🔵 修正後の実走で「日足は…陽線連続、**週足も上昇トレンドの押し目回復の形**」と、
  両方を使った判断が出ることを確認

**【2】保有画面の読み取りが信用建玉を捨てていた**

`readPosition.mjs` のプロンプトが「**現物の**保有を読む」と指示していたため、
松井証券の「建玉一覧」を読ませると **positions が空**になっていた。
読み取りAI自身が note に「現物ではないので空にした」と正直に書いていたのに、
呼び出し側はそれを「保有なし」と受け取り、**口座を全部手仕舞いに同期する**ところだった。

🔴 **ぽいロボは信用スイング前提**なので、指示のほうが実態と合っていない。

- → 現物・信用のどちらも読む。`kind`（`cash` / `margin_long` / `margin_short` / `unknown`）を必ず記録
- → 🔴 **売建は下げに賭ける建玉**。買建と同じ扱いにすると方向が丸ごと逆になるので、
  検知したら警告を出して**同期はしない**（人に確認を促す）
- 🔵 修正後、1321の信用買建（14口・建単価70,700・評価損益−84円）を
  `kind=margin_long` として正しく読めることを確認。
  「画面下部の『7株』は注文通知であって保有数量ではない」と引っかけも回避していた

🔵 **教訓＝画像と外部APIが絡む部分は、`--dry` の実走でしか壊れが見えない。**
点火の前後で必ず1回は通しで流し、**出てきた文面を目で読む**こと。

### 2026-08-12(26): 🔴🔴 空売り比率が**ずっと間違った列**だった（7〜16ポイント低く出ていた）

ユーザーが外部の解説動画のスプレッドシート（日次の空売り比率・2市場信用倍率）を見せてくれたのがきっかけ。
**信用倍率は完全一致**（7/31: 買残6,202,268／売残728,711／8.51／−8.44）だったが、**空売り比率だけ食い違った**。

| 日付 | 外部の公表値 | ぽいロボ（誤） | 正しい内訳 |
|---|---|---|---|
| 2026-08-10 | 41.1 | **25.0** | 33.6（規制なし）＋ 7.5（規制あり） |
| 2026-08-03 | 45.4 | **35.5** | 34.1 ＋ 11.3 |
| 2026-07-31 | 43.8 | **29.4** | 35.1 ＋ 8.7 |

🔴 **原因**＝`daily2year.json` の **col[11]**（別の指標）を空売り比率として読んでいた。
正しくは **col[22]（価格規制なし）＋ col[24]（価格規制あり）**＝東証の定義そのもの。
11日ぶんを突き合わせて**ズレ0.00ポイント**で一致することを確認した。

🔴 **なぜ気づけなかったか**＝列を「値の範囲（16〜71）」で**自動検出**していた。col[11] もその範囲に収まる
「それらしい数字」だったため、検出は成功しているように見えていた。
🔵 **教訓＝範囲で列を当てにいかない。列は固定し、意味のある検算（内訳の和・水準の妥当性）で守る。**

### 直したこと（すべて本番反映済み）

1. `scripts/shortSell.mjs` を新設（列固定＋検算＝合計が10〜80%を外れたらその日を捨てる）。テスト5件
2. `short_sell.json` を**日次でも保存**（約2年ぶん・規制あり／なしの内訳つき）。週次（`data`）は従来どおり
   「その週の最終営業日の値」で、画面の形は変えない
3. 画面：需給指標パネルの上に**直近10営業日の日次**を出す（40%以上は色を変える／ツールチップで内訳）
4. エンジンのプロンプト：**コード変更は不要**だった。空売り比率は
   **系列内の百分位（`pctRank`）**で使っており、絶対値のしきい値は持っていない。系列全体が正しくなったので
   出力もそのまま正しくなる（表示される実数も正しい値に変わる）
5. 過去2年ぶんを取り直し（`npm run fetch-data`）＝週次52件／日次500件

### 2026-08-12(25): 🔴 15:00の判断がChatworkに届かなかった＝**GitHubのスケジュール遅延**

ユーザーから「15:00に通知が来ていない」。調べたところ**ぽいロボ側の不具合ではなく**、
GitHub Actions の `schedule` が遅れていた。

| | |
|---|---|
| 事実 | 15:00 JST（`0 6 * * 1-5`）の回が **16:41 JST に起動**（**1時間41分の遅れ**）。ジョブ自体は success で、判断ステップも通っていた |
| なぜ困るか | 運用の約束は「15:00の通知を見て**15:25までに引成（MOC）で発注**」。遅れると発注に間に合わず、記録（終値で約定）と実物がズレる |
| 背景 | この cron を入れたのは 8/11 17:39（`08bf08c`）。**8/12 が初回**だった＝以前から壊れていたわけではない |

### 直したこと＝**早く起動して、ジョブの中で15:00まで待つ**

- cron を `0 6`（15:00）→ **`0 4`（13:00 JST）** に前倒しし、判断ステップの先頭で
  **15:00 JST まで `sleep` してから実行**する（2時間の余裕）。
- 🔵 **材料は古くならない**＝判断スクリプトは価格を**実行時に直接取りに行く**（`public/data` の JSON は使わない）。
  だから「早めに起動して待つ」だけで、15:00 時点の材料で判断できる。
- 🔵 **リポジトリが公開なので、待っているあいだの Actions 課金は発生しない**。
- 🔵 すでに15:00を過ぎて起動したときは**待たずに即実行**し、何秒遅れたかをログに出す
  （遅延が常態化していないか後から見るため）。
- 🔴 **二重に判断させないため、cron は1本のまま**（保険で2本にすると同じ日に2回判断してしまう）。

🔵 画面・手順書の「毎営業日 15:00 に判断」は**変更不要**（実際に判断するのは15:00のまま）。

### あわせて入れた2つ

1. 🔴 **発注期限（15:25）を過ぎた日は口座に積まない**（`robo-trade.mjs`）。
   8/12 は 16:42 に届き、発注できないのに `pending`（1321を14口）だけ残っていた＝**その pending は取り消した**。
   期限超過の日は通知の見出しを **「⚠ 本日は見送り（通知が遅れて発注期限を過ぎました）」** に変え、
   「記録していません／発注しないでください」を先頭2行で言い切る（本来の判断は下に残す）。`--force` で無視可。
2. 🔵 **休場日は待たずに終わる**（`scripts/market-open.mjs` を新設し、待つ前に終了コードで判定）。
   待ってから休場判定をすると、祝日でも2時間ランナーを占有してから「何もしない」と分かることになる。

### 🟡 まだ残っているタイムスケジュールの話

- データ更新側（19:30／21:30／土曜09:00）も**同じだけ遅れる**（実測：19:30→20:17、21:30→22:56、土曜09:00→09:59）。
  時刻厳守ではないので実害は無いが、**土曜の「データ更新のお知らせ」は1時間近く遅れて届くことがある**。
- 13:00 の回でもデータ取得とコミットが走る（当日終値はまだ無いので実質「前営業日ぶんの取り直し」）。
  🔴 **鮮度チェックは13:00の回では走らせないようにした**（2026-08-12・先回りで対処）。
  閾値（日次ファイル5日）は「その日のぶんが取れている」前提なので、**連休明けの13:00は誤検知する**。
  実際に計算すると **GW明けの木曜13:00＝最新は連休前の金曜＝6日前 > 5日 → 誤ってIssueが立つ**。
  🔵 年末年始は既に +7日 のスラックがある（`holidaySlack`）ので、そちらは元から誤検知しない。
  鮮度は当日ぶんを取ったあと（19:30／21:30／土曜）の回で見れば十分。

### 2026-08-12(24): プレビューモードを追加 ✅（`4ad6976` / `40276a2` push 済＝Vercel自動デプロイ）

会員限定ページを**「中身はダミー・閲覧だけ」**で見せるモード。設定のトグルではなく**URLで入る**。

🔗 **配るリンク**：`https://pointlab.vercel.app/stock-calendar/?preview=poirobo-preview-9f3a`
　（合言葉は `src/utils/previewMode.ts` の `PREVIEW_TOKEN` 1か所。変えると配布済みリンクは無効になる）

| | |
|---|---|
| 入り方 | 合言葉が一致したときだけ有効。**タブを閉じるまで続く**（sessionStorage）＝画面を移動してパラメータが消えても切れない。合言葉違いは黙って通常表示（「違う」と教えると総当たりの手がかりになる） |
| ダミー | 🔴 **個人のデータは実物を一切読まない**。メモ・スティッキーメモは固定のダミー（**今日から見た相対日付**で作る＝月をまたいでも空のカレンダーを見せない）。ロボ口座はデモ用の口座 |
| 需給データ | 🔴 **ダミー**（2026-08-12 追記指示）＝信用倍率／投資主体別売買動向／裁定残・騰落レシオ・空売り比率／日経平均の寄与度・業種別騰落率。**`dataCache.fetchWithCache` の1か所**で差し替える（`previewMarketData.ts` のキー表）ので、どの画面から呼ばれても同じ数字が出る。🔴 **乱数を使わない**（seed固定）＝開き直すたびに数字が変わると壊れて見える。🔴 **キャッシュには読み書きしない**＝通常表示に戻したとき偽の数字が残らないように |
| 環境・先物タブ | 🔴 **こちらもダミー**（2026-08-12 追加指示）＝VIX（日次・週次Δ）／NT倍率／USD・JPY／NASDAQ100／先物価格、CFTC手口／建玉残高・取引高・PCR。残る実データはニュース見出しだけ |
| 🔴 踏んだ落とし穴 | ①**VIXパネルだけ `dataCache` を通らない**（`VixPanel.tsx` が Yahoo を直接叩いて自前キャッシュ）＝キー表に足しても効かず、`fetchVix()` にも分岐を置いた。②**キーが v4→v5 に上がっていた**（`dataCache.ts` の purge 一覧が古い）＝実際に使われている `poical-nk-futures-price-v5` に合わせた。③**表は新しい順で保存されている**＝古い順で返したら表の先頭が去年の日付になった（`previewFuturesDaily` / `previewNkFuturesPrice` は計算後に反転） |
| 🔵 分かったこと | USD/JPY が「取得中」で止まるのは**プレビューと無関係の実バグ**。`usdjpy.json` が12時間より古くなると静的JSONを捨てて不安定なプロキシ経由のライブ取得へ落ちる（実測：更新から12.6時間で発生）。直すなら「古くてもまず表示してから裏で更新」か、判定時間を実際の更新間隔に合わせる |
| 表示制限 | 🔴 **セクターローテーションは非表示**（実データそのものでダミーに置き換えられない）。🔴 **カレンダーは今月だけ**（前後の月へは動かない・理由を出す）＝ダミーが尽きた月を見せると「中身が無いアプリ」に見えるため |
| 書き込み | 🔴 **全て無効**＝メモ保存/削除・スティッキーメモ・予約/取消・お問い合わせ送信・通知登録・ログイン。押すと右下に理由が出る |

- 実装＝`src/utils/previewMode.ts`（判定・`blockedInPreview()`・トースト）＋ `src/utils/previewData.ts`（ダミー）。テスト10件
- 🔴 **自動保存の経路は黙って捨てる**（`saveNote` / `saveStickyNotes`）。ここで理由を出すと**入力1文字ごとにトーストが飛ぶ**。
  理由を出すのは人が押したとき（保存ボタン・削除・送信など）だけ
- 🔴 **「保存しました」のトーストも止める**（`DayNotePanel` の保存ボタン）。保存していないのに成功の表示だけ出て、
  保存できたように見えていた（ローカルで実際に踏んで修正）
- 🔵 ロボ口座のダミーは既存の `roboDemo.ts`（`?demo=` 用）を動的 import で使い回す＝初期バンドルには載らない
- 🔵 ローカル確認済み（`?preview=…` でカレンダーが未ログインで開き、ダミーのメモ・スティッキーメモが出て、保存はブロックされる。
  シールド＞現物の4パネルがすべてダミー（「サンプル電子」等）になり、月送りは止まって理由が出て、セクターの入口が消えることも画面で確認）
- 🔵 追加のダミーは `previewMarketData.ts` の `PREVIEW_BY_CACHE_KEY` にキーと生成関数を足すだけ（他は触らなくてよい）

### 2026-08-12(23): アクセシビリティ点検と上位4件の対処 ✅（`8ccba0a` push 済＝Vercel自動デプロイ）

ユーザー依頼で全体を点検し、**「キーボードだけでは使えない」に直結する4件**を直した。
テスト **426 pass**（+5）／`tsc -b` OK ／ `eslint` 0。

| 直したもの | 中身 |
|---|---|
| 🔴 **カレンダーがマウス専用だった** | 月ビューのセル（メモ・タスクを開く）と日付数字（その日へ移動）、週ビューの日付ヘッダー、週/日ビューの予定ブロックを Tab で辿れるようにし Enter / Space で実行。共通処理は新設 `src/utils/a11y.ts`（`activateOnKey` / `dateLabel`・テスト5件）。🔵 **時間帯のマス（24×7＝168個）は意図的に対象外**＝Tabが168回止まると逆に使えない（時刻は入力欄で指定する） |
| 🔴 **サイドバーのフィルタが偽チェックボックス** | プライベート／米国／日本／アノマリー／ぽいロボレーダーは `<span>` だけで**実体の `<input>` が無く**、Tabで到達できず ON/OFF も読み上げられなかった。透明な `input` を重ねる形に統一し、`Sidebar` 内の `FilterCheck` に集約（5か所→1か所）。フォーカス時は行に輪郭 |
| 🔴 **ピンチズームを禁止していた** | 全HTML11ファイル（index / poirobo / poinavi / hakaseAI）の viewport から `maximum-scale=1.0, user-scalable=no` を削除（WCAG 1.4.4・Android Chrome では実際に拡大できなかった） |
| 🔴 **入力欄に名前が無い（14箇所）** | メモ／スケジュール／時刻／問い合わせ／メンバー追加／エントリー分析レポート等に `aria-label`。お問い合わせの「お客様種別」は **`display:none` だった radio** を透明配置に変えてキーボードで選べるようにし `role="radiogroup"` を付与。EvalsPanel の閉じる `×` にも名前を付けた |

🔵 **見た目は変えていない**（透明な入力を重ねる／button の既定枠を打ち消す形にした）。

🔵 **見送った指摘**（次にやること候補へ）
- ダークテーマの `--text-dim`（rgba(255,255,255,0.28)）が背景 #141623 上で **約2.5:1**（`index.css:139`）。
  OFF状態のフィルタ名などに使用。ライトテーマ（219〜226行）は比率をコメントで管理していて良好なので、ダーク側も同様に。
- `aria-live` が全体で0件＝保存トースト・エンジン結果・アラートが読み上げられない。
- AuthModal だけ Esc で閉じられない／全モーダルで初期フォーカス・フォーカストラップ未実装。
- `-webkit-tap-highlight-color: transparent`（`index.css:16`）／`prefers-reduced-motion` はView Transitionsと一部パネルのみ。

### 2026-08-11(22): 画面と手順書の「08:30」が古いまま残っていたのを修正 ✅

🔴 判断を 08:30 → 15:00 へ移したとき、**コードの cron だけ直して表示を直していなかった**。
ユーザーがロボ口座の画面で気づいた（「毎営業日 08:30 に判断しています」と出ていた）。

直した箇所:
- `RoboAccountPanel.tsx` の左列 … 「毎営業日 15:00 に判断しています／執行は当日の引成（MOC）」
- `SpecView.tsx` … 判断時刻と約定の記述（寄値→終値）
- `docs/robo-trade-runbook.md` … §③ と時系列表（15:00判断 → 15:25までに引成 → 翌15:00に約定記録）

🔴 **教訓＝運用の時刻や約定のしかたを変えたら、次の3つを必ず見直す。**
　 ①`fetch-data.yml` の cron ②画面の直書き文言（`RoboAccountPanel`）③手順書（runbook）。
　 仕様書（SpecView）だけ直しても、ユーザーが見るのは画面のほう。

### 2026-08-11(21): テクニカル分析を約100通り総当たり／フォーメーション分析ページを新設 ✅

**① テクニカル分析の総当たり（採用0・設計書 §5.10）**

| 分類 | 数 | 主な結果 |
|---|---|---|
| オシレーター5種 | 5 | MACD/RSI/%B/ストキャスは**前後半で符号反転**。どれも「25日線乖離」の別表現 |
| | | サイコロジカルラインだけ符号が一致（25日58-75%で t=−6.13）だが**バックテスト全滅** |
| 移動平均・クロス | 8 | GC/DCは26年で50回程度しか出ず統計にならない。**GCがマイナス方向・DCがプラス方向** |
| パラボリックSAR | 3 | **単独運用で CAGR −10.65%**、売買回数1000超。日次でも下降中のほうが良い |
| 一目均衡表 | 8 | **三役好転 −0.363%(t=−2.39)／三役逆転 +0.631%(t=3.15)** ＝シグナルが逆。BT −14.19% |
| ローソク足・複数足 | 18 | 候補4つは**すべて教科書と逆**（大陰線・陰の包み・黒三兵・明けの明星） |
| フィボナッチ・キリ番 | 15 | フィボは**水準がフィボ数である必要が見当たらない** |

🔴 **教科書のシグナルはほぼ全部が逆向き**（10通り）。偶然ではなく共通の原因がある。
　 ①これらは「もう上がった」ことの言い換えで、上がった後に買えば平均回帰で不利
　 ②有名なシグナルほど反応し終わった後の価格を買うことになる

🔴 **「日次で強い」と「この戦略に足せる」は別、を7回踏んだ**（抵抗 t=−3.52／価格帯別出来高 t=−3.45／
CVDダイバージェンス t=6.56／三連休 t=2.95／大陰線 t=2.61／1万円キリ番 t=−3.42／サイコロ t=−6.13）。
理由は毎回同じ＝**エントリーを除外する**か**保有が寸断される**。

🔵 **保留＝レンジ内の位置「安値圏(0-20%)」**。t=4.35（前半+0.966/後半+2.845）で今日最強クラス。
バックテストは CAGR 14.44%→24.43% だが売買回数 230→492 で DDそろえ後 −1.72%。
**ただし後半だけなら +2.54%**。寸断を減らす形なら生きるかもしれない。次に測る価値がある。

**② フォーメーション分析ページ（DATA→未来ガジェット）**
チャートパターン16種をSVGで図示。線が描かれる演出つき。
🔴 主張は「覚えよう」ではなく**実測の併記**。未測定のものは「未測定」と書く。
🔴 ヘッダーはタイムマシンと同形にそろえた。立場と測り方は ? に畳んである。
🔴 踏んだ落とし穴＝①データ文字列に Markdown の `**` を書いてそのまま画面に出た
②グリッドは行内で高さが揃うので、1枚開くと隣のカードの中身が縦中央にずれる
（button は既定で中身を縦中央に置く）→ flex の上揃えで固定。

**③ DATA→イベントドリブン→「権利確定日前」を削除**（ユーザー指示）。

### 2026-08-11(20): 周期から戻るとPCのサイドバーが閉じたままになる不具合を修正 ✅

🔴 (10) でスマホの「黒い覆いが残る」を直したとき、**PCでも無条件にサイドバーを閉じていた**。
PC のサイドバーは常設で覆いも出ないので、閉じてはいけなかった。

🔴 しかも `sidebarOpen` を戻す経路は `useEffect(..., [isDesktop])` だけなので、
**画面幅を変えるまで直らない**（周期へ一度行くと、以後ずっと閉じたまま）。
→ `if (!isDesktop) setSidebarOpen(false)` に修正。

🔵 教訓＝**スマホの不具合を直すときは、PCで同じコードが走らないか必ず確認する**。
今回は「覆いを消す」目的だったのに、覆いの無い環境でも副作用だけが残った。

### 2026-08-11(19): ETFの棚卸し（次に測るものの下調べ）📋

🔴 **日本株の枠内では、器の拡張はほぼ打ち止め**という結論。

**日経との相関（12年・日次）**

| | 相関 | 出来高 | 判定 |
|---|---|---|---|
| TOPIXブル2倍(1568) | 0.940 | 280万 | ❌ 分散にならない |
| NASDAQ100(1545) / S&P500(1547) | 0.763 / 0.782 | — | ❌ 株式は世界中一緒に動く |
| インド株(1678) | 0.566 | 57万 | ❌ |
| 東証REIT(1343) | **0.391** | 111万 | 🔵 唯一まともだが**レバ/インバースが無い** |
| **米国債20年超(2621)** | **−0.022** | 32万 | 🔵 ほぼ無相関。ただし**履歴5.8年**で検証には短い |
| 米国債7-10年(1482) | −0.174 | 10万 | 🔵 履歴10.2年 |
| 純金(1540) | 0.226 | 20万 | 🔵 レバ/インバース無し |

🔴 **日本にはレバレッジ・インバースETFが日経とTOPIXにしかない**。セクターにもREITにも金にも無いので、
両方向を取る器（いまの設計）には乗らない。先物か信用が必要になり、そこは金利で負けると測定済み。

🔴 **データ品質の注意**＝Yahoo の日足は**株式併合の調整漏れ**が多い。1306(TOPIX)で3日、1568で4日、
1570/1357 でも2日ずつ。**|日次変化|>35% の日を弾いてから使うこと**（弾かないと CAGR が −17% 等になる）。
🔴 1459（楽天日経ダブルブル）は相関 **−0.989**＝名前と逆の動き。銘柄取り違えの疑いがあるので使わない。

**次に測るなら**: ①米国債(2621)で分散 ②セクターETF17業種で分散。
🔵 ただし🔴 **相関が低い＝この戦略で儲かる、ではない**。ドンチャン＋押し目は日経で26年検証したもので、
債券や金で機能する保証はない。**実運用のデータを7ヶ月貯めるほうが次の一手の材料になる**（ユーザーと合意）。

### 2026-08-11(18): 年度末ベアを追加・カレンダー系17件は全滅 ✅（`9d939c6`）

🔵 **CAGR 12.43% → 13.77%（さらに 14.22% ※出来高込みで再計測）**。今日3本目の当たり。

**採用＝年度末ベア**（3月の最終2営業日はベアを建てる）
26年中18年がマイナス（69%・平均 −1.24%）。実測 t=−3.10、前半 −0.623%/後半 −0.707% と
**前後半でほぼ同じ大きさ**。配当落ち＋期末の益出し。DDそろえ後 +1.67%、DD も −38.4%→−37.5% と浅くなる。
🔴 9月末は効かない（t=−1.34）。🔴 **出来高フィルターの後に上書きする**
（前に入れると割り込みで判定がリセットされ DD −37.5%→−41.9% に悪化・実際に踏んだ）。

**不採用＝カレンダー系17件**
曜日／月末月初／SQ／クリスマスラリー／タックスロスセリング／投資の日／NISAの日／節分天井／
彼岸底／1月効果／セルインメイ／月別季節性／権利確定前／三連休／GW／六曜／水星逆行。

🔵 **SQは方向に出ないが量に強く出る**（メジャーSQ当日の出来高は平常の1.5倍・t=6.44）。
🔵 **三連休の歪みは実在**（寄り +0.190%・引けまで +0.350%・t=2.95）。
　 🔴 だが「休み前は必ずブル」は下げ相場でも買うことになり壊れる（3日以上で CAGR 2.87%・DD −72.2%）。
🔵 GWは平均 +0.274% と大きいが n=34・t=0.65 で確定できない（+5.6%の年も −3.3%の年もある）。

🔴 **年度末ベアだけが生き残った理由＝下げる方向で、年2日しかなく、機械的な理由がある。**
頻度が低く方向が明確なものだけが、ルールの土台を壊さずに乗る。

**本日の総括＝27案検証 → 3案採用（打率11%）。CAGR 8.00% → 14.22%（2倍）。DD −40.9% → −38.5%。**
🔴 ただしこの +6.2ポイントは**同じ26年のデータを見て出した数字**。実運用では3〜5割縮むのが普通で、
**想定は 10〜12%、良くて15%** に置くのが健全（ユーザーと合意）。

### 2026-08-11(17): 手法の検算（偽薬テスト）に合格 ✅

🔴 16案測って2案採用したが、その2案が本物か確かめようがない。そこで**効くはずのないもの**を
同じ手続きで測った。効いてしまったら、検証が偶然を拾う仕組みだという証拠になる（ユーザー発案）。

- **六曜**＝6つとも |t|<2。**大安 +0.046% と仏滅 +0.047% がほぼ同じ**。前後半で符号も反転
- **水星逆行**＝差 −0.062%・t=−1.29・前後半で符号反転

🔵 両方きちんと落ちた＝**手続きは「何を入れても当たりが出る」ゆるいものではなかった**。
🔴 ただし「手法が壊れていない」を示すだけで、採用2案が実運用で効くことは保証しない。

**あわせてカレンダー効果も全滅**（曜日・月末月初・SQ週）。
🔵 ただし副産物＝**週末を挟むオーバーナイトは危なくない**（平日+0.036% vs 週末+0.046%、
三連休は+0.190%・t=3.19）。🔴 **週末を避けると壊滅する**（CAGR 12.43%→3.77%）。

### 2026-08-11(16): 期待値の検証を16案まわし、2案を採用 ✅（`75520b7` / `12641a6` / `6e0fe27`）

🔵 **CAGR 8.00% → 12.90%（2倍・26年）**。1日で +4.9ポイント。

**採用した2案**
1. **判断15:00・執行は当日の引成**（+2.33%）… 日経のリターンはオーバーナイトに集中している
   （26年・1倍で オーバーナイト +11.07% / 日中 −4.90%）。翌朝の寄りで建てると初日を丸ごと逃す。
2. **出来高フィルター**（+1.61%）… ドンチャンのブレイクで建てるとき、①出来高が20日平均の1.1倍以上
   ②OBVの20日変化がプラス を両方満たさなければ建てない。🔴 **入る瞬間だけ**。保有中は見ない。

**入れなかった10案**（詳細は設計書 §5.7）
レバ／確信度／頻度／複数市場／日中足／ボラターゲティング／上位トレンド／テクニカル指標8種／
抵抗を試した回数／価格帯別出来高／チャートパターン6種／CVDダイバージェンス／分割エントリー／
ピラミッディング。

🔴 **今日いちばんの学び＝当たった2案はどちらも「執行」、外れた案はほぼ全部「予測」だった。**
この戦略は87回中5回の大勝ちで利益の9割を稼ぐ形なので、日次の小さな予測力（+0.4%程度）で
エントリーやポジションをいじると、**細かく正しくなることが大きく当たることを邪魔する**。

🔴 **日次で予測力がある ≠ この戦略に足せる**。3回続けて踏んだ:
  ・抵抗を試した回数（日次 t=-3.52・頑健）→ 濾すとエントリーが消える
  ・価格帯別出来高（日次 t=-3.45・12通り安定）→ 売買回数 170→424 で保有が寸断
  ・CVDダイバージェンス（日次 t=6.56・今日最強）→ バックテスト −0.91%

🔴 **トレーリングは幅を2倍に修正**。建値と同じ幅だと DDそろえ後 −0.93%・売買回数 166→434 と悪化していた。
　 ドンチャン25の安値割れが既に価格構造のトレーリングとして働いており、値幅の線を重ねると二重になる。
　 それでも外さないのは、**ロボ口座の手仕舞いはAIが判断する**から（AIが降りそこねたときの歯止め）。

🔴 **この +4.9ポイントは同じ26年のデータを何度も見て出した数字**。頑健性は真面目に確認したが
　 （引成は独立2測定で機序一致・出来高は15通り全部プラスで前後半とも同符号）、
　 **実運用では3〜5割縮むのが普通**。CAGR 10%前後を想定しておくのが健全。

🔴 **踏んだ落とし穴**＝`analyze-expectation.mjs` が `volume` を渡しておらず、出来高フィルターが
　 素通りして実物と違う成績（10.93%）を出していた。**ルールが新しい入力を使い始めたら、
　 渡している経路を全部見直すこと。**

### 2026-08-11(15): 判断を15:00へ・執行を当日の引成に ✅（`08bf08c`）

（上の(16)に要約。設計書 §5.4 に詳細）
🔴 **運用側の約束＝15:00の通知を見て、15:25までに引成（MOC）で発注する。**
　 ここが守られないと、記録（終値で約定）と実物がずれる。

### 2026-08-11(14): 週足チャートも撮るようにした ✅（`c726fea`）

日足に加えて週足（`interval=W`）も撮り、2枚とも Chatwork へ投稿する。
🔴 位置づけは**AIの読みの補助まで**。画像から読んだものは過去に遡って再現できず**検証できない**。
　 仕組みで効かせたいものはチャートに任せず、数値で定義して測ること。

### 2026-08-11(13): 約定を「実際の寄値」で記録するようにした ✅

🔴 ユーザー指摘＝**9時に価格が大きく動くので、8:30の価格では買えない**。実測すると
1570 の窓（前日終値→寄値）は**平均2.08%・中央1.47%・上位10%で4.82%**（n=241）。
終値で約定したことにしていた従来の記録は、**実物と別のものを測っていた**。

- 判断した日は `account.pending` に**注文を積むだけ**。次の実行のとき日足の最終行が
  その日になっていて、**始値＝実際に買えた値段**が入っているので、それで約定させる（1日遅れて確定）。
- 損切りも `detectStopHit` で**判定するだけ**にして、決済は翌朝の寄値。窓で飛べばもっと悪い値段になるが、
  それが実際に降りられる値段。
- 🔵 **窓ガード**＝寄値が「判断時点の終値から引いた損切り値」の向こう側なら建てない。
  🔴 損切り値は**判断時点の終値から**引くこと。寄値から引き直すと、どんなに飛んでいても
  必ず内側に収まってガードが働かない。
- 通知に「翌営業日の寄りで執行予定」を追加（書かないと注文したのに口座が変わらないと読める）。

**通し確認で出た差**＝同じ値動きで、旧は建値30,000／手仕舞い31,000で **+20,000円の勝ち**、
新は建値30,800（実際の寄値）／手仕舞い29,500（翌朝の窓）で **−26,000円の負け**。
🔴 **勝ち負けが逆に出ていた。**

**採らなかった案**
- 9:30 に執行（ユーザー案）→ 窓は09:00で確定済みなので**取り戻せない**。実測で寄値→9:30 は
  平均 −0.15%（偏りなし）・絶対値で平均1.47%のブレが**上乗せされるだけ**（n=14）。
  日足に9:30の値は無く、5分足という別経路への依存も増える。
- 執行時刻をユーザーの裁量にする → 疑似口座が再現できず、**AIの成績と人の執行の腕前が混ざる**。
  記録は寄値で固定し、実際の執行とのズレは `divergences` / YOUR CALL 側で測る。

🔵 約定0件のうちに入れたので、過去の記録の作り直しは不要だった。

### 2026-08-11(12): 損切りが価格の構造を見るようになった ✅

🔴 ユーザー指摘＝**損切りも価格判断の一種で、コード化＝ATR一本が正しいとは限らない**。指摘は正しく、
ATR×k は値幅しか見ていないため、誰が見ても支持されている水準の**すぐ内側**に置いてしまうことがある。
そこは真っ先に試される場所で、刈られてから元の方向へ戻られる形がいちばん痛い。

- ① **スイング安値**（直近25本）の「すぐ内側」に来たときだけ、安値のわずか下へ回す
  （条件: `atrStop >= sl` かつ `atrStop - sl < 0.5×ATR` → `sl - 0.1×ATR`）
- ② **イベントが2営業日内**なら倍率を1.2倍（`roboCalendar.eventNear`）。
  🔴 ATRは過去の値幅なので、イベント前は**まだ広がっていない**。🔵 SQ は `macroCalendar` に無いので `sqCalendar` も見る。
- ③ 深くするのは ATR幅の1.5倍まで（安全網。既定値では当たらないが、定数をいじったとき用に残す）
- 建値もトレーリングも**同じ `stopPrice` を通す**（2か所に持つと食い違う）

🔴 **対立軸は「コード化 vs 価格を見る」ではなく「事前に決める vs その場で決める」**。
構造を見るのは純関数の中だけで、AI には触らせない。損切りが交渉可能になると、
**負けている場面ほど「まだ支持線の内側だから」が通ってしまう**。これが設計原則3の本体。

🔴 **踏んだ落とし穴**＝最初の実装で「深いほうを採る」と書き、**遠い安値まで拾ってしまった**
（テストで発覚）。正しくは「安値のすぐ内側にあるときだけ」。条件の向きに注意。
🔵 **キリ番を避ける案は入れなかった**。刻みを桁から出すと 200円台の1571と40,000円台の1321のどちらかで
必ず意味を失う。実データが出るまで持たない。

実データ（8/10終値・VIX 15.46・CPI前日）＝スイング安値は遠く不発、イベント幅だけ効いて k 2.0→2.4
（1570 は −11.57% → −13.88%）。

### 2026-08-11(11): 周期のスマホタブ割りを 円環 / 業種＋銘柄 に直した ✅（`f9d8081`）

🔴 最初は「セクター＝円環＋業種カード ／ 個別＝銘柄検索」で割ったが、**業種カードと銘柄検索はつながっている**
（業種を押す＝その業種の銘柄を検索する）。この割り方だと**押した結果が裏のタブに出る**（ユーザー指摘）。
→ 業種カードを個別側へ移し、**セクター＝円環だけ ／ 個別＝業種カード → 銘柄検索**に。流れが1タブで完結する。
🔵 タブ移動の自動遷移は入れていない（同じタブに収まったので不要になった）。
🔵 円環はひとりになったので縦の中央寄せに（`flex: '1 0 auto'`＝余った高さは受け取るが小さい画面で潰されない）。
　 3列を縦積みしていた頃は `flex: 1` にすると潰れたが、同じタブに他の中身が無いので取り合いにならない。
🔴 教訓＝**画面を分けるときは「押した結果がどこに出るか」を先に確かめる**。見た目の区切りだけで割ると導線が切れる。

### 2026-08-11(10): 周期をスマホのみ2タブに・エンジンと周期をサイバー調に ✅（`7124d5e`）

- 周期（セクターローテーション）を**スマホだけ**「セクター」「個別」の2タブに（PCは3列とも見えているので出さない）。
  並び順は `src/utils/sectorTabs.ts` の単一情報源。テストで「isMobile 条件が入っていること」も固定した。
  🔴 出し分けは **display のみ**。作り直すと検索欄の入力やAIの結果がタブ移動で消える。
- エンジン（`shield`）と周期（`sector`）のフッターと下部タブを**研究室と同じサイバー調**に（ダークのみ）。
  フッター側は `CalendarHeader` の `isLab`、タブ側は `App.tsx` の `isNeonBar` に足す。
- フッターナビの並びを **カレンダー / チャート / シールド / エンジン / 研究室** に。
- 🔴 スマホでメニューから周期へ飛ぶと**黒い覆いだけが残っていた**（ユーザー指摘）。覆いはメニューの一部で
  画面を替えても自分では閉じない。→ `handleOpenSector` でメニューごと閉じる。
  **メニューの中から画面を替えるものは、必ず閉じてから遷移すること。**

### 2026-08-11(9): ロボ投資の積極化＝トレーリングストップ ✅（`711ab26`）

🔴 **どこを積極化すると期待値が上がるか**を整理したうえで、いちばん効く1点だけ入れた。
この戦略は**勝率34〜40%が正常**で、期待値は「たまに来る大きな勝ちを取り切れるか」でほぼ決まる。
それまでは**損切りが建てた瞬間から動かず**、**利確ルールも無く手仕舞いは LLM の毎日の判断任せ**だった。
プロンプトで「細かく利確すると期待値が壊れる」と戒めてはいたが、**仕組みで止めていなかった**＝早期利確の余地が残っていた。

- `trailStop`（純関数）＋ `applyTrail`（口座に書くだけ）を追加。毎営業日、終値から ATR20×倍率の位置へ損切りを引き上げる。
- 🔴 **上げるだけ。下げない。** ボラが膨らんだ日に線を下へずらすと「負けを小さく」が崩れる。
- 🔴 **利確ルールは入れない。** 上限を決めて降りると、いちばん大きな勝ちを取り逃す。
- 🔴 **引き上げは損切り確認の後**。今日の終値で持ち上げてから今日の終値と比べたら、損切りに永遠に触れない。
　 今日引き上げた線が効くのは明日から。
- 採らなかった案＝**レバを上げる**（3倍で CAGR 22%／DD −80%。20年検証で日経2倍持ちっぱなしは CAGR 9.89%・
　 対照群 9.23% ＝ ルールの価値はリターンではなく DD を −88%→−42% に半減させたこと）、
　 **確信度で建玉倍率を変える**（本体実測で確信度は自信過剰かつ**反転**。70%台で実勝率25%）。
- 通し確認（本番と同じ順番で値動きを流した）＝損切りが 28800→35200 まで一度も下がらずに上昇し、
  崩れた日に建値より上で手仕舞い。**動かさない従来の作りでは同じ期間に一度も損切りに触れない**（＝AI任せのまま）。
- 🔵 **ベアは「やらない」のではなく「少なめ」**（ユーザー方針）。価格が下向きでも需給が良いならベア1倍より hold を優先。
  2倍（1357）は例外。迷ったらブル寄り／持たない側に倒す。プロンプトに明文で入れた。
- 🔴 口座は現在**ノーポジ・約定0件**。実運用でトレーリングが効くのは AI が建玉を持ってから。

### 2026-08-11(8): 読み込み中の表示をぽいロボのドット絵に統一 ✅（`775c3c2`）

`PoiroboLoader` を新設し、画面・パネルの待ち表示をすべて置き換えた（QuantView / StocksView / MicroQuantView /
MarketDailyPanel / BacktestPanel / RoboAccountPanel / CommunityLockScreen ＋ `App.tsx` の `ViewLoader`）。
🔵 **ボタンの中の小さなぐるぐるは置き換えない**。あれは「押した操作が動いている」印で、扱う場所が違う。
🔵 画像ではなくコードで描いているので、**読み込み中の表示が読み込みを増やさない**。

### 2026-08-11(7): 年月ラベルの幅を固定し、＜＞が動かないようにした ✅

🔴 週ビューで月をまたぐと「2026年 9月」→「2026年 9月 – 10月」と伸び、**その分だけ ＜＞ が右へずれていた**
（ユーザー指摘）。ラベルが `minWidth: 112` だったため、長い形のときだけはみ出していた。
→ **幅を固定**（172px＝いちばん長い「2026年 12月 – 1月」が収まる）。
🔵 **中央寄せをやめて左寄せにするのが要点**。中央寄せのままだと幅を固定しても、
　 文字数が変わるたびにタイトル自身の左端が動いてちらつく。
🔵 実測で確認＝月をまたいでも ＜＞ の左端は 450 / 488 のまま動かない。

### 2026-08-11(6): 週ビューで祝日の列に色が付かなかったのを修正 ✅

🔴 週ビューの列の背景が `closed && !td`（＝**今日は除く**）になっていたため、
**今日がちょうど祝日の日だけ色が消え**、祝日なのに平日と同じに見えていた（8/11 山の日で発覚・ユーザー指摘）。
→ `closed` だけで見るように修正。今日の強調はヘッダー側で出しているので、ここで外す必要は無い。
🔵 日ビューは元から `closed ?` だけで見ており、これで揃った（月ビューの `!dim` は別month の話なので正しい）。
🔴 ついでに踏んだ＝**JSXコメントを `return (` の内側に置くとパース エラー**になる。この日2度目。
　 コメントは `return` の外（式の前）に書く。

### 2026-08-11(5): ドット絵の横線を根本解決・スマホの「板」を撤去 ✅（`e3b69b5` / `566084f`）

**① 🔴 ドット絵の体に薄い横線が出ていた（ユーザーが2度指摘）**

スプライトのデータは正しく、原因は**1ドット＝1つの四角で描いていたこと**。
端末の画素比（125%表示など）では隣り合う四角の下端と上端が1画素ぶんずれ、そこが背景として透ける。
🔴 **四角を5%重ねても、動かし方を steps() に変えても消えなかった**（どちらも対症療法）。

→ **同じ色のドットをまとめて1つの `path` にする**のが根本的な直し方。
　1つの図形の内部に境目は無いので、画素比がいくつでも線は出ない。
🔵 まばたきする目だけ別の path に分ける（お腹のLEDは点きっぱなし）。
🔵 1ドットずつ動かす `steps()` は残した（輪郭がにじまない・ドット絵らしい）。

**② 🔴 スマホでエンジン画面の中身の下に「板」が出ていた**

`ShieldView` の外枠が**スマホだけ下に 130px 空けていた**ため、パネルの背景がその手前で切れ、
下に色の違う面が現れていた。しかも中のスクロール面にも同じ余白を入れており二重。
→ 外枠側を撤去。逃げ（浮いているタブに隠れない余白）はスクロール面の側だけで確保する。
🔵 **モバイル実機での確認は未実施**（開発環境で画面幅を変えても表示が切り替わらなかった）。
　 まだ板が残るようなら、次はエンジン画面の背景そのものを疑う。

### 2026-08-11(4): ぽいロボのドット絵・エンジンに左のビジュアル列 ✅

**① ぽいロボのドット絵（24×24）**＝`src/utils/poiroboSprite.ts`（データ）＋`PoiroboPixel.tsx`（描画）。
使い所＝**画面切り替えのローディング**（回転スピナーを置き換え）と**エンジンの左列**。

- 🔴 **32×32で元絵に似せる方向は試して戻した**（ユーザー判断）。皿アンテナ・リング・腕の関節まで
  描き込んだら、**細部が増えたぶん読みにくくなった**（腕が離れて見える／頭身が広がる）。
  小さく見せる絵は、線を足すより減らしたほうが伝わる
- 🔴 **踏んだ不具合＝体の途中に薄い横線が出た**（ユーザーが気づいて発覚）。
  スプライトのデータは正しく、原因は**ふわふわ動かすアニメーション**。
  小数ピクセルの位置に来ると `crispEdges` の四角が行ごとに丸め直され、隙間が背景として透ける。
  → **1ドットぶんずつ `steps()` でカクッと動かす**形に変更（ドット絵らしさも増した）。
  あわせて隣接ドットを 5% 重ねる（`OVERLAP`）
- 🔴 サイズは**升目の数の倍数**にすること（220pxで端数になり継ぎ目が出た → 216px）
- テストで固定＝正方形／パレット外の文字なし／特徴（目・LED・アンテナ）／体の左右対称

**② エンジン画面に左のビジュアル列**（PCのみ・ユーザー指示）

幅は**シールド画面の左パネルと同じ 500px**（`EnginePanel` と同値）。画面をまたいで同じ位置に
同じ幅の柱が立つので、切り替えても目線が動かない。
- 目的は2つ＝①中身が画面いっぱいに伸びて間延びするのを止める ②毎日見る画面なので飽きさせない
- 🔵 **情報は置かない**。数字はすべて右側にあるので、ここに増やすと二重管理になる
- スマホは縦に積むと邪魔なだけなので出さない

### 2026-08-11(3): バナーをドット絵に・説明書とプレイブックを現行仕様へ ✅

**① バナーをドット絵の円環に**（ユーザー指示）。升目を走査して環の帯に入るマスだけを四角で置く。
🔵 極座標で計算した位置に丸を打つのではなく**升目に沿って置く**のが要点（格子に乗っていないと
ドット絵に見えず、ただの点描になる）。`shapeRendering="crispEdges"` で拡大してもぼやけない。

**② 🔴 説明書とプレイブックが「削除済みの機能」を説明したままだった**

2026-08-09 に旧「エンジン（ポジション分析・保有画面のスクショをAIへ）」を削除して
ロボ口座（疑似トレード）に置き換えたのに、**資料は旧仕様のまま2日間残っていた**。
資料が消えた機能を説明していると、読んだ人が使おうとして見つからない。

| 資料 | 直したところ |
|---|---|
| 説明書（`ManualView`） | エンジン＝**ロボ口座**（3タブ・開発者のみ閲覧）に全面書き換え／**周期の節を新設**／「エンジン右下から周期へ」の記述を削除（独立ページになったため）／シールドのタブ名を「分析」→「シールド」に |
| プレイブック（`StrategyPlaybookPanel`） | エンジンのスライドを全面書き換え（AIの判断を検証する・勝率では見ない）／**セクターローテーションのスライドを新設**（13枚→14枚）／全体像のカード文言／1週間の流れの「エンジンで出口を見守る」 |

🔵 **資料は機能を消した時点で一緒に直す**。あとで気づく形にすると、その間ずっと誤案内になる。

### 2026-08-11(2): 周期を独立ページへ・サイドバーにバナー ✅

**🔴 周期（セクターローテーション）をシールド画面のタブから外し、独立ページにした**（ユーザー指示）。
理由＝**周期は日経平均の話ではない**。日経を見る道具（シールド画面）と同居させない。

- シールド画面は **シールド／環境／現物／先物** の4タブに
- 新しいビュー `ViewMode 'sector'`。入口は**カレンダーのサイドバー・メモの上のバナー**
- Android戻るの戻り先はカレンダー（サイドバーから開くため）

**バナーは円形・ビジュアル特化**（同日、ユーザー指示で作り直し）。文字は中心の
「SECTOR ROTATION」だけで、局面名・移行期・次の局面といった説明は**すべてページ本体に任せる**。
4分割の円環を描き、**現在地の象限だけを光らせる**（色は本体の図と同じなので、開いたときに
同じ色が同じ位置にある）。判定できない日は**どこも光らせない**（根拠の無い現在地を見せない）。
🔵 最初は局面名や「次は◯◯」を文字で出していたが、**読ませる場所ではない**という判断で落とした。

🔴 **踏んだ落とし穴＝移行期の「現在地」は `macroPhase()` の `id` ではない。**
金利とインフレが食い違う日、返る `id` は**行き先**であって現在地ではない。現在地は直前に確定した
アンカー（`macro.lastAnchor`）。最初これを取り違えて、**バナー「逆業績相場」／ページ本体「逆金融相場」**と
食い違った（実機で気づいた）。本体の図の「いまここ」と同じ扱いに揃えてある。

### 2026-08-11: タブの左右逆スライドを修正・エンジンを3タブに ✅（`39faf73` / `aed8a81`・push 済）

**① 🔴 周期タブでスライドが左右逆に動いていた**（ユーザー指摘）

原因＝**タブボタンの並びとパネルのDOM順が別々に定義されていた**こと。
```
ボタン : bunseki, kankyou, genbutsu, micro, sector   （周期は右端）
DOM    : bunseki, sector, kankyou, genbutsu, micro   （周期は2番目）
```
移動量はDOM順で計算しているので内容は合うが、**先物→周期で「右のタブなのに左へ戻る」**動きになっていた。

- 周期パネルのJSXを先物の後ろへ移し、DOM順をボタン順に合わせた
- 🔴 **並び順を `src/utils/quantTabs.ts` の1か所に集約**（App.tsx と QuantView が同じ定義を読む）。
  2か所に持っている限り、次に並べ替えたとき同じ壊れ方が必ず再発する
- `quantTabs.test.ts`（4件）＝**ソースを読んでDOM順とボタン順の一致を機械的に固定**。
  目で見て気づきにくい壊れ方なので、テストで止める
- 実機で `translateX(-80%)`（5枚中5番目）を確認

**② エンジン画面を「ロボ口座／成績／履歴」の3タブに**（ユーザー指示）

3カラムのままだと**資産推移のグラフと約定履歴が1/3幅に押し込まれていた**。特に履歴は
1件ごとの判断理由が長く、400px幅では折り返して読みにくかった。
- `src/utils/engineTabs.ts` に並び順とラベルを集約（①と同じ約束）
- 縦スクロールは**面ごと**に持たせた（全幅で中身が伸びるため）
- `engineTabs.test.ts`（4件）／実機（`?demo=1`）で3タブとも確認
- 🔵 代償＝俯瞰できなくなった。使ってみて不便なら成績＋履歴を1タブにまとめる案がある

**③ 🔵 ローカルで会員限定画面を見る方法**

`http://localhost:5173/calendar/?demo=1` で入れる（`canViewMemberPages` が `demoMode()` でも通る）。
🔴 **付けないと `CHECKING ACCESS...` で止まる**（未ログインだと Firebase の認証解決が終わらないため）。
本番ビルドでは `demoMode()` が常に null なのでこの枝は落ちる。

### 2026-08-10: 撮影の自動化・ログイン不要化・判断を朝へ ✅（`7cc0ce8` ほか・push 済）

**① 判断を 19:30 → 翌朝 08:30 へ移した**（ユーザー判断）

材料（前営業日の終値）も執行タイミング（寄り付き）も変わらない。変えた理由は
**通知を読んでから執行するまでの間隔を縮める**こと（夜に読んで朝に実行すると一晩空く）。
- cron に `30 23 * * 0-4`（前日23:30 UTC＝JST平日08:30）を追加し、判断ステップの条件をそちらへ
- 19:30 / 21:30 の回は**データ取得のみ**（同じ材料で2回判断しない）
- 🔴 **記録される日付が1日ずれた**。成績を日付で追うときは 2026-08-10 が境目。
  🔵 本番のトレードが1件も無いうちに変えたので過去の記録は混ざっていない

**② チャート撮影を平日16:00の自動実行にした**

Windowsのタスクスケジューラ `poirobo-capture-chart`（平日16:00・`StartWhenAvailable`）。
実行ログ＝`.captures/capture.log`。
- 🔴 **休場日は撮らない**（判断側と同じ `marketStatus`）。祝日ぶんの画像が毎日積まれるのを防ぐ
- 🔴 **canvas が描かれていない画像は投稿しない**。自動実行にすると、ログインが切れた日から
  毎日エラー画面を投げ続け、AIがそれをチャートとして読む
- 🔴 ログイン切れの検知が**すり抜けていた**（実際の画面は「このチャートレイアウトを開くことが
  できません…ログインする必要があります」で「ログイン」と「チャート」の両方を含む）。
  文言ではなく**canvasの有無**で判定するよう変更

**③ 🔴 ログイン不要の公開チャートに切り替えた**

`https://jp.tradingview.com/chart/?symbol=INDEX%3ANKY&interval=D`。
- 保存レイアウトは**ログインしないと開けない**うえ、**Googleが自動操作ブラウザのログインを弾く**
  （`Chrome for Testing` 判定）。メール＋パスワードなら入れるが、切れるたびに記録が止まる
- 🔵 **画像が判断に効いているかは未検証**。効果が不明なもののために運用の脆さを背負うのは順序が逆
- 🔵 副産物＝**銘柄のズレも解消**（保存レイアウトは `NK1!`＝SGXの先物だったが、数値は現物。
  公開URLは現物指定なので画像と数値が揃う）
- 代償＝BB・MACDが載らない。欲しくなれば `ROBO_CHART_URL` で戻せる（runbook に手順）

**④ 🔴 `.env.local` を読んでいなかった**（自動実行の直前に発覚）

撮影は**ローカルPCで動く**ので **GitHub Secrets は届かない**。それなのに
`capture-tradingview.mjs` は `loadLocalEnv()` を呼んでおらず、Chatwork のトークンが
`process.env` に入らないまま投稿処理へ到達していた。**誰も見ていない16:00に静かに落ちる**形。
→ 読み込みを追加し、未設定なら理由と直し方を出して終わるようにした。

🔴 **残るユーザー作業＝`.env.local` に `CHATWORK_API_TOKEN` と `CHATWORK_ROOM_ID` の2行**。
🔵 初回は **8/12(水) 16:00 撮影 → 8/13(木) 08:30 判断**（8/11 は山の日でどちらもスキップ）。

### 2026-08-09(6): 営業日カレンダー連動・3カラムUI・YOUR CALL ✅（`6440af2`・push 済）

**① 🔴 東証の休場日には走らせない**（ユーザー指示「トレード時間は、ぽいロボカレンダーの営業時間みてね」）

- `scripts/robo-trade.mjs` の先頭で `marketStatus(todayJst())` を見て、休場なら**判断も口座更新も通知もせず終了**
- なぜ止めるか＝休場日に「本日は hold」を記録すると、**判断した日と何もしなかった日が混ざり成績の分母が壊れる**
- 🔴 **休場判定を `src/utils/marketCalendar.mjs` に集約**（アプリと Node の単一情報源・`tevCore.mjs` と同格）。
  `src/utils/marketHolidays.ts` は**型付きの re-export だけ**に変えた（既存の import は無変更で通る）
- 日付は JST で判定（Actions は UTC のため `Intl` で Asia/Tokyo に寄せる）
- 配線確認用に `--force`（休場日でも走る）。**本番では付けない**

**② イベントをプロンプトに渡す**（ユーザー指示「トレードはカレンダーとかも見てね」）

- `scripts/roboCalendar.mjs` の `upcomingEventsText()` が**今後5営業日**の FOMC・日銀・CPI・PCE・雇用統計・短観・介入実績をまとめて添付
- 出どころは `src/utils/macroCalendar.ts`＝**アプリのカレンダーと同じ表**。Node の型ストリップで `.ts` を直接 import
  （動作確認済み。CI は Node 22 系＝型ストリップ既定の版）
- 🔴 **読めなくても判断は続ける**（警告1行だけ足す）。イベントは「あれば効く」情報

**③ UI を3カラムに（他ページと同じ構成・100vh に収まる）**

| 位置 | 中身 |
|---|---|
| 左 | 評価額 / 建玉＋LAST SYNC / 資産推移 |
| 中 | PERFORMANCE（ロボ vs 対照群） / 🆕 YOUR CALL |
| 右 | 約定履歴（**ここだけスクロール**） |

**④ 🆕 YOUR CALL / 従った時 vs 外した時**
同期から次の同期までを1区間とし、区間の頭で AI の判断どおりだったか（`matched`）で振り分けて区間リターンを比較。
PERFORMANCE が「AI は決定論より上手いか」を測るのに対し、こちらは**運用者の介入が効いているか**を測る。
区間ごとの内訳も表示。🔴 4区間未満は「数回では判断できません」と明示する。

**⑤ 開発用ダミーデータ** `src/utils/roboDemo.ts`（`?demo=1` / `?demo=empty` / `?demo=loss`）。
`import.meta.env.DEV` 限定で本番ビルドでは丸ごと落ちる。

検証: tsc(app)=0 / lint=0 / vitest **368 pass**（`marketCalendar.test.ts` 7件を新規追加）/ build 成功。
実機で `?demo=1` / `?demo=empty` を確認済み。

### 2026-08-09(5): 実保有への同期・UI・タブ構成の最終形 ✅（`3c56d02` / `55b3234`・push 済）

**① ロボ口座を実保有に同期する仕様に変更**（ユーザー決定）

🔵 ユーザー判断＝「基本的にAIの判断どおりに動き、最終決定だけ自分で下すので、
ロボ口座の残高を実保有に合わせたい」。**キャプチャは売買した日だけ**投稿する運用。

- 🔴 **同じ画像で二度同期しない**（`last_synced_file_id` で判定）。売買しない日も Chatwork には
  前回の画像が残るため、これが無いと口座が壊れる
- 🔴 **差分は必ず `divergences` に残す**（同期しても「AIは何と言い、人は何をしたか」を消さない）
- 🔴 **対象4銘柄を2つ以上持っていたら同期しない**（勝手に選ばない）
- 🔴 **同期由来の約定（`exit_reason:'sync'`）は成績集計から除外**。AIの判断ではないため。
  副次的に、読み取りの誤りが勝率・期待値を汚すのも防ぐ（建玉の誤りは次のキャプチャで自然に直る）
- **前回からの変化を日本語で出す**（`describeChange`）＝「買い増し(+20口)」「新規で建てた」
  「全部手仕舞い」「乗り換え」。通知と画面に出る

**誤読対策**（ユーザーの「他の銘柄も保有していたら？」「精度は？」への回答）
- 🔴 `validateRealPosition` … **AIの自己申告に頼らず実際の価格と突き合わせる**。
  単価が現在値から±60%超なら桁誤りとして弾き、資金に対し過大な数量も弾く
- 読み取り対象を**4銘柄に限定**し、他銘柄は件数だけ数えさせる（行ずれの誤読を減らす）
- **松井証券の保有株式（残高照会）画面**であること、取引履歴ではないこと、
  桁・行ずれ・マイナス記号の注意をプロンプトに明記
- 🔵 **精度は未実測**。実物のキャプチャで答え合わせするのが次の確認事項

**② 旧ポジション分析を削除**（ユーザー指示・仕様書 §3 の削除タスク）
- `ShieldView` から ShieldPanel / MemoPanel / AI起動 / SYSTEM LOG を撤去しロボ口座1枚に
- `src/utils/shieldPrompt.ts` と `shieldData.ts` を**削除**
- 🔴 localStorage `poical-shield-memo` と Firestore `shieldMemo` は**消していない**
- 🔵 **需給分析（シールドタブ・EnginePanel）は無傷**（名前が似ていて紛らわしいので注意）

**③ タブ構成の最終形**
| タブ | 中身 |
|---|---|
| シールド（`'quant'`） | シールド / 環境 / 現物 / 先物 / 周期 |
| エンジン（`'shield'`） | **ロボ口座1枚。サブタブなし** |

🔵 いったん市場データをエンジン側へ移したが、エンジンがロボ口座専用になったため戻した。

🔴 **バグ修正（実機で発見）**: `QuantView` のスライダー移動量を **DOM の並び順**で計算するよう修正。
パネルは JSX の順（`ALL_QUANT_TABS`）で並ぶのに、タブボタンの順（`visibleTabs`）で index を
取っていたため、周期をタブ末尾へ動かした時点で「現物」を選ぶと環境の中身が出るズレが発生していた。

**④ UI をぽいロボ系（サイバー調・シアン）に統一**
`cyberTheme` の `cy()` を使用。ターミナル調の見出し・ダスト/スキャンライン・
脈動する保有インジケータ・判定30件までの進捗バー。約定履歴は理由と**反証**を並べる。
評価額は `equity_curve` の最新値を使う（建玉を簿価評価すると含み損益がゼロに見えるため）。

検証: tsc(app)=0 / lint=0 / vitest **361 pass** / build 成功。実機で全タブ確認済み。
🔵 確認用に開けた会員ゲート・管理者判定はすべて戻し、サンプルデータも削除済み。

### 2026-08-09(4): 疑似トレード 実装 ✅ 一式そろった（`3cdd185`〜・push 済）

🔴 **設計は全項目確定・実装は一式そろった**。残るのは**本番の鍵設定と初回ログインだけ**（下記「再開時に必要な設定」）。

**できているもの**

| ファイル | 中身 |
|---|---|
| `src/utils/robotStrategy.mjs` | 🔴 決定論ロジックの**単一情報源**（`tevCore.mjs` と同格）。対照群シグナル＋損切り＋資金クリップ |
| `scripts/backtest-robo.mjs` | 対照群バックテスト（指数近似20年＋ETF実データ上場来の2本立て） |
| `scripts/roboPrompt.mjs` | 判断プロンプト（価格の特徴加工・優先順位・裁定ルール・対照群提示・背景） |
| `scripts/llmDecide.mjs` | Claude API（`claude-opus-5` / effort `high` / Structured Outputs / refusal 対応） |
| `scripts/roboData.mjs` | 価格（Yahoo日次）と需給（public/data）の読み込み |
| `scripts/roboAccount.mjs` | 口座の状態遷移（ドテン分解・損切り確定・資金クリップ・成績再計算） |
| `scripts/chatwork.mjs` | 通知の送信／画像の受け取り／ファイル投稿 |
| `scripts/readPosition.mjs` | 保有画面キャプチャの vision 読み取り（推測で埋めない・確度と未読項目を返す） |
| `scripts/robo-trade.mjs` | **実行本体**（`--dry` / `--no-llm` あり） |
| `scripts/capture-tradingview.mjs` | 🔴 **ローカル専用**。Playwright で TradingView を撮って Chatwork へ投稿 |
| `src/utils/roboAccount.ts` / `src/components/RoboAccountPanel.tsx` | 画面（管理者のみ表示・`ShieldView` の右カラム） |
| `.github/workflows/fetch-data.yml` | `robo-trade` ステップ追加（**平日19:30 JST のみ**・`continue-on-error`） |

テスト: robotStrategy 33件 / roboDecision 25件 / roboChatwork 14件 / roboAccount 21件
検証: tsc(app)=0 / lint=0 / vitest **342 pass** / build 成功。

🔵 **通し確認済み**（`node scripts/robo-trade.mjs --dry --no-llm`）: 価格取得 → 需給12項目（VIX 14.9）
→ 対照群判定 → 口座反映 → 通知本文生成まで動作。

🔴 **途中で見つけて直した問題**: `public/data` の JSON は共通で `{ updatedAt, data }` 形式だが、
**並び順がファイルによって逆**（週次＝新しい順／日次＝古い順）。末尾を最新として読んでいたため
需給が2項目しか取れていなかった。日付を見て最新行を選ぶ方式に変更。

**🔴 対照群の実測（Go/No-Go の比較対象。設計書 §5 に詳細）**

| | トレード | 勝率 | 期待値 | CAGR | 最大DD |
|---|---:|---:|---:|---:|---:|
| ETF実データ（1570/1357・2014-07〜） | 57件 | 40% | +47,347円 | 11.39% | −42.68% |
| 指数近似（^N225×2倍・20年） | 93件 | 34% | +54,317円 | 9.23% | −42.09% |
| 参考: 日経2倍持ちっぱなし | 7件 | 14% | +811,243円 | **9.89%** | **−88.42%** |

🔴 **対照群の価値はリターンでなくDD**（持ちっぱなしよりCAGRは低く、DDを−88%→−42%に半減）。
🔴 **勝率は34〜40%が正常**。プロンプトで勝率を上げろと指示してはいけない。
🔴 **ベアは20年で63日（1%）しか立たない**＝ベア側は対照群として比較力を持たない。
→ この3点はプロンプトに反映済み。Go/No-Go の DD 条件も**絶対値→対照群比**に修正済み（§2）。

**🔴 動かすために必要な設定（未実施・ユーザー作業）**

1. **GitHub Secrets を3つ登録**
   `ANTHROPIC_API_KEY` / `CHATWORK_API_TOKEN` / `CHATWORK_ROOM_ID`
   🔵 room_id の値は `~/.claude` メモリ `reference_stock_calendar_chatwork_room` にある（設計書には書かない）。
   🔴 **これが入るまで判断は実行されない**（ステップは `continue-on-error` なので他は動く）。
2. **ローカルで Playwright を入れ、初回ログイン**（チャート画像を使う場合）
   ```
   npm install -D playwright
   npx playwright install chromium
   npm run capture-chart -- --login     # ブラウザが開くのでログイン→Enter
   ```
   🔵 撮影しなくても判断は動く（数値だけで続行する）。
3. **運用**: 引け後にローカルで `npm run capture-chart`（チャート）＋ 保有画面を Chatwork に投稿
   → 19:30 JST に Actions が判断・記録・通知。

**🔵 旧ポジション分析の扱い（仕様書 §3 の削除タスク）**
- 左カラムの `ShieldPanel`（AIにポジション分析させるプロンプトのコピー）は**残した**。
  人が使う道具として性質が違い、ロボ口座が動き出す前の唯一の手段でもあるため。
- 右カラムの `MemoPanel`（手書きレポート保存）は**管理者にはロボ口座を表示**するよう差し替え。
  🔴 localStorage `poical-shield-memo` / Firestore `users/{uid}/data/shieldMemo` は**消していない**
  （過去のレポートを失わせないため。読み出し口を落としただけ）。

**🔴 次の判断ポイント**
- 30トレード貯まったら Go/No-Go（期待値>0 かつ 対照群を上回る）。週次シグナルなら**約7ヶ月**先。
- 実運用で LLM のコストを実測し、`effort` を `high` のままにするか判断する（見積もり約1,000円/月）。

### 2026-08-09(3): 疑似トレード（ロボ口座）＋通知の技術設計書 ✅（`bb3bf2b`・push 済）

🔵 ユーザーから仕様書「疑似トレード（表示名エンジン）+ 通知システム」を受領。
その §10 の指示（**いきなりコードを書かず、まず技術設計書を出す／未決定事項は決めさせず質問で返す**）に従い、
**`docs/robo-trade-design.md` を作成**。実装は未着手。

**ユーザー決定（2026-08-09・すべて確定）**

| 項目 | 決定 |
|---|---|
| 主目的 | **(b) 自律AIトレーダー** |
| 対象銘柄 | ブル1倍 **1321** / ブル2倍 **1570** / ベア1倍 **1571** / ベア2倍 **1357** |
| 疑似元本 | **100万円** |
| 判断 | 🔴 **LLM が最終判断**（`claude-opus-5` / effort `high`） |
| 通知 | 本人のみ・**hold の日も毎日** |
| キャプチャ | **Chatwork の通知ルームに投稿** → Actions が取得 |
| キャプチャ無しの日 | 前回分を「◯営業日前」と明示して使う（判断は止めない） |
| Go/No-Go | N≧30・期待値>0・DD≧−20%・**対照群を上回る** |

**設計上の要点（詳細は `docs/robo-trade-design.md`）**
- 🔴 **v1 にリアルタイムAPI・ブラウザスクショ・PC常駐は不要**。引け値＋既存 `fetch-data.yml` で完結。
  既存バックテストが全部日次終値ベースなので、リアルタイム判断は検証条件と執行条件を食い違わせる。
- 🔴 **TEV の実測（52週で勝率44%・Bear 39%・確信度 gap −11）をプロンプトに明記して渡す**。
  伏せると負けている指標を根拠に自信を持って判断される。20年R&D の結論（トレンドフィルター必須）も併記。
- 🔴 **LLM 判断はバックテストできない** → **決定論ルールを対照群として並走記録**し、
  Go/No-Go に「**対照群を上回る**」を条件として入れた。追加 API コストはゼロ。
- 🔴 **判断の入出力を丸ごと git に保存**（`robo_logs/{date}.json`）。再現不能な判断器の唯一の検証手段。
  判断器の指紋（モデルID・effort・プロンプトhash）も記録し、変わったら成績表を分ける。
- 出力は **Structured Outputs で固定**。`counter`（この判断が外れる条件）を必須項目にした。
- 🔴 **損切りは LLM に決めさせない**（ATR20×VIX連動の純関数）。仕様書 §3・設計原則3 と一致。
- 銘柄4本だが**同時保有は1銘柄のみ**。**倍率の選択がそのまま確信度の表現**。
  逆方向の指示が来たらコード側で「決済→新規」の2約定に機械的に分解する。
- 🔴 **実保有キャプチャは `real_position.json` に分離**（`robo_account.json` と混ぜない）。
  同期させると**AIの成績と人の介入が分離できなくなり**、Go/No-Go も対照群比較も無意味になるため。
  LLM には両方渡し、乖離があれば `user_note`（別欄）で指摘させる。
- 🔴 **画像そのものはリポジトリにコミットしない**（実口座の残高が git 公開履歴に残る）。
- 🔴 **通知の宛先を本人限定にしたのは法務判断**。特定売買の名指し配信は
  `docs/legal/01` の論点A（投資助言業）に直撃する。無料でも助言性は上がる。
  ルームIDと実名は設計書に書かず GitHub Secrets（`CHATWORK_ROOM_ID`）で渡す。
- 🔵 コスト見積もり: 1回約50円 → **1日1回・営業日20日で約1,000円/月**。
  🔴 **prompt caching は効かない**（TTL 5分〜1時間に対し実行間隔24時間）。安くする手は effort か回数。

**次の実装順序**（設計書 §9・§11 準拠）
`backtest-robo.mjs`（対照群の期待成績を先に確定）→ `robotStrategy.mjs`＋テスト →
LLM 判断まわり（`llmDecide` / `roboPrompt`）→ Chatwork 連携（通知・キャプチャ取得）→ 画面（`RoboAccountPanel`）

### 2026-08-09(2): タブを機能ごと2画面に振り分けた ✅

🔵 ユーザー指示（2026-08-09）＝**「環境・現物・先物 → エンジンへ／周期 → シールドへ。タブをタブのまま移動」**。
中身の統合はせず、タブという単位のまま所属画面を入れ替えた。

| 画面 | 内部識別子 | タブ |
|---|---|---|
| シールド（需給分析） | `'quant'` | **シールド / 周期** |
| エンジン（ポジション分析） | `'shield'` | **エンジン / 環境 / 現物 / 先物** |

🔴 **タブの識別子はすべて据え置き**（`'bunseki'` / `'sector'` / `'kankyou'` / `'genbutsu'` / `'micro'` / `'shield'`）。
所属する画面だけが変わった。

**実装のかなめ**
- 🔴 **環境/現物/先物 の中身は `QuantView` のまま**なので、**エンジン画面も `QuantView` を描画する**。
  どのタブを並べるかは新 prop **`visibleTabs`** で渡す（`App.tsx` の `SHIELD_VIEW_TABS` / `ENGINE_VIEW_TABS`）。
  スライダーの幅と `translateX` は `visibleTabs.length` から動的に計算
  （旧＝`width:400%` 固定・各パネル `25%`・`transform` は4分岐のハードコードだった）。
- 🔴 **データ取得は分割していない**。分析タブの AI プロンプト（`buildExportJson`）が全項目を使うため、
  `QuantView` はタブ集合に関わらず従来どおり全部読み込む。
  `dataCache`（localStorage）を共有するので2画面ぶんの実 fetch にはならない。
- 🔴 **エンジン画面はタブ切替で `QuantView` を再マウントしない**＝`display:none` で出し分ける（`styles.tabPane`）。
  アンマウントすると state が消えて、タブに戻るたびにローディングが出るため。
  市場3タブは**初回訪問時にマウントして以降維持**（`engineMarketMounted`。`micro` タブの既存パターンと同じ）。
- `SectorPanel` は `ShieldView` → `QuantView` へ移動（props は `theme`/`isMobile`/`user` のみなので単純）。
  `ShieldView` は周期タブを失いポジション分析1枚になったので、`shieldTab` / `onShieldTabChange` props と
  `mode` 分岐、`SectorPanel` の import を削除。`paddingBottom` の分岐（セクター時は付けない）も不要になり単純化。
- `QuantView` の `onQuantTabChange` prop は**もともと本体で使っていなかった**ので削除（タブ操作は App 側の
  フローティングサブバーが持つ）。
- ビューを離れたときのタブリセットに `shield` を追加（`prev === 'shield'` → `setShieldTab('shield')`）。

🔵 **確認済み**: フッターナビの並びとアイコン（カレンダー → 盾 → ロボット → チャート → 研究室）を
ローカル（`npm run dev`）で実測。**タブの中身は会員ログインが要るのでローカルでは未確認**＝本番で見ること。

検証: tsc(app)=0 / lint=0 / vitest **249 pass** / `npm run build` 成功。

### 2026-08-09(1): 「エンジン」⇔「シールド」の名称を入れ替えた ✅

🔵 ユーザー指示（2026-08-09）。**需給分析＝「シールド」／ポジション分析＝「エンジン」**。アイコンも一緒に入れ替え。

| | 内部識別子 | 旧名 | **新名** | アイコン |
|---|---|---|---|---|
| 需給分析（QuantView / EnginePanel） | `'quant'` | エンジン | **シールド** | 盾 |
| ポジション分析（ShieldView） | `'shield'` | シールド | **エンジン** | ロボット |

🔴 **画面の位置（ナビの並び）は据え置き**＝カレンダー / シールド / エンジン / チャート / 研究室。
ユーザー判断で「開く画面が入れ替わる」より「名前が入れ替わる」を選んだ。

🔴 **入れ替えたのは表示名・アイコン・プロンプト内の呼称・ドキュメントだけ**。
以下は**すべて据え置き**（改名しないこと）:
- ViewMode の `'quant'` / `'shield'`、タブの `'shield'` / `'sector'`
- localStorage キー（`poical-quant-memo` / `poical-quant-memo-history` / `poical-shield-memo` / `poical-shield-mkt-data-v2`）
- Firestore パス（`users/{uid}/data/shieldMemo`）
- ファイル名（`EnginePanel.tsx` / `ShieldView.tsx` / `enginePrompt.ts` / `shieldPrompt.ts` / `shieldData.ts` / `engineExport.ts`）
- 関数名（`getRecentEngineReport` / `buildShieldData`）、CSS クラス（`engine-dust` / `shield-dust`）

🔴 **据え置きの理由＝保存キーを入れ替えるとユーザーの保存済みレポートの中身が別画面のものとして表示される**
（`poical-quant-memo` は需給レポート、`poical-shield-memo` はポジションレポート）。
前例＝「セクター」→「周期」のタブ改称でも識別子 `'sector'` は据え置いている。
**この食い違いは `CLAUDE.md` の不変ルールにも明記した。**

**プロンプト側の変更**
- 需給（`enginePrompt.ts`）: Role「ぽいロボ エンジン」→「**ぽいロボ シールド**」
- ポジション（`shieldPrompt.ts`）: Role「ぽいロボ シールド」→「**ぽいロボ エンジン**」／
  「シールドの3原則」→「エンジンの3原則」／出力見出し「シールドログ・シールド判定・シールド総評」→
  「エンジンログ・エンジン判定・エンジン総評」／エラー識別子 `SHIELD ERROR` → `ENGINE ERROR`
- `ShieldView` が添付する二次参考の見出し「ぽいロボ エンジン 直近レポート」→「**ぽいロボ シールド 直近レポート**」
- ターミナルログ `POI-ROBO SHIELD v1.0` → `POI-ROBO ENGINE v1.0`

**触った範囲**: `CalendarHeader`（ナビの label と icon）/ `App.tsx`（`QUANT_LABELS` `SHIELD_LABELS` `ErrorBoundary` label）/
`EnginePanel`（ヘッダー・COPYボタンのアイコンを盾へ）/ `ShieldView`（同じくロボットへ）/
`ManualView` / `StrategyPlaybookPanel` / `PoiroboAboutPanel` / `LegalModal` / `LockSkeletons` / `SectorPanel` /
`CyberAiLaunch` / `SpecView`（現行仕様の記述＋改称の節を追加）/ `requirements.md` / `CLAUDE.md` /
`docs/legal/*` / `docs/maintenance/ai-model-check.md` / `scripts/analyze-engine-signals.mjs` / `scripts/backtest-tev.mjs`

🔵 **触っていない**: `SpecView` と本書の**★日付つき過去履歴**（当時の名称のまま。過去の記録を書き換えないため）。
2026-08-08 以前の記述で「エンジン」とあれば需給分析、「シールド」とあればポジション分析を指す。
`poirobo/` `poinavi/` は別プロダクト（旧静的サイト・翻訳アプリ）なので対象外。

**ついでに直したもの**
- `requirements.md` のシールド節が「イベント予想プロンプト」と**誤っていた**のを実装どおりに書き直した
  （実体＝日経平均ブル/ベア専用のポジション分析。イベントはプロンプトの一部品にすぎない）。
- `ManualView` の「シールド画面右下のメニューで『イベント予想』に切り替える」は**廃止済みの機能**だったので
  現行の「周期（セクター画面）」に直した。

🔴 **残っている齟齬（既知・実害なし）**: ポジション分析側のプロンプトに
「防衛（盾）／最大化（槍）／管理（鎧）」という**盾のメタファー**が残っている。
名称が「エンジン」になったので、文言を作り直すかは今後の判断。

検証: tsc(app)=0 / lint=0 / vitest **249 pass** / `npm run build` 成功。

### 2026-08-08(12): 総括の決め方を手順で縛る ✅（`f19e654`・push 済）

🔴 **基準を書くだけでは足りず、2回目の誤判定が出た**（LIFULL 2120・2026-08-08）。
解消日数 **6.7日分**で5日基準を超えているのに、
「5日基準を上回るものの…**消えつつある**」と結論した（自分で基準超過を書きながら踏み越えた）。

**対策＝手順と例外条件まで明示する**:
1. 解消日数 **5日分未満** → 「重しが消えつつある」
2. **5日分以上** → 原則「まだ重い」。
   🔴 例外は **直近3か月で買残が20%以上減っている**ときだけ。
   （ピークからの累計解消率が高くても、**直近が止まっていれば重い**。
   　いま居座っている量が問題で、過去に減ったことは関係ない）
3. 数字が取れない → 「どちらとも言えない」

🔴 「◯日分で基準を上回るものの」と書きながら「消えつつある」と結論するのは矛盾、と明記。

**あわせて2点**
- 過去1年の平均出来高は**月足の合計÷営業日数で概算**させる（安易に「不明」にさせない）
- 🔴 **信用残は最新の週を使う**（LIFULL は7/24＝1週古い数字で答えていた）

🔵 **LIFULL の実例**（判定基準の妥当性の確認）:

| | LIFULL | グリムス（軽い例） |
|---|---|---|
| 解消日数 | **6.7日分** | 1.78日分 |
| 買残の金額 | **7.0億円** | 2億円 |
| 解消率 | 24.8% | 44.0% |
| 3か月変化 | **−1.4%** | −42.3% |

LIFULL はチャート側（現在地10%・月足MACD反転）は良いが、**需給だけが噛み合っていない**。
7億円・6.7日分が3か月ほぼ横ばいで居座っている。新しい手順ならこれは「まだ重い」になる。

### 2026-08-08(11): 円環の表示整理・現在地の解釈・中央の切替ボタン ✅（push 済）

コミット: `b3c922e`（丸を現在地へ／中央テキストの重なり／金利カード削除）
／`1a6ccc9`（移行期の両論併記）／`354fa25`（現在地と行き先の丸を分離）
／`ad41738`（中央を切替ボタンに）／`413bfb7`（文言短縮）
／`c91bf54`（データ手動更新＝`rate`/`infl`/`macro` の初回生成）

**① 表示の修正**（ユーザー指摘）
- 🔴 **中央テキストが円の穴（半径88px）に収まらずリングに重なっていた**。
  行数と文字数を詰めて最大4行に（「いまの位置（金利↑×インフレ↓）」→「金利↑ × インフレ↓」等）。
- 🔴 **丸マーカーを「業種の並びが最も近い型」から「現在地（マクロ）」へ移した**。
  現在地をマクロで決めるようにしたのに丸が一致度に付いたままで、主張が2つ並んで読めなかった。
  凡例も「いまの位置（金利×インフレ）」に変更。
- 🔴 **「金利と業種」カードを削除**（判定に使っていないため）。
  🔵 ロジック（`rateAlignments` / `RATE_SENSITIVITY`）は `sectorRotation.ts` に残してある。

**② データを手動更新**（`c91bf54`・push 済）
CI は平日19:30 JST のため、土曜の時点では `rate`/`infl`/`macro` が無く、
現在地が従来の一致度表示にフォールバックしていた。`npm run fetch-data` で生成。
🔵 異常値ガードの作動も確認（1629 商社・卸売で2日を除外）。

**⑤ 円環の中央を表示切替ボタンに**（ユーザー・2026-08-08）

円の穴をクリックで「**現在地**」⇔「**一致度**」を切り替える。両方を同時に出すと扇が3行になって重なるため。

| | 現在地モード（既定） | 一致度モード |
|---|---|---|
| 中央 | マクロの現在地・行き先 | 一致度1位 |
| 扇 | ◀ いまここ／◀ 行き先？ | 一致度 ◯◯ |
| 丸 | 実線＝確定／破線＝行き先の候補 | なし |
| 下の説明 | 「◯◯相場とは？」 | 「一致度とは？」＋「?」 |

🔴 **上部に常設していた説明文と「?」はこの位置へ移した**（モードで役割が変わるため）。
🔵 穴の下端に「クリックで◯◯へ」と小さく出して、切替に気づけるようにしている。
🔵 文言を短縮＝扇の「一致」→「**一致度**」／凡例の「選択中（クリックで切替）」→「**選択中**」／
「いまの位置（金利×インフレ）」→「**いまの位置**」／
「景気の局面で物色対象が回るという見方 × 実測」→「**一致度とは？**」。
🔵 **画面の文言は短く**。ユーザーの一貫した指示（同日・`feedback_concise_results_only` も参照）。

**④ 移行期を両論併記に**（ユーザー判断・2026-08-08）

🔴 **ソフトランディングの判定は作らない**。理由＝①事後にしか確定しない（NBER・内閣府の
景気基準日付は事後認定）②リアルタイム指標は外れる（逆イールドは2022〜23年に外れた）
③正解ラベルが無く検証できない（一致度・RRG・循環の順序と同じ壁）。

代わりに**移行期は現在地を1つに確定させず、両方の経路を出す**:

```
中央   金利↑ × インフレ↓ ／ 逆金融相場 ／ 行き先は 逆業績 か 金融
       （ソフトなら逆業績を素通り）
扇     逆金融相場「◀ いまここ」（実線の丸）
       逆業績相場「◀ 行き先？」／金融相場「◀ 行き先？」（破線の丸）
```

- 🔴 **確定していないのは行き先のほう。現在地は直前のアンカーで確定している**
  （ユーザー指摘・2026-08-08。最初「現在地の候補が2つ」と誤解して実装し、直した）。
- 🔴 丸は**実線＝現在地（確定）／破線＝行き先の候補（未確定）**。
  大きさと脈動は揃える（細い破線だと見えないため）。
- 🔵 **弧は1本**＝直前のアンカーから行き先まで。ハードもソフトも**行き先は同じ**なので
  2本にしても情報が増えず、同じリング上で重なって読めない。
  外周にもう1本という案も、`SIZE = 344` に余白が無くスマホ（320px）で円環が縮むため不採用。
- 🔵 「次に来るとされる業種」は経路によらず金融相場なので変更なし

🔵 **実測との整合**：いま資金が向かっているのは商社1位・鉄鋼2位・エネルギー3位＝**逆金融相場の業種**。
逆業績相場の業種（食品8位・医薬品7位・電力ガス10位・小売11位）は動いていない。
＝ 実測は「まだ逆金融の中／素通り中で資金は前の局面に残っている」と読める。両論併記はこれとも矛盾しない。

**③ 現在地の解釈について**（記録）
外部AIが「業績相場の後半〜逆金融相場への移行」と読んだのに対し、ぽいロボは「逆業績相場」。
🔴 **背理法が想定していないケース**＝「逆金融の次は逆業績」は引き締め→景気後退の順路が前提だが、
実際に起きているのは**インフレだけが沈静化（金利↑×インフレ↓）＝ソフトランディング**。
記憶なし分類器ならこの象限は「業績相場」と判定する。
🔵 **ユーザー判断＝ソフトランディング中なら逆業績相場でよい**（2026-08-08）。判定は変更しない。
🔵 なお**次に来るのは金融相場**で、これはどちらの経路でも変わらない
（逆金融→逆業績→金融／逆金融→金融のどちらでも行き先は同じ）。
🔴 移行期の2局面は業種の裏づけが取れていない（逆業績 +0.04／業績 −0.17）ことは変わらないので、
現在地は参考にとどめ、需給とチャートで判断する運用。

### 2026-08-08(10): 需給の総括を「厚み」で判定させる ✅（push 済）

🔴 **実運用で誤判定が出たので直した**（グリムス3150・2026-08-08）。
AIが**信用倍率20.15倍だけを見て「まだ重い」**と結論した。実際は:

| | グリムス | 市場全体 |
|---|---|---|
| 信用倍率 | 20.15倍 | 8.51倍 |
| 解消率 | **44.0%** | 11.6% |
| 買残 ÷ 平均出来高 | **1.78日分** | — |
| 買残の金額 | 約2億円（82,600株×2,475円） | — |

倍率が高いのは**売残が4,100株と極端に少ないから**で、買残の実弾は小さい。
2日足らずの商いで消える量を「重し」と呼ぶのは誤り。

**対策＝判定の軸を「残っている買残の厚み」に変更**（倍率は参考値へ降格）:
1. **買残 ÷ 平均出来高（解消日数）** — 最優先。**5日分未満なら「重い」と判定しない**
2. **買残の絶対額**（株数×株価）— 表にも項目追加
3. 解消率（市場全体との比較）
4. 買残の変化率（3か月）
5. 出来高の増減・直近÷1年の厚み
6. 🔴 **信用倍率＝単独で判定しない。参考値**

🔴 プロンプトに「**倍率が高い ＝ 重い、と書かないでください**」を明記。
🔵 理由の1行には**解消日数と解消率を必ず入れさせる**。

テスト+1（計248 pass）。誤判定の実例をテストのコメントに残してある。

**追加の小修正**（ユキグニファクトリー1375 の出力を見て・2026-08-08）
- 🔴 **金額は掛け算で必ず埋めさせる**（買残の金額＝株数×株価／平均売買代金＝平均出来高×株価）。
  「平均売買代金＝不明」と返ってきたが、出来高と株価があれば計算できるので、計算式を明示した。
- 🔴 **出来高急増の要因が一過性かを書かせる**。1375 は 8/6 の1Q決算で平常比5.44倍に膨らんでいた。
  決算一過性なら解消の進みは元の水準に戻るため、そこを見落とさせない。

🔵 **判定は正しく効いた**（1375）＝解消日数1.04日分・解消率50.2%・倍率3.55倍で
「重しが消えつつある」。理由の1行にも解消日数と解消率が入っていた。

### 2026-08-08(9): 出力を表形式に固定・出来高を7項目に・タブ名を「周期」へ ✅（push 済）

**① タブ名変更**（`a08ba54`・push 済）
シールドの「セクター」→ **「周期」**。内部の識別子 `'sector'` は据え置き（保存値・テストが参照）。
🔴 **変更理由は字面を揃えるため**＝エンジン画面のタブが「エンジン／環境／現物／先物」＝
カタカナ＋漢字3つ。シールドも「シールド／周期」で同じ並びになる（ユーザー・2026-08-08）。
🔵 **実体はセクター画面**なので、仕様書・引継ぎ資料・コード内の呼び名は
**「セクター画面」のまま据え置く**（置換しない）。変わったのはタブの表示名だけ。

**② 需給プロンプトの出力を表形式に固定**（`63696d1`・push 済）
🔵 ユーザー「解釈はほとんどいらず、結果だけでいい」。
🔴 AIの回答を **表＋総括2行だけ**に限定し、前置き・解説・要約・免責を**禁止**。
総括＝「重しが消えつつある／まだ重い／どちらとも言えない」＋理由1行＋不明項目の列挙。

**③ 出来高を7項目に拡張**（未コミット）
買い残は商いがないと解消されないので、**解消の見込みを日数と厚みで読めるように**した。

| 項目 | |
|---|---|
| 平均出来高（直近1か月・1日） | 株 |
| 平均出来高（過去1年・1日） | 株 |
| **直近 ÷ 1年** | 商いの厚み（倍） |
| 平均売買代金（直近1か月・1日） | 円 |
| **買残 ÷ 平均出来高** | **解消に要する日数** |
| 出来高の傾向 | 増／減／横ばい |
| 直近の出来高急増日 | 日付・平常比・きっかけ |

🔵 **回答の書き方についても指示あり**（`~/.claude` メモリ `feedback_concise_results_only` に保存）＝
こちらの出力も短く結果だけにする。解釈・背景説明は不要。

### 2026-08-08(8): AI分析プロンプトを「需給分析」専用に作り替えた ✅（push 済）

コミット: `3825b22`（プロンプトと配線）／`daa7be7`（仕様書・本書）。

🔵 ユーザー判断（2026-08-08）＝**「会社が何をしているかは TradingView でも見られる。需給以外の情報は不要」**。
プロンプトの用途を、会社の基礎情報の整理から **個別銘柄の信用需給の分析**へ振り切った。

**① 数字を記憶から書かせない**
🔴 信用残のような細かい数値はAIが作り話をするので、**出典URLをプロンプトに明示**する
（`https://www.nikkei.com/nkd/company/history/trust/?scode={コード}`）。
参照できなければ「取得できず」と書かせ、**推測で埋めさせない**。

**② 市場全体はぽいロボの実データを渡す**（`summarizeMarketMargin()`）
個別が無いだけで、市場全体は `margin.json` に52週ぶん持っている。検証済みの比較材料として渡す:
直近の信用倍率／52週レンジ内の位置／**買い残のピークからの解消率**／直近8週の推移表。
実例（2026-07-31）＝ 8.51倍・下から92%・ピーク7,016,733(6/26)から**解消率11.6%**。

**③ AIに求めること**
①直近の数字 ②推移を**%で** ③解消率を**%で** ④市場全体との比較（重いか軽いか）
⑤売り残と踏み上げ余地（信用倍率1倍割れか）
⑥🔴 **出来高との関係＝買い残が平均出来高の何日分か**
　（ユーザー指摘「**商いがないと解消されない**」。ここが抜けると解消の見込みが読めない）
⑦増資・分割・優待変更・大株主異動など需給に直接効く出来事

**④ 総括を3択で**
「重しが消えつつある／まだ重い／どちらとも言えない」から選ばせ、理由を1〜2行。
🔴 ただし**売買の判断ではない**と明記（「買い時」「仕込み場」は禁止）。状態記述型の方針は維持。

🔵 リンクボタン（(7)で追加）は**残した**。AIが参照に失敗することがあるので、
**一次情報へ1クリックで行ける経路は残しておく**ほうが安全という判断。

テスト**+9件**（計247 pass）＝需給分析であること／出典URLの明示／市場データの受け渡し／
出来高の要求／%での要求／3択の総括／推奨の禁止／市場データが無くても作れること／`summarizeMarketMargin` の計算。

### 2026-08-08(7): 個別の信用残を日経リンクで補った ✅（push 済）

コミット: `f87e776`（リンク追加）／`e4adf84`（仕様書・本書）。

🔵 ユーザーの実際の判断手順が言語化された（2026-08-08）:
**「チャートOK → 需給改善OK → エントリー」**。需給が最後の関門。

これで判断の3層が揃った。

| 層 | 答えること | どこで | データ保有 |
|---|---|---|---|
| チャート | どれを・いくらで | TradingView（自作インジケータ） | なし |
| 市場の需給 | いま買える地合いか | **ぽいロボ `margin.json`** | あり（既存・52週） |
| **個別の需給** | この銘柄は上がれるか | **日経会社情報（新規リンク）** | なし |

**実装**: AI分析パネルに「↗ 信用残・信用倍率（日経）」ボタン。
`https://www.nikkei.com/nkd/company/history/trust/?scode={コード}` を別タブで開く。
新形式コード（197A 等）もそのまま通る。2432 / 3793 / 9843 で HTTP 200 を実測。

🔴 **データは持たない**＝TradingView と同じ「持たずにリンクする」方式で **S3 の方針どおり**。
🔵 個別の信用残は **J-Quants Standard（月3,300円）** なら取れる（`reference_stock_calendar_data_apis` に
プラン表あり）。ただし候補が数銘柄に絞られた後の確認用途なら**リンクで足りる**、という判断。
月3,300円は「12銘柄を毎日自動で見たい」段階になってから。

🔵 **参考・市場全体の需給の現状**（2026-07-31時点・`margin.json`）:
信用倍率 **8.51倍**（買い残620万／売り残73万）。過去52週レンジ 2.78〜9.85倍の中で**上から92%**
＝買い残が歴史的に厚く、**戻り売りが出やすい局面**。直近2週は 9.85→8.93→8.51 と整理が進行中。

### 2026-08-08(6): 現在地を「アンカー＋背理法」で割り出す ✅（push 済）

コミット: `81fbbe3`（取得側でアンカー確定）／`5e89d92`（判定ロジックと画面）／`38e0a4b`（仕様書・本書）。

🔵 ユーザー提案＝**「逆金融相場 → 逆金融相場ではない ＝ 逆業績相場」**。
(5)の記憶なし分類器はその日の象限だけを見るので**循環を逆走できた**（実際 2026-08-07 は
逆金融相場からインフレだけ下を向いて「業績相場」に戻っていた。業績相場は景気回復の初期なので、
引き締めのピークから逆業績相場を通らずに戻ることはない）。

**① 考え方**
- **アンカー**＝金利とインフレが**同じ方向**の日。金融相場（どちらも↓）／逆金融相場（どちらも↑）。
  🔵 実測で業種の裏づけが取れたのがちょうどこの2つ（+0.32／+0.92）。
  どちらも「金融政策が効いている局面」で、循環の**対角**にある＝構造的に強い2つだった。
- **移行期**＝方向が食い違う日。象限から直接決めず、**直前のアンカーと循環の順序から割り出す**。
  金融相場のあと → 業績相場 ／ 逆金融相場のあと → 逆業績相場。

**② 3方式の実測比較**（2015-07〜2026-08・判定できた1,745日）

| | 記憶なし | 前進のみ | **アンカー＋背理法** |
|---|---|---|---|
| 入れ替わった日 | 4.1% | 1.3% | **4.1%** |
| 続いた期間の中央値 | 8営業日 | 31営業日 | 8営業日 |
| 循環の順番どおり | 41.7% | 100%（🔴定義上） | **73.6%（実質）** |
| 詰まり | なし | **あり**（金融相場に52%居座る） | **なし** |
| 業種の裏づけ | 2/4 | 2/4 | **3/4** |

🔴 **前進のみの100%は成果ではない**（前にしか進まないので当たり前）。
アンカー方式の73.6%は**アンカーが自由に飛べる中での数字**なので実質的な情報。

**③ 業種の裏づけ**（その後1か月の対TOPIX超過・教科書の業種 − それ以外）

| 局面 | 記憶なし | アンカー＋背理法 |
|---|---|---|
| 金融相場（アンカー） | +0.32 ✓ | +0.32 ✓ |
| 逆金融相場（アンカー） | +0.92 ✓ | +0.92 ✓ |
| 業績相場（導出） | −0.58 ✗ | −0.17 ✗ |
| **逆業績相場（導出）** | **−0.83 ✗** | **+0.04 ✓** |

🔴 **逆業績相場の +0.04 はほぼゼロ**＝「もう間違ってはいない」だけで「合っている証拠」ではない。
🔴 **業績相場は −0.17 のままで該当日も5%と少ない。ここが最も弱い。**
＝ 現在地の表示にだけ使い、「この局面だからこの業種が上がる」とは書かない方針は**維持**。

**④ 実装**
- 🔴 **アンカーの履歴は取得側が確定させる**（`fetch-jpx.mjs` の `computeLastAnchor()` →
  `sector_perf.json` の `macro.lastAnchor`）。フロントは日次スナップショットしか持たないので
  **当日の値から推定してはいけない**（`analyze-sector-anchor.mjs` の逐次更新と食い違う）。
  最初フロントで推定する `inferLastAnchor()` を書いたが、この理由で捨てた。
- `macroPhase(rate, infl, lastAnchor)`。返り値に `derived`（背理法で割り出したか）を持たせ、
  画面に「移行期：◯◯相場 のあと」と出す。**アンカーか導出かを黙って混ぜない。**
- テスト+3件（計241 pass）＝アンカーは直接判定／移行期は直前のアンカーで答えが変わる／
  アンカーが無ければ象限のまま。

🔵 **実例（2026-08-07）**: 金利+0.296（↑）× インフレ−0.20（↓）＝移行期。
直前のアンカーが逆金融相場なので → **逆業績相場**。
「次に来るとされる業種」も金融相場の顔ぶれ（情報通信5位・金融除く銀行12位・電機精密15位・不動産16位）
に切り替わり、**順位が低い＝まだ動いていない**という本来の使い方に噛み合う形になった。

### 2026-08-08(5): 円環の「現在地」をマクロ（金利×期待インフレ）に変えた ✅（push 済）

コミット: `268547e`（取得）／`827a4ec`（現在地）／`135c128`（仕様書・本書）。

🔵 ユーザー提案。(4)で金利を入れたが、**円環の現在地はまだ一致度（業種の並び）が決めていた**＝循環が残っていた。
金利だけだと4局面を2つまでしか絞れない（金融/逆業績はどちらも金利↓、業績/逆金融はどちらも金利↑）ので、
**期待インフレを2本目の軸**に入れて4象限にした。Merrill の Investment Clock と同じ並び。

| 金利 | インフレ | 局面 |
|---|---|---|
| ↓ | ↓ | 金融相場 |
| ↑ | ↓ | 業績相場 |
| ↑ | ↑ | 逆金融相場 |
| ↓ | ↑ | 逆業績相場 |

**① 実測（`scripts/analyze-sector-macro.mjs`・2015-07〜2026-08・判定できた1,745日）**

| | 一致度だけ（旧） | **マクロ（新）** |
|---|---|---|
| 局面が入れ替わった日 | 21.6% | **4.1%** |
| 続いた期間の中央値 | 2営業日 | **8営業日** |
| 最長 | 2.8か月 | **7.1か月** |
| 循環の順番どおり | 35.6% | **41.7%** |

🔵 **「ほぼ毎日入れ替わる」問題が構造的に解消した**。業種の騰落率を使わないので完全に非循環。

🔴 **ただし業種の裏づけが取れたのは4局面中2つだけ**（その後1か月の対TOPIX超過・教科書の業種−それ以外）:
逆金融相場 **+0.92 ✓**／金融相場 **+0.32 ✓**／業績相場 −0.58 ✗／逆業績相場 −0.83 ✗。
うまくいっていない2つは該当日が少ない（業績11%・逆業績8%）のでサンプル不足の可能性が高いが、
**いまの証拠では「この局面だからこの業種が上がる」とは書けない**。
＝ **現在地の表示にだけ使う。** ユーザー了解済み（最終判断はTradingViewのチャート側で行うため、
この画面は「現在地 → 教科書どおりの業種 → 銘柄の絞り込み」までで良い、という整理）。

**② 実装**
- `fetch-jpx.mjs`: FRED の `T10YIE`（10年ブレークイーブン）を追加。🔵 **APIキー不要**のCSV・2003年から日次。
  CPI の発表を待たず、市場が織り込んだ期待インフレをその日に取れる。`sector_perf.json` の `infl` に同居。
- `sectorRotation.ts`: `macroPhase(rate, infl)`。どちらかが横ばい
  （金利 ±0.10 / インフレ ±0.05%ポイント未満）なら **null＝判定しない**。
- `SectorPanel.tsx`: 円環の中心＝マクロの現在地。扇に「◀ いまここ」。
  一致度は**「業種の並びがこれを支持しているか」に降格**（食い違う日は「業種の並びは◯◯に近い」と併記）。
  🔴 **「次に来るとされる業種」の起点もマクロに統一**（画面に現在地が2つある状態を解消）。
  マクロが判定できない日は従来の一致度表示に戻る。
- テスト+2件（計238 pass）＝4象限が循環の順番と一致／横ばいなら判定しない。

🔵 **実例（2026-08-07）**: 金利+0.296（↑）× インフレ−0.20（↓）＝ **業績相場**。
一致度は逆金融相場（83.3）なので食い違い、画面には「業種の並びは 逆金融相場 に近い」と併記される。

**③ 本番で出るのは CI の次回実行後**（`infl` を `fetch-data.yml` が生成するため）。

### 2026-08-08(4): 金利を入れた ─ 初めて「外側の情報」が入った ✅（push 済）

コミット: `2ebed01`（取得）／`a265b46`（カード）／`744614b`（仕様書・本書）。

🔴 **(3)で行き詰まった原因が特定できた＝循環していたこと。**
業種の騰落から局面を決めて、その局面で業種を語っていたので、**新しい情報が一切入らない**。
どう精密にしても中身が増えない構造だった。

🔴 **そして、金利データを1本も持っていなかった。**
4局面理論はもともと「**金利 × 業績**」の2軸で定義されるのに、持っていたのは
その2軸のどちらでもなく、結果として出てきた株価だけだった。

**① 実測（`scripts/analyze-sector-rate.mjs`・2015-07〜2026-08・2,624営業日）**

A) **同時点＝はっきり出た。** 金利が3か月で±0.25%ポイント動いた局面の対TOPIX超過リターン平均:

| 業種 | 金利↑期 | 金利↓期 | 差 | 相関 |
|---|---|---|---|---|
| エネルギー資源 | +6.73% | −4.87% | 11.60 | 0.465 |
| **銀行** | **+7.34%** | −3.57% | 10.91 | **0.484** |
| 金融（除く銀行） | +4.33% | −1.77% | 6.10 | 0.430 |
| 食品 | −3.52% | +1.34% | −4.86 | −0.303 |

B) 🔴 **予測＝使えない。** 重なりを除いた独立サンプル（各40件・有意の目安 |r|>0.316）で
3か月先の相関が閾値を超えたのは3件（運輸・物流0.540／食品0.395／電力ガス0.350）だが、
**17業種を同時に検定している**ので多重検定の補正（閾値≈0.45）をかけると残るのは1件だけ。
「17回引いて1回当たった」と区別がつかない。🔵 ただし符号の並び（ディフェンシブ＋／景気敏感−）は一貫。

**② 実装したもの**
- `fetch-jpx.mjs`: `^TNX`（米10年債利回り）を1リクエスト追加し、**`sector_perf.json` に `rate` として同居**。
  🔵 専用ファイルもフロントの新規ローダーも増やさない。取得失敗時は null で、業種表示は続く。
  🔵 日本国債10年は無料で安定して取れないため米10年債で代用（画面にも明示）。
- `sectorRotation.ts`: `RATE_SENSITIVITY`（**15年の実測相関**をそのまま定数化・理論の当てはめではない）と
  `rateAlignments()`。金利が3か月で **±0.25%ポイント未満**しか動いていなければ**カード自体を出さない**
  （動いていない金利で「こうなるはず」と言う根拠がないため）。
- `SectorPanel.tsx`: 中列に「金利と業種」カード。教科書の期待（上位/下位のはず）と実測順位を並べ、
  ズレを ⚠ で立てる。
- テスト+3件（計236 pass）＝17業種すべてに感応度がある／金利が動いていなければ空／向きで期待が反転する。

🔴 **出すのは「一致／ズレ」まで。ズレを予測の材料にしない。**
⚠ は「教科書と違うことが起きている」という事実の指摘に留める（文言にも明記済み）。

**③ 本番でカードが出るのは CI の次回実行後**
`rate` は `fetch-data.yml`（平日19:30/21:30 JST）が生成する。それまでは `rate` が無いので静かに非表示。

**④ 次の案（未検証）**: 🔵 ユーザー提案＝**金利の向きで4局面を2つに絞り、一致度で残り2つから選ぶ**。
金利↑なら業績相場/逆金融相場、金利↓なら金融相場/逆業績相場に候補が限定される
（理論上 金融相場と逆業績相場はどちらも「金利↓×業績悪」で、金利だけでは区別できないため）。
🔵 検証可能な指標＝①1位の入れ替わり頻度が 20.2% より下がるか ②循環の順番に従う率が 37.5% より上がるか。
正解ラベルは無いままだが、**この2つは測れる**。着手前に必ず測ること。

### 2026-08-08(3): セクターの精度を上げようとして「上がらない」と分かった日 ✅（push 済）

コミット: `1d062f8`（異常値ガード）／`412c46b`（検証スクリプト）／`93a440f`（仕様書・本書）。
🔵 ガードは **CI（平日 19:30 / 21:30 JST の `fetch-data.yml`）で効く**。
異常が出たら実行ログに `⚠ 1629 商社・卸売: 異常値 2日を除外（0.8%）` の形で残る。

**① 🔴 本番に関わるデータ異常を発見し、ガードを入れた**

`1629`（商社・卸売）の Yahoo 系列は **15年のうち約970日ぶんが壊れている**。

```
2026-03-27  288.60円   出来高    993,000
2026-03-30    0.5676円  出来高 253,010,000  ← 別銘柄のデータ
2026-03-31    0.5498円  出来高 409,140,000
2026-04-01  288.70円   出来高    553,490
```

2014〜2015年には**調整後終値がマイナス**（−452,348 等）の期間もある。
🔵 騰落率は期間の両端しか見ないので**今日の本番表示は無事**（1か月=7/8→8/7、6か月=2/5→8/7 が該当日を踏んでいない）。
🔴 しかし**壊れた日が端点に来たら、その業種が −99% や +52,410% として順位に入り、
17業種のランキングが黙って崩れる**。検知する仕組みが無かった。

対策＝`fetch-jpx.mjs` に `dropAnomalies()` を追加。
**前後10営業日の中央値から35%以上外れる日**と0以下の値を落とし、
落とした割合が5%を超える業種はその業種ごと捨てる（既存の `rows.length < 14` が最後の砦）。
🔴 **前日比で切ってはいけない**。連続する異常（0.5676→0.5498 のような2日以上の区間）は
互いに近いので前日比ではすり抜ける。窓の中央値を基準にすること。
実データ検証＝1629 の該当2日だけ除外（日次最大変動 52,410%→8.2%）、**他16業種は誤検出ゼロ**。

**② 精度を上げる試み＝RRG は不発。実装しない**

「いまは何相場か」は正解ラベルが無く永久に検証できないので、
「この業種は市場平均に対して強いか／加速しているか」（＝RRGの4象限）に置き換えれば
**次の1〜3か月の超過リターンという正解**ができる、という筋で試した。

その後1か月の対TOPIX超過リターン（2015-07〜2026-08・延べ44,489件）:

| 象限 | 平均 | 中央値 | 勝率 |
|---|---|---|---|
| **Improving**（弱いが加速・期待していた象限） | **−0.15%** | −0.17% | **47.8%** |
| Leading（強く加速） | −0.23% | −0.37% | 45.4% |
| Weakening（強いが減速） | +0.02% | −0.07% | 49.3% |
| **Lagging**（弱く減速） | **+0.19%** | +0.09% | **51.4%** |
| （全体） | −0.03% | −0.12% | 48.5% |

🔴 探したかった **Improving は平均以下**。わずかに勝つのは Lagging＝単なる平均回帰で、
差は勝率3ポイント弱。しかも**窓が重なった観測を延べで数えている**ので実質サンプルはもっと小さく、
**有意性は主張できない**。象限は反時計回りに49.6%で回る（でたらめなら33.3%）が、
回転しても先の超過リターンには結びついていない。

🔴 **結論＝業種の相対順位には1〜3か月先を当てる力がほとんど無い**。手法の問題ではない。
**予測を出さない現在の建て付け（理論と実測を並べるだけ）を維持する。**

**③ 円環の入れ替わり方も測った**（`analyze-sector-span.mjs` に統合）
- 前日と1位が変わった日: **20.2%**（739日/3,667日）
- 3営業日連続で1位が全部違った: **92回**（2.5%・年6回ペース）
- 移り先が**循環の順番どおりだったのは37.5%**（でたらめなら33.3%）
  ＝ 日次で見るかぎり「金融→業績→逆金融→逆業績」の順番は現れていない

🔵 **この画面の価値は予測力に乗っていない**。実体は「業種を選ぶ→銘柄を絞る→TradingView→AI分析」
という**探索の導線**で、そこは円環が当たるかと無関係に機能する。円環は羅針盤ではなく**地図**。

**④ 次にやるなら**（精度を追う場合）
価格から先を当てにいくより、**エンジン側の需給（信用・裁定・投資部門別）と業種を突き合わせる**ほうが筋が良い。
「誰が買って誰が売ったか」は構造情報で、価格の後追いではないため。ただし相応に大きな話。

### 2026-08-08(2): TradingView の開き方を管理者だけ変更／不要物の削除 ✅（push 済）

コミット: `e263b07`（セクター＋シールド）／`c8f61da`（残骸削除）／`3d9865a`（仕様書・本書）。

**① TradingView のURLを管理者とそれ以外で分けた**（ユーザー・2026-08-08）

| | 開くURL |
|---|---|
| 管理者（`isAdminEmail`） | `https://jp.tradingview.com/chart/ecEzo0V0/?symbol=TSE%3A{コード}&interval=1M` |
| それ以外・未ログイン | `https://jp.tradingview.com/symbols/TSE-{コード}/?timeframe=1M` |

`ecEzo0V0` は**管理者本人の保存済みチャートレイアウト**（画面右上の名前は「ポイントラボ」）。
🔴 **一般ユーザーには使わない**＝他人が開くと管理者のインジケーター構成や描画が見えるため。
判定は `utils/admin.ts` の `isAdminEmail()`（`firestore.rules` との齟齬を CI が検知する既存の単一情報源）。
配線は `ShieldView` → `SectorPanel` に `user` を渡す形。
🔵 **両方とも月足**。インジケーターや配色は管理者だけ自分のレイアウトのものが乗る。
🔴 **足のパラメータ名が違う**＝チャート（`/chart/`）は `interval`、銘柄ページ（`/symbols/`）は
`timeframe`（表示範囲）。取り違えると効かない。`interval=1M` が効くことは実機で確認済み
（ツールバーの「月」が選択状態・銘柄名の横も「TSE:1377・1月」）。

🔵 **調べた結論（再調査しないこと）**
- TradingView 規約：**外部リンクは制限対象外**。制限されるのは「コンテンツ/データを自前の画面に表示すること」
  （Section 3 の display-only／スナップショットには出典表示が必要）と「自動アクセス・機械処理」。
  レイアウトURLの共有は TradingView 公式の「共有」機能が生成するものなので禁止条項なし。
- **有料プランは相手に渡らない**。機能制限は**開いた人のアカウント**で判定され、招待制/有料の
  サードパーティ製インジケーターはアカウント単位の許可制でソースも見えない。伝わるのは「構成」まで。
  ※ 別アカウントでの実地検証はしていない（仕組み上の説明）。確かめるならシークレットウィンドウで開く。

**② 下端の空白の帯を解消**（ユーザー指摘）

`ShieldView` の `paddingBottom: isMobile ? 130 : 0` が、セクタータブでは**常時見える死んだ帯**になっていた
（`SectorPanel` は自前で1本スクロールするため、親の padding が中身の末尾にならない）。
🔴 `paddingBottom` は **`mode === 'shield'` のときだけ**に変更し、セクター側の下端の逃げは
**スクロールの中身の末尾**＝`SectorPanel` 右列の `padding-bottom: 120px` に移した
（浮いている「シールド／セクター」トグルとフッターに最後の項目が隠れないため）。
実測でスクロール枠が 650px → **776px** に拡大、最下部の AI 起動アイコンも隠れないことを確認。

**③ 削除・据え置き**
- 🔴 **反転臨界モニターの残骸3ファイルを削除**（`src/utils/reversalWatch.ts` /
  `src/utils/__tests__/reversalWatch.test.ts` / `scripts/check-reversal-watch.ts`）。
  削除前に `src`/`scripts`/`docs`/`package.json`/`.github` を走査し参照ゼロを確認。
  **テストは 252 → 233 件**（削除したテスト自身の19件ぶん・テストファイル 20→19）。
- 検索結果の行末に置いていた `↗` は**削除**（ユーザー判断）。
- 「ぽいロボとは？」ページを開いたときにフッターを閉じる挙動は**残す**（ユーザー判断）。

検証: tsc(app)=0 / lint=0 / vitest **233 pass**。

### 2026-08-08(1): セクター画面のスマホ表示を修正／行クリックを TradingView へ ✅（push 済）

コミット: `75053d1`（セクター画面）／`380750b`（フッター）／`b66abff`（本書）。
🔵 push 時に CI のデータ自動更新 `b25899d` が先に入っていたため rebase 済み（`public/data` のみで競合なし）。

**① スマホ表示が壊れていた（S4・実測で確認）**

縦積み（`isMobile`）のとき、**主役の「次に来るとされる業種」が高さ 63px の帯に潰れていた**
（中身は306px＝243px が内部スクロールに隠れる）。右列も 83px / 中身184px で入力欄が切れていた。

🔴 **原因＝中列・右列が `flex: 1` + `overflowY:'auto'` を PC/スマホ共通で持っていたこと**。
左列だけ `flex: isMobile ? '0 0 auto' : 1` と分岐済みだったため、縦積みでは
左列が円環の高さ(344px)を先に取り、**残りを中列と右列が奪い合って潰れた**。

修正（`SectorPanel.tsx`）:
- 中列・右列も `flex: isMobile ? '0 0 auto' : 1` / `overflowY: isMobile ? 'visible' : 'auto'` に。
  スクロールは**ルートの1本だけ**にする（内部スクロール 2本 → 0本）。
- 中列の `justifyContent` はスマホでは `flex-start`（高さが中身なりになるので中央寄せは無意味）。
- 右列の `padding-top` はスマホでは 44→24px（3列で高さを揃えるための余白なので縦積みでは不要）。
- 🔴 **円環の SVG に `maxWidth:'100%'` + `height:'auto'`**。344px 固定＋左右padding16pxで
  **376px 必要**だったため、360px 以下の端末で横にはみ出す。viewBox があるので縮小で解決。

検証（`TEMP_PUBLIC_ALL_PAGES=true` + 同一オリジン iframe で 390/360/320px）:
横スクロール 0 / 内部スクロール 1本 / 円環 390→344・360→323・320→283px に自動縮小。

**② 検索結果の行クリック＝TradingView を別タブで開く**（ユーザー・2026-08-08）

`https://jp.tradingview.com/symbols/TSE-{コード}/` を `window.open(..., 'noopener,noreferrer')`。
🔴 **コードのクリップボードコピーは廃止**（`copiedCode` state と「コピーしました」表示を削除）。
貼り先が TradingView と決まっている以上、貼る手間を省いて直接開く、という判断。
行クリックでの**選択（下のAI分析パネルの対象にする）は従来どおり**残る。
行末に `↗` を置いて外部リンクであることを示す。ヘルパは `openInTradingView()`（モジュール直下）。

**③ 宿題の整理**
- 🔴 **既存0「反転臨界モニターの日足モメンタム確認」は削除**。機能ごと消えている（ユーザー指摘）。
  UI からの参照はゼロで、残っているのは `src/utils/reversalWatch.ts` /
  `scripts/check-reversal-watch.ts` / そのテストの3ファイルのみ。**削除するかは未判断**。
- 🔵 「直近1年で高値圏/安値圏か」を出す案は**ユーザー判断で見送り**（2026-08-08）。
  ※業種17本なら `fetch-jpx.mjs:1427` が既に `range=1y` の日次を取っているので追加通信ゼロで出せる、
  という調査結果だけ残す。再提案はしないこと。

🔵 **スマホ幅の確認方法（次回も使う）**: Chrome が最大化されているとウィンドウのリサイズが効かない。
`src/App.tsx` の `TEMP_PUBLIC_ALL_PAGES` を一時的に `true`（会員ゲートを外す）にして、
**同一オリジンのページに幅指定の iframe を並べる**と `matchMedia` が iframe 幅で評価され `isMobile` が効く。
🔴 **確認が終わったら `TEMP_PUBLIC_ALL_PAGES` を `false` に戻すこと**。

**④ フッターの自動クローズを削除**（ユーザー・2026-08-08）

🔴 第22で入れた「PC でコンテンツエリアをクリックすると開いているフッターを閉じる」を**削除**。
画面を操作するたびに閉じてしまうため、**開閉はつまみ（^）の手動操作だけ**にする。
`App.tsx` の `<div style={styles.body} onClick={collapseFooter}>` から `onClick` を外した。
🔵 `collapseFooter` 自体は残す（「ぽいロボとは？」ページを開いたときに閉じる用途で使用中）。
SpecView 第22節の記述も直した。**復活させないこと**。

検証: tsc(app)=0 / lint=0 / vitest **252 pass**。

### 📌 2026-08-07 のまとめ（セクター画面をゼロから作った日）

シールドの「イベント予想」タブを潰して**セクター画面**を新設し、同日中にダミー→実データ→
UIの作り込みまで一気に進めた。コミットは feat 5本 + docs 5本 + refactor 1本。

**この画面の目的（最重要・以後ここから外れていないか毎回確認する）**
> 「今伸びている業種ではなく、**これから上がるであろう銘柄を探すのが目的**なので、次のセクターが分かればいい」
＝ 導線は **いまの型 → 循環の次の局面 → その業種 → その銘柄 → コピー(TradingView) / AI分析**。

**画面の構成（3列）**
| 列 | 中身 |
|---|---|
| 左 | 円環（4局面 × 一致度）／次の局面への矢印／「◯◯相場とは？」モーダル／「?」ヘルプ |
| 中 | 次に来るとされる業種／いま資金が向かっている業種 |
| 右 | 銘柄を探す（行クリックでコードをコピー）／AI分析プロンプト |

**データ（すべて無料ソース・`fetch-jpx.mjs`）**
- `stock_master.json` … JPX 上場銘柄一覧（内国株 3,713銘柄・月1更新）
- `sector_perf.json` … TOPIX-17 業種別ETF(1617〜1633)の調整後終値から騰落率・順位・`periods`

**このセッションで確定した方針（崩さないこと）**
1. 🔴 **局面の判定はしない**。出すのは「型との一致度」まで。**確率とは呼ばない**（正解ラベルが無く検証できないため）
2. 🔴 **予測を書かない**。理論（循環の順番・業種の顔ぶれ）と実測（順位・騰落率）を並べるだけ
3. 🔴 **画面に出す期間は直近1か月だけ**。3か月以上は直近を含む重なった窓で、主役交代時に混ざる
4. 🔵 **UI方針**＝説明的すぎず、UIで分かりやすく、洗練され、最低限の説明はある
5. 🔵 **役割で場所を分ける**＝「◯◯相場とは？」は理論だけ／「次に来る業種」は理論×実測の突き合わせ

🔴 **この画面の弱点（ヘルプにも明記済み）**
`scripts/analyze-sector-span.mjs` で15年ぶん実測した結果、**一致度1位は中央値2営業日で入れ替わる**
（15年で740回・1か月以上続いたのは37回）。腰の据わった局面判定ではなく、その日の並びの特徴にすぎない。
ユーザー判断で**平滑化は入れない**（元々が経験則の枠組みなので厳密さを求めない）。

🔵 **ユーザーはUIデザイナー**（元エンジニア）。UIの指摘は具体的な修正指示として来るので、
そのまま直せばよい。逆に**こちらから出す画面は、実機のスクリーンショットで確認してから出すこと**
（今回は重なり・高さのガタつき・枠線の潰れを、指摘されるまで自分では見つけられなかった）。

🔴 **今日踏んだ罠（同じ手を繰り返さない）**
| 症状 | 原因 | 対処 |
|---|---|---|
| 選択中の行だけ左の色帯が3px→1pxに潰れる | React のインラインで `border`(一括)と`borderLeft`を混ぜた | 4辺 longhand に分ける |
| ダークなのに入力欄だけ白い | Chrome オートフィルが `!important` で背景上書き | `box-shadow ... inset` で塗る＋`autoComplete="off"` |
| 「銀行」で検索して銀行株が出ない | 17業種名「金融（除く銀行）」に部分一致 | 業種名の**前方一致**を最優先にした関連度順 |
| 順位が分配金で入れ替わる | 素の終値を使っていた | **調整後終値(adjclose)** で計算 |
| 本番の反映が確認できない | `index-*.js` だけ見ていた | セクター画面は **`ShieldView-*.js`** という別チャンク |

### 2026-08-07(9): 行クリックでコードをコピー／モーダルの役割分担／局面の長さを実測 ✅（push 済）

- **検索結果は行のどこを押してもコードをコピー**（選択とコピーを1クリックにまとめた）。
  コード部分だけの個別ボタンは廃止。「コピーしました」を1.6秒表示。
- 🔴 **「?」から開くものはすべてモーダルに統一**。片方が吹き出しで挙動が揃っていなかった（ユーザー指摘）。
- 🔴 **「◯◯相場とは？」モーダルは理論だけにする**（ユーザー「業績相場の説明なので理論が大事」）。
  順位・騰落率・一致度・支持/矛盾のバッジを全部外し、名前／景気の位置づけ／説明文／業種の顔ぶれのみ。
  🔵 実測は 円環（一致度）と中央列（順位・騰落率）に既にあるので**役割で場所を分ける**。
  🔵 対して「次に来るとされる業種」は**理論（顔ぶれ）×実測（順位）の突き合わせ**が目的なので混ぜたままでよい。

🔴 **局面の長さを15年ぶんで実測した（2011-08〜2026-08・3,689営業日）**:
| | |
|---|---|
| 一致度1位が続いた期間の中央値 | **2営業日** |
| 平均 | 0.2か月 |
| 入れ替わり回数 | **740回** |
| 1か月以上続いた回数 | 37回（平均1.3か月・最長2.8か月） |

＝ 円環の「1か月の並びが最も近い型」は**ほぼ毎日入れ替わる**。腰の据わった局面判定ではない。
「次に来るとされる業種」もこの1位を起点にしているので日によって変わる。
🔵 参考：教科書側は 内閣府の景気基準日付で1循環 約4〜5年＝**1局面あたり1年強**（株価は景気に先行するぶん短め）。
🔵 ユーザー判断＝**平滑化は入れない**（元々が経験則の枠組みなので厳密さを求めない）。
代わりに**この不安定さをヘルプに明記**した。再測は `node scripts/analyze-sector-span.mjs`（指標を変えたら測り直すこと）。

### 2026-08-07(8): 銘柄コードのコピー、局面モーダル、UI不具合2件の修正 ✅（`3c086e7` で push 済）

🔵 **コピー先は TradingView 想定**（ユーザー・2026-08-07）。
＝ ぽいロボ側に個別銘柄の株価を持つ必要がない。チャートも指標もTradingViewが持っているため。
無料で取れるのは値動きだけ（時価総額/PER等は Yahoo quoteSummary で不安定・業績は実質不可）なので、
**個別のファンダはAIに調べさせる**という今の分業のままでよい。

- **銘柄コードのコピー**を追加。素の `1332` と、取引所つき `TSE:1332` の2種類。
  🔵 TradingView は素のコードだと海外銘柄と競合することがあるので `TSE:` 付きも用意した。
  検索結果の行は**コード部分だけクリックでコピー**（点線下線・`stopPropagation` で行選択と分離）。
- **選んだ局面の内訳をモーダルへ**。左カラム下部の「◯◯相場とは？＋?」から開く。
  Esc / 背景クリック / × で閉じる。モーダル内なので `minHeight` は外した（下に何も無くガタつかない）。
- 🔴 **Chrome のオートフィルで入力欄だけ白くなっていた**のを修正。
  `background` では `!important` に勝てないので **内側シャドウ（`box-shadow ... inset`）で塗り潰す**。
  併せて `autoComplete="off"`。他の入力欄で同じ症状が出たらこの手を使う。
- 🔴 **選択中の行だけ左の色帯が 3px → 1px に潰れていた**（ユーザーが発見）。
  原因＝**React のインラインスタイルで `border`（一括指定）と `borderLeft` を混ぜていた**こと。
  選択状態が変わると React は値の変わった `border` だけを再適用し、
  値が同じ `borderLeft` は設定し直さないため、一括指定が左の3pxを潰す。
  🔴 **インラインスタイルで一括指定と個別指定を混ぜないこと**（4辺 longhand に分けて解消）。
  同じ書き方をしていた他2か所も直した。実測で全行 3px を確認。

### 2026-08-07(7): 左・中は上下中央寄せに戻し、スクロール枠を広げた ✅（`cefa016` で push 済）

- 🔵 **左（円環）と中（業種）は上下中央寄せに戻した**。`minHeight` で高さを固定したので、
  局面を切り替えても中身が動かなくなったため（ユーザー「高さを変化させなければ動かないから大丈夫」）。
- 🔴 **右（銘柄）だけは上揃えのまま**。検索結果とAIパネルで**高さが変わる**ので、
  中央寄せにすると入力欄が上下に動く。ここは触らないこと。
- 🔵 検索結果と業種ランキングのスクロール枠を **220/300 → 420** に拡大。
  列に高さの余裕があるのに数件しか見えず、絞り込みづらかった（実機で12件見えるのを確認）。

### 2026-08-07(6): 3列レイアウト化と、説明の削ぎ落とし ✅（`bbb2fa3` で push 済）

🔵 **画面づくりの方針が言語化された**（ユーザー・2026-08-07）:
「**説明的すぎず、UIで分かりやすく、洗練された、でも最低限の説明がある**ぐらい」。
以後この画面に文章を足すときは、まずUIで表現できないかを考えること。

- **3列に再編**。左＝円環／中＝業種（局面の内訳・次に来る業種・いま強い業種）／右＝銘柄を探す＋AI分析。
  🔵 円環の脇に出していた吹き出しは廃止（3列だと中央列がその役目を果たす）。
- 🔴 **「この見方での次の局面 → ◯◯（循環の順番。いまの位置を…）」の文章を削除**し、
  代わりに**円の外周を回る破線の矢印**で「いまの型 → 次」を示すようにした。
  🔵 矢印は `R_NEXT = R_OUT + 24`。内側に描くと中央のテキストと重なって読めない（実機で確認）。
  そのぶん `SIZE` を 320→344 に広げてある。
- 🔴 一致度の計算方法の説明を局面カードから削除。**左上の「?」ヘルプ1か所に集約**（重複解消）。
  一致度の一覧表も撤去＝期間が1つになったので円環の各扇の「一致 67.3」と同じ内容だった。
  **視線を円環に集めるため**。
- 🔴 **「いま資金が向かっている業種」は既定でプラスの上位5つだけ**。
  17件全部だとマイナスまで並び、見出しと矛盾する（ユーザー指摘「資金が向かってるのにマイナスはおかしい」）。
  「全17業種」ボタンで展開でき、そのときは見出しを「業種ランキング（全17業種）」に切り替える。
- 🔴 **上下中央寄せをやめて上揃え**。検索結果やAIパネルが増減するたびに中央位置が変わり、
  **入力欄が上下に動いてしまう**ため（ユーザー指摘）。ただし上に貼り付きすぎるので `padding-top: 44px`。
- 「反発」の説明も1行に圧縮。
- 🔴 **カードの高さを固定**。局面ごとに説明の行数（1〜2行）と業種数（3〜6）が違うため、
  円環をクリックするたびに下の「次に来る業種」以下がガタついた（ユーザー指摘）。
  局面カードの説明文と業種リスト、および「次に来る業種」のリストに `minHeight` を入れ、
  **いちばん大きい局面（6業種）に合わせて場所を確保**してある。
  🔵 業種を増減させるときはこの `minHeight` も合わせること。

### 2026-08-07(5): 画面の主役を「**次に来るとされる業種**」に変えた ✅（`737ac6d` で push 済）

🔴 **この画面の目的が確定した**（ユーザー・2026-08-07）:
「今伸びている業種ではなく、**これから上がるであろう銘柄を探すのが目的**なので、次のセクターがわかればいい」。
＝ **いまの型 → 循環の順番で次の局面 → その業種 → その銘柄 → AI分析** が一本で繋がる導線が本体。
以後この画面を触るときは、この目的から外れていないかを最初に見ること。

- **「次に来るとされる業種」を右ペインの先頭に新設**。いまの型（一致度1位）の次の局面と、
  その業種を「いま17業種中何位か」つきで出す。行クリックでその業種の銘柄検索へ。
  🔵 順位が低い＝**まだ動いていない**、という事実の確認になる（それが探したいもの）。
  🔴 **予測は書かない**。「循環の順番ではこう」という理論と、その業種のいまの実測を並べるだけ。
  🔵 ユーザー指摘「特定の銘柄ではないので大丈夫では？」＝そのとおりで業種レベルは個別推奨より
  risk が低い（無料・友人限定も効く）。ただし**4局面モデル自体が経験則で順番どおり回る保証が無い**ため、
  法務というより**精度**の理由で断定は避ける。代わりに**くどい注意書きは削って2行に圧縮**した。
- **期間の切替（1か月/3か月/6か月）を廃止**。
  🔴 ユーザー指摘「1か月とか3か月とか分かりづらい」「1か月と6か月を比較する理由も分からない」
  「1か月だけではだめなの？」＝ほぼそのとおり。**主役は直近1か月（いまの勢い）に固定**。
  🔵 ただし1か月だけだと**下げたあとの戻りを「いま強い」と誤読する**。
  実例＝鉄鋼・非鉄 1か月 +6.0%（3位）／3か月 −19.1%（最下位）。
  → `sectorRanking()` で **1か月プラス×3か月マイナスに「戻り」印**を付けて見分けられるようにした（テストで固定）。
  6か月はデータは取り続けるが**画面には出さない**。
- **「1か月」の実日付を出す**（`periods` を `sector_perf.json` に追加＝`{days, from, to}`）。
  営業日で数えているので「◯か月前の同じ日」とは一致しない。推測させず `7/8→8/7` と書く。
  一致度マトリクスの見出しも「いまの勢い／その前からの流れ」＋日付に変更。
- **「いま資金が向かっている業種」ランキング（17業種）**を追加（1か月の強い順・戻り印つき）。
  ＝すでに動いた側。「次」を判断する材料として、順番が実際に回っているかを見るために置く。
- 検証: **vitest 250 pass（+5）**／tsc(app) 0／lint 0。実機で「次に来る→クリック→銘柄検索」まで確認。

### 2026-08-07(4): 局面の内訳を、円環の脇の**吹き出し**に移した ✅（`737ac6d` に同梱）

ユーザー要望「金融相場を選択したら左のエリアに吹き出しで出てくる、みたいにできるかな」。
右ペインにあった局面カードを左（円環の隣）へ移し、**局面の話＝左／銘柄の話＝右**に役割を分けた。

- 🔴 **円環に重ねて出すのは失敗**（一度実装して実機で確認）。
  吹き出し自身が、いま選んでいる局面のラベルを隠してしまう。
  左ペインの幅では、重ねずに置ける吹き出しは200px弱しか取れず、内訳が入らない。
  → **円環と吹き出しを横に並べる**形に変更（左=円環320px／右=吹き出し300px）。
- 🔵 吹き出しの枠は**選択の有無にかかわらず場所を取る**。そうしないと円環が横に動く。
- 🔵 尻尾は円環側（左）に向け、選んだ局面が上半分（金融0度・逆業績270度）なら上寄り、
  下半分なら下寄りに置いて、どの扇から出ているかが分かるようにした。
- スマホは横に並べる余白が無いので、従来どおり右ペインに素で出す（`isMobile` で分岐）。

### 2026-08-07(3): 「いまどの局面か」を**型との一致度**で出せるようにした ✅（`f9ef33e` で push 済・Vercel 自動デプロイ）

**きっかけ**＝ユーザーから「どこの可能性が高いぐらいならできるのでは？」。
これまで「局面の判定はしない」で通していたが、**断定ではなく一致度なら出せる**し、
「プロだって感覚とか一致度で出しているはず。明確な基準がないんだから」という指摘ももっとも。
**むしろ数字で出すほうが正確**、という整理で実装した。

- **`phaseFits(rows, key)` を新設**（`sectorRotation.ts`）。
  その局面で強いとされる業種が**17業種中どのあたりに並んでいるか**だけで 0〜100 を出す。
  上位を独占＝100／下位を独占＝0。局面ごとに業種数が3〜6と違うので、
  取りうる最良・最悪の平均順位で**正規化**して揃えている（しないと業種数の多い局面が不利）。
  🔵 平均騰落率ではなく**順位**を使うのが肝。銀行 +20% のような突出1業種に
  グループ全体の評価を持っていかれない（テストで固定）。
  🔵 上位1/3を `support`／下位1/3を `contradict` に分類し、**点数の根拠を画面に出す**。
- 🔴 **「確率」と呼ばない**。局面には正解ラベルが無く**当たったかを検証できない**ため。
  過去に確信度が自信過剰かつ反転していた（70%台で実勝率25%・第25セッション）反省を踏まえ、
  画面・AIプロンプトの両方に「確率ではありません」と明記し、
  AIには**確率への言い換えを禁止**する一文を入れた（テストで固定）。
- 🔴 **4局面すべてを並べて出す**（1位だけ出すと断定に見える）。
  さらに**1か月/3か月/6か月を切替ではなく同時に**マトリクス表示。
  切替だと「最近になって型が変わった」という一番の情報が見えないため。
- 円環は一致度ベースに変更（塗りの濃さ＝一致度／中央＝最も近い型と点数／マーカー＝一致度1位）。
  グループの平均騰落率は右カードに残した（別の指標として併記）。

🔵 **この日の実測**＝3か月では 金融相場67.3 / 逆業績63.5 / 逆金融47.6 / 業績27.3 だが、
**1か月では逆金融が83.3で突出**（3か月26.9→の金融相場は最下位に転落）。
＝**直近で逆金融相場の型に切り替わりつつある**、という並び。
逆金融の内訳は 銀行1位=支持／商社13位・エネルギー14位=矛盾 で、点数の割れ方まで追える。
🔴 これは型との一致度であって、局面の判定でも予測でもない。

- 検証: **vitest 245 pass（+9）**／tsc(app) 0／lint 0。実機で確認済み。

### 2026-08-07(2): セクター画面を**実データ**にした（銘柄マスタ＋業種別の相対強弱）✅（`fa01dd4` で push 済・Vercel 自動デプロイ）

ダミーで作った画面の中身を本物に差し替えた。狙いは
**「教科書上どの業種が強いとされるか」と「実際にどの業種が強いか」を同じ円の上で重ねる**こと。

**取得（`scripts/fetch-jpx.mjs` に2本追加・既存の try/catch＋Okフラグの作法どおり）**
- `fetchStockMaster()` → `public/data/stock_master.json`
  JPX「東証上場銘柄一覧」`data_j.xls`（月1更新・無料）から**内国株式のみ 3,713銘柄**。
  🔵 リンクは一覧ページから拾い、取れなければ既知URLへフォールバック。
  🔵 17業種は**コードだけ**持つ（3700行に文字列を持たせない）。pretty-print もしない。
  🔴 ETF/REIT/外国株/PRO Market は業種が `-` なので落とす。
- `fetchSectorPerfData()` → `public/data/sector_perf.json`
  **業種別株価指数そのものは有料**なので、TOPIX-17 に連動する
  **NEXT FUNDS 業種別ETF（1617〜1633）**の Yahoo 日足で代用。1M/3M/6M の騰落率と順位。
  🔴 **調整後終値(adjclose)で計算**。素の終値だと分配金の権利落ち（1回0.7〜2%）が
  業種ごとにバラバラの日付で乗り、**順位が入れ替わる**（実測で 鉄鋼・非鉄 が -19.85%→-19.12% と動いた）。
  🔴 ETFコードと17業種コードは **1617+(n-1)** の一対一。`data_j.xls` のETF名で検証し、テストで固定。
  🔵 出力に `proxy: 'etf'` を残し、画面にも「指数そのものではない」と書く。

**画面（`src/utils/sectorRotation.ts` / `sectorData.ts` / `sectorStockPrompt.ts` / `SectorPanel.tsx`）**
- ダミー（`DUMMY_NOW` / `DUMMY_STOCKS`）は全廃。分類の単位を **TOPIX-17** に統一し、
  銘柄 → 17業種 → 局面 が一本で繋がるようにした。**17業種すべてをどれか1局面に割り当て**（テストで固定）。
- 円環は「4分割＝教科書の対応表」「色の濃さと数字＝実測」。
  🔴 **局面の判定はしない**。マーカーは「実測で平均が最も強いグループ」であって「いまが◯◯相場」ではない。
  文言・凡例・AIプロンプトの3か所すべてでそこを崩さないこと。
- 期間切替（1か月／3か月／6か月）。
- 🔴 **銘柄マスタは localStorage に入れない**（3700件＝数百KB。他のキャッシュを QuotaExceeded で追い出す）。
  モジュール変数で使い回し、**検索を使い始めたときに初めて**取得する。`sector_perf` だけ `fetchWithCache`。
- 🔴 **検索は関連度順**。単純な部分一致だと「銀行」で17業種名「金融（除く銀行）」が
  169件先に出て銀行株に届かなかった（実機で発生）。業種名の**前方一致**を最優先にして解消。
  結果は40件で打ち切り、総ヒット数を別に出す。
- AIプロンプトに業種の実測を同梱。🔴 売買推奨・目標株価に加えて**「いまの局面の断定」も禁止**する文言を追加。

**その他**
- `check-freshness.mjs` に `sector_perf.json`（許容5日）を追加。
  🔴 `stock_master.json` は**対象外**（月次更新かつ日付がトップレベル `asOf` で `latestDateIn` が拾えない）。
- `fetch-data.yml` は `git add public/data/` なので新ファイルも自動で乗る（変更不要）。
- 検証: **vitest 236 pass**／tsc(app) 0／lint 0／`npm run check-freshness` 全16ファイル最新。
  実機で 円環・期間切替・「銀行」検索・銘柄選択・AI分析パネルまで確認済み。

🔵 **実測の所見（2026-08-07・3か月）**＝銀行 +20.0% が17業種で首位、金融(除く銀行) +14.9%、
情報通信 +13.8%。グループ平均では金融相場グループ +6.46% が1位、業績相場 −2.93% が最下位。
🔴 これは**測った結果**であって局面の判定ではない。

### 2026-08-07: 反転モニターを削除／シールドの「イベント予想」を「セクター」に置き換えた ✅（`fa01dd4` で push 済・Vercel 自動デプロイ）

ユーザー指示は3つ。①研究室の反転モニターを削除 ②シールドの「イベント予想」タブごと削除
③その位置に「セクター」タブを追加（円環UI＋銘柄検索）。

> 🔵 **③のダミー部分はこの後すぐ上の「2026-08-07(2)」で実データに差し替え済み**。
> 以下の `DUMMY_NOW` / `nowAngle` / 「ダミー20銘柄」の記述は**この時点の記録**で、現在のコードには無い。
> 同じコミット `fa01dd4` に両方入っている。

- **① 反転モニターの削除**＝`SupportView.tsx` から `ReversalWatchCard` の描画を外し、
  `src/components/ReversalWatchCard.tsx` を削除。
  🔵 **`src/utils/reversalWatch.ts` とそのテスト、`scripts/check-reversal-watch.ts` は残した**。
  画面から消しただけで、コマンドラインの検証ツール側はまだ使えるようにしてある。
- **② 「イベント予想」タブの削除**＝`ShieldView.tsx` から `NewsPanel`（約160行）と
  `NEWS_STATUS_LINES` / `EVENT_MEMO_CONFIG` / `handleNewsCopy` / `newsLogState` を撤去。
  `buildNewsPrompt` の import も外した（`buildUpcomingEventsText` は継続利用）。
- **③ セクタータブの新設**（`SHIELD_TABS = ['shield','sector']`・App.tsx で状態を持つ）:
  - `src/utils/sectorRotation.ts`＝景気4局面（金融→業績→逆金融→逆業績）を角度0/90/180/270で持ち、
    `phaseAt` / `nextPhase` / `nowAngle` / `searchStocks` を提供。
    🔴 **局面判定も銘柄マスタ（20件）もダミー**。画面にもプロンプトにも「ダミー」と明記している。
  - `src/components/SectorPanel.tsx`＝左に円環（サイバー調・回転スイープ／「いまここ」の脈動マーカー／
    セグメントのホバー＆選択で外へ伸びる）、右に銘柄検索とAI分析。左右とも**上下中央寄せ**。
    右ペインは **maxWidth 520** で止めている（付けないと検索欄だけ極端に横長になる）。
  - `src/utils/sectorStockPrompt.ts`＝選んだ銘柄をAIに投げるプロンプト。
    🔴 **売買の推奨・目標株価を書かせない**（アプリ全体の方針）／
    🔴 **ぽいロボは株価を持っていない**ので「AI側が調べ、出典と取得日を明記する」と書かせる／
    🔴 現在地がダミーである旨も渡す（渡さないとAIが確定事実として相場観を語る）。
  - シールドと同じ「プロンプトをコピー → `AILaunchRow` からAIを開く」方式。
- **テスト** `src/utils/__tests__/sectorRotation.test.ts` を新設（18本）。
  角度↔局面の対応・境界・負角度・検索の当たり方（数字は前方一致／名前とセクターは部分一致）と、
  プロンプトの禁止事項（推奨を書かせない・ダミーである旨・株価非保有）を固定した。
- 検証: **vitest 224 pass（+18）**／tsc(app) 0／lint 0。実画面（dev）で円環・検索・AI分析パネルまで確認済み。
- ⚠ `TEMP_PUBLIC_ALL_PAGES`（App.tsx）はシールドが会員限定のため確認用に一時的に true にしたが、
  **false に戻してある**。commit 前に必ず false であることを確認すること。

### 2026-08-06: 鮮度チェックの「許容日数」が短すぎて誤警報していたのを直した ✅（`34dadb9` で push 済）

**きっかけ**＝7/31の日米協調介入のあと「外国人が買っているのか」を見ようとしたら、
`investor.json` が **7/24 で13日前**・鮮度チェックが 🔴 を出していた。取得の詰まりを疑って追ったが、**取得は壊れていなかった**。

- **JPX のアーカイブを直接確認**（HTTP 200・リンク29件）。最新は **2026年7月第4週(7/21〜7/24)** で、
  **7月第5週(7/27〜7/31) はまだ公表されていない**。＝ローカルが古いのではなく**公表前**。
- 🔴 **本当の原因＝閾値の設定ミス**。週次データの鮮度は
  **①行間隔（週末→次の週末＝7日）＋②公表ラグ**が**積み上がる**のに、②を足し忘れて一律12日で置いていた。
  ②を **git 履歴から実測**（最新行が更新されたコミット日 − その行の日付）:
  | ファイル | 実測の公表ラグ | 旧許容 | 新許容 |
  |---|---|---|---|
  | `investor.json` | **7〜8日** | 12 | **15** |
  | `margin.json` | 5〜7日 | 12 | **15** |
  | `arbitrage.json` / `arbitrage_daily.json` | 5〜6日 | 12 | **14** |
  ＝ investor は**毎週、次の週が公表される前に必ず鳴る**設定だった（今回まさにそれ）。
- **年末年始スラックを新設**（`holidaySlack()`）。12/31〜1/3 の休場で**全ファイルの行間隔が構造的に伸びる**
  （実測 52週分：margin **14日**／investor・裁定残・騰落レシオ・空売り比率 **10日**・いずれも 2026-01-09 の行）。
  🔵 通年で広げると検知力を捨てるので、**12/26〜1/20 だけ +7日**。月次公表もの（許容30日以上＝`futures_oi`）は対象外。
- `check-freshness.mjs` を **import 可能に**（直接実行時のみ main を走らせる）。従来は import すると `process.exit(0)` でテストが落ちた。
- **再発防止テスト** `src/utils/__tests__/freshness-threshold.test.ts` を新設（12本）。
  閾値が12日以下に戻ったら落ちる／スラックの期間境界／並び順非依存を固定。
- 検証: **vitest 206 pass（+12）**／tsc(app) 0／lint 0／`npm run check-freshness` → **全15ファイル最新（警告ゼロ）**。

🔴 **この日の相場側の含意**＝介入後の外国人フローは**まだ誰も見られない**（JPXの公表待ち・7/27〜7/31週は 8/6 以降）。
需給の話をするときは「信用残＝国内個人・7/31まで」と「投資主体別＝7/24まで」で**基準日が違う**ことに注意。

### 2026-08-03: 為替介入・日銀の「結果」を実績として貯め始めた ✅

**きっかけ**＝7/31に**日米協調介入**（15年ぶり・円買いでは28年ぶり）。8/3に片山財務相が談話で公表。
この局面を後から検証したくても、`macroCalendar.ts` は**予定日しか持っておらず**、
「日銀が動いたのか据え置いたのか」「介入は単独か協調か」が残らないため**一件ずつ調べ直す**しかなかった。

- **`MacroEventDetail`（`headline` / `note`）を追加**し、`MacroEvent` に `detail?` を持たせた。
  🔵 任意フィールドなので既存の消費側（`MacroEventBadge` / `ClockWidget` / `engineExport`）は壊れない。
  バッジは「日銀｜据え置き」のように併記し、ポップアップは結果を説明文より**先に**出す。
- **`intervention` を新設**。🔴 **介入は事前に日程が公表されない**ので予定日リストを持たず、
  **実績のある日にだけ**出る。単独／協調まで区別して持つ。
- **記録済み**＝2022年3回（9/22・10/21・10/24／単独・合計約9.2兆円）、2024年4回（4/29・5/2・7/11・7/12／単独）、
  **2026年2回（7/30 単独・7/31 日米協調）**。日銀は **2026-07-31＝据え置き**を記録。
- 🔴 **確認できた出典があるものだけ入れる**（推測で埋めない）。日銀の `detail` 空欄＝**未記録**であって「据え置き」ではない。
- `getInterventionDates()` を公開＝R&Dスクリプトから「介入の翌日以降どうなったか」を測れる。
- 検証: lint 0 ／ vitest **194 pass**（+6）／ tsc(app) 0。

**この日の実測でわかったこと（判断の材料）**
| 材料 | 結果 |
|---|---|
| 週足20週線タッチ（日経・21年・n=48） | **浅い接触(0〜-2%)の方が分が悪い**＝12週後 -0.6%・プラス率49%・**安値割れ78%**。深い(-2%以下)は12週後+4.6%・プラス率82%・安値割れ36% |
| 反転臨界モニター（8/3時点） | **2/8点灯**（信用買い残の整理・日足モメンタムの鈍化）。価格構造・騰落レシオ・NT倍率は**未転換** |
| 需給（7/24〜7/30） | 信用買残 6.73→6.48兆・裁定買残 2.52→2.24兆・評価損益率 -10.45→-8.24% ＝**土台は改善**。外国人は7/24週に買越転換 |
| 騰落レシオ | **126.10（過熱圏）**。日経が下げているのに高い＝広範な売りではなく**セクターローテーションの継続** |

🔴 **需給データは7/24〜7/30が最新で、7/31の介入以降は反映されていない**（週次のため）。
🔴 **介入は金利差を動かさない**。7/31は日銀据え置きと同日＝「介入だけ」の局面。次の分岐は**2026-09-18の日銀会合**。

### 2026-08-03: 反転臨界モニターをコマンドで確認できるようにした ✅

`scripts/check-reversal-watch.ts`（`npx tsx scripts/check-reversal-watch.ts`）。
モニターは研究室（管理者専用・PC限定）にしか出ないため、確認のたびにアプリを立ち上げてログインする必要があった。

- 🔴 **7/28からの持ち越し「日足モメンタムが動くか」は解決**＝`ntRatio` を日経終値（Yahoo）から再構成すれば
  MACDが計算でき、「データ不足」にならないことを確認（実測 MACDヒスト -418）。
- 🔴 **NT倍率は `topix.json` 依存なので、topix が古いとNTも古くなる**（現在 7/28 で止まっている・下記）。

### ✅ topix のCI取得は直った（7/31のフォールバックが実測で効いた）

**7/28からの持ち越し課題「CIで topix が取れない」は解決した。**

作業中は topix.json が 7/28 で止まっており「フォールバックは未検証」と書いていたが、
それは**ローカルが古かっただけ**だった。`git pull` したところ CI のデータ更新コミットが3本
（7/31×2・8/1）入っており、**topix.json も 7/31 まで更新されていた**。

- `npm run check-freshness` → **全15ファイルが最新**（警告ゼロ）。
- 🔴 **教訓＝鮮度を見るときは先に `git pull` する**。ローカルの public/data は CI が更新するので、
  pull しないと「データが止まっている」と誤診する（今回まさに誤診しかけた）。
- これで「次にやること」4① は消化。残るのは 4②（J-Quants への移行検討）だけ。

### 2026-07-31: データが「静かに古くなる」問題に対処 ✅

**背景**: 7/28 の引継ぎに残っていた🔴「CIで topix 取得が失敗する原因は未特定」を追った結果、
**`topix.json` は 2026-07-13 を最後に CI で一度も更新できていなかった**（7/28 の復旧は人手）。
2週間気づかなかった理由は、**鮮度チェックが `futures_daily` と `investor` の2ファイルしか見ていなかった**ため。

- **原因**: kabutan は **GitHub Actions の IP からは取れない**（ローカルからは200で取れるので気づきにくい）。
  `fetch-jpx.mjs` は topix 失敗を catch して警告だけ出すため CI は success のまま＝**静かに古くなる**。
- **対処①（取得）**: `buildTopixData()` に**自前の Vercel API へのフォールバック**を追加（`fetchTopixFromOwnApi`）。
  `/api/stocks-daily?only=topix` は**同じ kabutan を Vercel の egress IP で叩く**ので通る＝CIから見れば自分のAPIを読むだけ。
  🔴 **両経路を実測で検証済み**（直取得＝300件→252件／kabutanを落とした状態でフォールバック＝252件・最新2026-07-31）。
- **対処②（検知）**: **`scripts/check-freshness.mjs` を新設**し、**全15ファイル**の鮮度を見るようにした（`npm run check-freshness`）。
  ワークフローのインライン2ファイルチェックは削除してこのスクリプト呼び出しに置換（ローカルとCIで判定が同じになる）。
  🔴 **並び順に依存しない**（全行走査して最大日付を採る）＝「vix_daily と ntRatio だけ古い順」のような取り違えで壊れない。
- **副産物**: 新チェックが2件の「古い」を検出したが、**どちらも本物の停止ではなく許容日数の設定ミス**だった。
  公表サイクルを調べて `EXPECT` に根拠つきで記録した：
  | ファイル | 実際のサイクル | 許容 |
  |---|---|---|
  | `arbitrage_daily.json` | **名前は daily だが中身は週次**（7/24, 7/17, 7/10…） | 12日 |
  | `futures_oi.json` | JPX **月次統計**（翌月上旬公表）＝月末は1ヶ月遅れが正常 | 45日 |
  🔵 オオカミ少年にしないことがこの仕組みの生命線。**データを足したら `EXPECT` にも足す**（載っていない＝無検査）。
- **検証**: lint 0 ／ vitest **188 pass** ／ tsc(app) 0。

🔴 **残**: kabutan が Actions から弾かれること自体は変わっていない（フォールバックで回避しているだけ）。
恒久策は **J-Quants（無料登録・JPX公式でTOPIX有）** への移行（メモリ `reference_stock_calendar_data_apis`）。

### 2026-07-31: 相場局面の実測スクリプトを3本追加（R&D）

日経が高値から-14%下げた後の急反発局面（日銀会合・PCE・キオクシア決算が重なる日）の判断材料として、
**「この形は過去どうなったか」を21年の実データで測る**スクリプトを作った。いずれも Yahoo から取得して集計するだけで、アプリ本体には影響しない。

| スクリプト | 何が分かるか | 主な結果 |
|---|---|---|
| `analyze-deadcat-vs-momentum.mjs` | 25日線から-7%以上離れた安値からの急反発の行方 | 日経n=20：安値割れ50%／②回復維持40%。**浅い下落(-7〜-10%)ほど①が優勢(58%)**。5日後に下げている割合75%だが**20日後は42%**＝一度下を試して戻すW底型 |
| `analyze-weekly-ma20-touch.mjs` | 週足20週線を割って終値で回復した週の行方 | 日経n=48。🔴 **深く割り込んだ方(-2%以下)が安値割れ36%・12週後+4.6%(プラス率82%)** と良好。浅い接触は安値割れ78% |
| `analyze-boj-days.mjs` | 日銀会合後の日経（会合日は `macroCalendar.ts` から読む） | n=20（2024年以降）。下落局面で迎えた6回は当日+1.0%。🔴 **乖離≤-5%で迎えた前例は0件**＝会合日リストが2024年以降しかなく標本が足りない |

🔵 **`analyze-boj-days.mjs` は会合日を `src/utils/macroCalendar.ts` から正規表現で読む**（日付を二重管理しない）。
🔴 そのため **BOJ/PCE の日程が2024年以降しか無く、2026年は「推定」**という制約がそのまま効く。
過去に遡って検証したいなら日程の拡充が要る（年次更新の手順書＝`docs/maintenance/calendar-dates-check.md`）。

### 2026-07-28: 反転臨界モニター（🔒管理者専用）を追加＋topix.json の更新停止を復旧 ✅本番反映

**背景**: 日経が高値73,000から-14.8%下げる局面で、需給・テクニカルを毎回手作業で調べていた。判断の材料を毎朝同じ形で見られるようにした。

- **新規 `src/utils/reversalWatch.ts`（純粋関数・テスト17本）**
  「いまのトレンドと**逆方向**への転換」が何個近づいているかを8条件で観測する。トレンドの向き（25日線乖離）で**条件セットが対称に反転**する。
  信用買い残／空売り比率／VIX／海外投資家／日足モメンタム(MACDヒスト)／価格構造／**NT倍率**／**騰落レシオ**。
- **新規 `src/components/ReversalWatchCard.tsx`** … 研究室（SupportView）の**右上**に常時表示。`isAdmin && !isMobile` のときだけ描画。
- 🔴 **売買の指示は出さない**（第19セッションの「客観的状態記述型」方針を踏襲）。点灯数と観測値だけを出す。会員には見えない内部R&Dツール。
- **`public/data/topix.json` を7/13→7/28に復旧**。
  🔴 **パーサは壊れていない**（ローカルからは kabutan で HTTP 200・300件取得できる）。`fetch-jpx.mjs` は topix 失敗を `catch` して警告のみ＝**既存ファイルを残して静かに古くなる**。`investor`/`margin` の失敗時しか `exit(1)` しないため CI は success のまま。**CI側で失敗している原因は未特定**（データセンターIP遮断の疑い）。

**実データで見つかった3つの取り違え（修正済み・再発防止テストあり）**
| 件 | 症状 | 原因 |
|---|---|---|
| 並び順 | VIXが3月の値を「最新」として表示 | **`vix_daily` と `ntRatio` だけ古い順**。他（nkFuturesPrice/margin/investor/shortSell/advanceDecline）は新しい順 |
| 単位 | 海外投資家が1/100で表示 | **`investor` は億円**（`senToOku` 済）、**`margin` は百万円**で混在 |
| 本数不足 | 日足MACDが「データ不足」 | `nk_futures_price.json` は**十数本しかない**。MACD(12,26,9)には35本要る → **日経終値の長期系列（ntRatio）から計算**へ変更 |

**🔴 残っている確認**
- **日足モメンタムは実画面でしか検証できない**（ntRatio は実行時APIのため、オフラインJSONでは長期系列を再現できない）。研究室を開いて「データ不足」でないことを確認すること
- CIで topix 取得が失敗する原因の特定（上記）

**この日の相場分析でわかったこと（モニターの設計根拠）**
- 日経の下落は**AI・半導体に集中**。7/10比で日経 -9.05% に対し **TOPIX は -1.62%**、**NT倍率 16.99→15.71（-7.5%）**
- **騰落レシオは 97.75(6/26・日経天井) → 123.49(7/27) と上昇**＝資金は逃げておらず**移動している**（セクターローテーション）
- 信用買い残はピーク70,167億から**-4.4%しか整理が進んでいない**／評価損益率 **-10.45%＝過去52週で唯一の-10%割れ**／空売り比率は低下＝**踏み上げ燃料が薄い**

### ✅ 2026-07-26: nikkei225jp.com 由来データが7/23から全滅 → **2026-07-28 時点で復旧を確認**

> 🟢 **2026-07-28 追記**：下記の断絶は**解消していた**（7/27のデータ更新コミット `47974e9` / `bb5bae3` 以降）。実データで確認：
> `margin.json` の **evalRatio は全52週すべて値あり・null ゼロ**（最新 7/17 = -10.45）／`advance_decline` `short_sell` はいずれも **7/27まで**取得／`futures_daily` の pcr も 7/24 = 2.795 で復活。
> **A〜Cの改修は不要になった**（データ元が復旧したためと見られる）。ただし下記の🔴「静かに壊れる設計」の問題は**そのまま残っている**（今回 topix.json が7/13で止まっていたのも同じ構造）。**取得0件が続いたら失敗扱いにする／通知する仕組み**は依然として入れる価値がある。以下は当時の調査記録。

<details><summary>当時の調査記録（2026-07-26）</summary>
- **発端**: ユーザー指摘「信用倍率テーブルの**信用評価損益率**が全部 `—`」。
- **調査結果＝データ元 nikkei225jp.com が 2026-07-23 に2点同時変更**:
  1. **データパス変更**: `/_data/_nfsWEB/DAY/*.json` → `/_data/_nfsDATA/DAY/*.json`（旧パスは404）。
  2. **Cloudflare のBotチャレンジ導入**: スクリプトからの直接 fetch は URL/UA/Referer を問わず **403（`cf-mitigated: challenge`）**。トップページすら403＝JS実行なしでは通過不可。ローカル(住宅IP)で実測。CIも同時期に停止しているため Actions も同様と推定。
- **影響（`public/data/` 実データで確認）**:
  | 指標 | ファイル | 状態 |
  |---|---|---|
  | 信用評価損益率 | `margin.json` `evalRatio` | 7/23以降 **全52週 null**（直前の `a99bb20` までは52件あり／破断は `67130ed`) |
  | 騰落レシオ | `advance_decline.json` | **7/22で停止** |
  | 空売り比率 | `short_sell.json` | **7/22で停止** |
  | PCR | `futures_daily.json` `pcr` | 7/23以降 **null** |
  | 信用残・投資主体別・建玉・先物価格 | JPX由来 | **正常** |
- 🔴 **静かに壊れる設計が問題**: `fetch-jpx.mjs` は取得失敗を `catch`→`evalRatio=null` で握りつぶすためCIは success のまま。**取得0件が続いたら失敗扱いにする/通知する**仕組みを併せて入れたい。
- **復旧の実証済みルート**: `https://r.jina.ai/<URL>` リーダー経由ならCF通過し新パスで取得可。`dailyweek2.json?495841` から `2026/07/17 col7=-10.45` を実取得（サイト表示と一致）。
  - 🔴 クエリ版数トークン（`?495841`）が**必須**（無し/別値は404）。値はページHTMLの `<script src>` に載るので、`sinyou.php` をリーダー経由で取ってトークンを抽出→JSON取得、の2段が要る。
  - 🔴 `daily2year.json`（騰落レシオ/空売り比率/PCR）は**新パスでも404**＝別ファイルに移った可能性。取得先の再特定が未了。
- **提示した選択肢（ユーザー未決・別プロジェクトへ移動）**:
  - **A 応急**: 新パス＋jinaリーダー フォールバックに改修（評価損益率は即復活・外部リーダー依存が増える）。
  - **B 本筋**: データ源乗換（空売り比率=JPX公式が日次公表／PCRはJPX建玉から自前計算／評価損益率・騰落レシオのみ代替源を探す）。
  - **C 恒久**: J-Quants（無料登録・JPX公式API）へ移行。メモリ `reference_stock_calendar_data_apis` 参照。
  - 推奨は **A で止血 → B**。

</details>

### 2026-07-07: 先物「建玉残高・取引高・PCR」が7/3で止まっていた件を復旧 ✅本番反映（`f112fc6`）
- **事象**: エンジン>環境 の `futures_daily.json`（建玉残高/取引高/PCR）が **7/3で停止**（`updatedAt`は7/6だが最新行は7/3）。日経先物価格チャートは7/6現在で出ていた＝**JPX日報PDF系だけ遅延**。
- **原因**: **JPX一覧JSON `daily_report_202607.json` のCDNエッジstaleキャッシュ**。7/6のZIP/PDF実体は公開済み（07:31 UTC）だったのに、7/6午後のワークフロー実行(14:07/16:02 UTC)時点で一覧JSONがまだ7/6を含んでいなかった。**コード・パーサー・データ破損なし**（当該7/6 PDFを手元でパース成功＝OI177,696/取引高27,715を実測）。
- **対応**: ローカルを origin に同期(ff)→ `npm run fetch-data` で7/6をバックフィル（`✓ 2026/07/06 vol=27,715 oi=177,696 close=70,520 pcr=2.83`）。全15データJSONが最新化・件数欠損なしを検証→commit&push→Vercel自動デプロイ→**本番 `/calendar/data/futures_daily.json` が newest=2026/07/06 を配信確認**。
- 🔴 **再発防止（未実施・候補）**: JPX一覧JSON取得(`fetch-jpx.mjs` L1211付近)に**キャッシュバスター**(`?_=<ts>`)を付けるとエッジのstale掴みを軽減できる。今回はユーザー選択でデータ再取得のみ実施。次回staleが再発するなら実装する。次回定期実行(平日19:30 JST)以降は一覧が更新済みなので自動で7/6以降も入る見込み。

### 第29 (2026-06-18): 環境タブ NS倍率 → **NT倍率（日経÷TOPIX）** へ変更 ✅本番反映済（`a8c7809` ほか）
- **概要**: 環境タブの倍率指標を NS倍率（日経÷S&P500）から **NT倍率（日経÷TOPIX）** に変更。ファイル名は元から `NtRatio*` だったが中身がNSのままだった配線を完成させた。
- **データソース（重要・難所だった）**:
  - 日経=Yahoo `^N225`（ライブ・既存プロキシ）。
  - 🔴 **TOPIX指数は無料で取りにくい**＝`^TPX` はYahoo欠損、`stooq ^tpx` は residential/公開プロキシ/**GitHub Actions/Vercel すべてでJSチャレンジによりブロック**（topix.jsonがgit履歴に一度も無いのが証拠）。ETF(1306.T等)は指数と水準ズレで不採用。
  - ✅ **解決＝kabutan の TOPIX時系列をスクレイプ**（`code=0010`・`/stock/kabuka`）。**Vercel egress IP からは200で取得可**を本番で実測確認。
  - 配線＝フロントは **①`/api/stocks-daily?only=topix`**（Vercel側でkabutan・🔴Hobby12 Functions上限のため専用ルートは作らず stocks-daily に相乗り・重いスクレイプ前に短絡）→ **②静的 `/calendar/data/topix.json`**（`fetch-jpx.mjs buildTopixData` がkabutanで生成・90日シード済）の順にフォールバック。
- **しきい値**: NT倍率の水準ラベル（酷暑/真夏日/適温/冷え込みの温度メタファー）を**実測レンジに較正**。本環境の実NT倍率は **≈17.5**（Nikkei71,053÷TOPIX4,068。直近90日 min14.5/中央15.2/max17.5）。`≥16.5酷暑 / ≥15.5真夏日 / ≥14.8適温 / <14.8冷え込み`（現状=酷暑＝値がさ過熱）。※ユーザー選択は「直近レンジ基準」（real world前提の13〜15）だったが、本環境のNikkei高値圏では常時"酷暑"固定になるため、intentを汲んで実データ四分位へ較正した。
- **エンジン非影響**: `engineExport.ts` は `NtRatioPoint.nikkei` のみ参照（ratio/benchmark不使用）＝TEV/エンジン計算に影響なし。需給プロンプト(`enginePrompt.ts`)/EvalsにもNS/NT倍率の参照は無く改修不要（「倍率」の語は全て『信用倍率』）。
- **変更ファイル**: `ntRatioData.ts`(NT化+APIフォールバック) / `NtRatioPanel.tsx`(ラベル/配色/TOPIX表示) / `QuantView.tsx`(見出し) / `api/stocks-daily.js`(?only=topix+kabutan) / `scripts/fetch-jpx.mjs`(buildTopixData=kabutan) / `public/data/topix.json`(seed) / SpecView/ManualView/VixPanel/LockSkeletons/requirements/dataCache(キー `poical-nt-ratio-v1`)。
- **本番検証済**: `/api/stocks-daily?only=topix`=200(30件・TOPIX4068.18) / 静的=200(90件) / tsc=0 / lint=0 / vitest 169 pass / build=0 / Vercel READY(alias済)。
- 🔴 **残注意**: ①kabutanはVercelからは可だが**CI(Actions)からは未確認**＝静的topix.jsonがCIで更新できない可能性（その場合も①APIが主なので表示は維持）。②kabutanは~30〜90日のみで旧NSの1年チャートより短い。③より堅牢にするなら **J-Quants(無料登録・JPX公式でTOPIX有)** へ移行が本筋（メモリ`reference_stock_calendar_data_apis`参照）。
- **追補①（同日）**: デプロイ直後にNT倍率が一瞬出なかったのは **CDNエッジ/PWAキャッシュの伝播ラグ**（コード/データは正常・伝播完了で表示）。※ローカル(Win)とVercel(Linux)でビルドハッシュが異なる点に注意＝バンドル確認は実チャンクを辿ること。
- **追補②（同日 `4dbb382`）**: 🔴 **スマホで VIX / NT倍率 チャートが非表示**だった問題を修正。両パネルは lightweight-charts 描画でチャート領域が `flex:1;minHeight:0`→高さ未指定の `panelMobile` 内で**高さ0に潰れていた**（建玉残高等はテーブルで自然高さなので表示できていた）。**モバイル時はチャート領域を固定高さ200pxに**＋`NtRatioPanel` に `isMobile` を配線。本番チャンクで `height:200` 反映確認済。
- **追補③（同日 `af54a42`）**: NT倍率チャートの**期間を約1年に延長**。kabutanは1ページ≒30営業日・**10ページで全履歴(~300営業日)**に到達（11ページ目は新規無し）。`api/stocks-daily fetchTopix` を10ページ並列取得→直近252日に、`buildTopixData` も1→10ページに拡張、`topix.json` を252日(2025-06-06〜)で再シード。本番API/静的とも252件を確認。

### 第28 (2026-06-18): 一時的に全ページ公開 → 同日撤回（`TEMP_PUBLIC_ALL_PAGES=false`・`src/App.tsx`冒頭・再ONで一時公開可）。コネクト/通知は据え置きでメンバー限定。検証 tsc/lint/test=169pass。

### 第27 (2026-06-15): 需給推進力(TEV)=null の真因対応 ✅push済 `c5e58c1`
**事象**: 日曜にエンジン出力で「需給推進力：計算不能（データ不足）」となり、AIが「最新の信用データが無いからnull」と**誤った理由を創作**していた。
**真因（調査結果）**: TEV の V/A は**日次モメンタム（USD/JPY 30%・NAS100 25%・VIX 20%・先物OI 15%）のZスコア**から算出され、信用残など週次データとは無関係。日次ローダー（`usdjpyData`/`nas100Data`/`vixData`）は静的JSONが**12時間より古いと不安定な公開CORSプロキシ（allorigins/codetabs）へライブ切替**する。週末はfetchワークフロー（平日のみ）が止まり静的JSONが必ず>12h→日次3系列が一斉にライブ依存→プロキシのレート制限/薄い週末データで系列が短縮・空→`score_today/score_3d`がnull→**TEV null**。週次データは12h制限が無く読めるため「信用残は豊富だがTEVだけnull」の非対称が混乱の元。
**対応4点**:
1. **真因の明示**（`engineExport.ts`）: tev null時に `tev_value!==null` ガードの外で `sanity_warnings` に「どの日次系列が何本か＋信用残とは無関係」を必ず積む。AIの創作を封じる。
2. **加速度の頑健化**（`engineExport.ts`）: 日次履歴<6本で `score_3d` が無い時、オフセット2→1で加速度を近似（`tev_A_approx`）。「Vは出るがAが無くTEVだけnull」を緩和。近似時は警告を積み `tev_for_execution` を保留に。
3. **プロンプト**（`enginePrompt.ts`）: null理由は必ず `sanity_warnings` から引用し、信用データ等を理由に創作禁止と明記。**EvalsPanel** のScenario3を真因入りに更新＋「信用起因と創作していないか」採点を追加。
4. **根本修正**（`usdjpyData`/`nas100Data`/`vixData`）: **市場休場日（週末/祝日）は静的JSONの最終終値が当日値**なのでライブ切替しない（休場時の許容を最大4日に）。これで週末のライブ一斉依存が消える。
**検証**: tsc(app)=0 / lint=0 / vitest **169 pass**（engineExport に回帰テスト2件追加）。

---

直近の過去作業はすべて push 済み（Vercel Git 連携で自動デプロイ）。

- `e52382b` プレイブックのスワイプ改善・文言調整＋CSPで data: フォント許可（第26節）
- `7a6002e` 時計フォント再サブセット（22.75→1.5KB）＋説明書/プレイブックの文言平易化
- `2701880` ロック画面のフッターをサイバー調に＋ログイン導線を明確化

### プレイブックのスワイプ改善・CSP（`e52382b`）
- 🔴 **スワイプでスライドが飛ぶ問題を是正**: コンテンツがはみ出すスライドで下スクロールすると次スライドに切り替わってしまっていた。`StrategyPlaybookPanel` のタッチ判定を、開始時に内部スクロールの上端/下端到達を記録し「下端で上スワイプ＝次へ／上端で下スワイプ＝前へ」のときだけ切替に変更（途中はスライド内スクロール優先）。`trackRef`/`touchAtTop`/`touchAtBottom`。
- カード文言: トレンドフィルターの `gradeLabel`「（一番だいじ）」と `plain` 末尾「これで大きな暴落をよけられます。」を削除。
- 🔴 **CSP**: 第三者ウィジェット（TradingView 等）が main document に注入する `data:` インラインフォントが `font-src 'self'` でブロックされコンソールエラーが出ていたため、`vercel.json` の CSP を `font-src 'self' data:` に緩和。**CSP は Vercel レスポンスヘッダーのため本番デプロイで初めて反映**。自前フォントは引き続き自己ホスト。

### 表示速度・文言（`7a6002e`）
- 時計フォント Cherry Bomb One を `0123456789:` の11グリフのみに `pyftsubset` で再サブセット（22.75KB→1.5KB、約93%減）。preload されるレンダリングブロッキング資源を削減。元フォントから再サブセットする手順は `src/index.css` コメント参照。
- 説明書（ManualView）: チャート「米国債(10年利回り)」→ 実シンボル `NASDAQ:TLT` に合わせ「米国債(米国長期国債ETF)」へ訂正。他は全文精査し誤りなし。
- 戦略プレイブック: 見出し「安全に回すコツ」→「リスクを抑えるには？」、「勝てる4つの理由」→「期待値が高い4つの作戦」。トグル「くわしい数字でみる」→「過去20年のバックテスト」。カード冒頭の説明文と用語集（CAGR/DD/乖離）を削除。カード詳細の専門用語を平易な日本語化（数値は不変）。

### ロック画面のサイバー調フッター＋ログイン導線（`2701880`）
- 非メンバーが会員限定ビュー（カレンダー/エンジン/シールド）を開きロック画面が出ている間、`CalendarHeader` の `forceNeon` でフッターを研究室と同じネオン配色に切替（`useNeon = (isLab || forceNeon) && dark`）。
- ロック画面の案内「右下の『ぽいロボ コネクト』から」は実態（コネクトは研究室にしか無い）と不整合だったため、**未ログイン時のみ「研究室でログイン →」ボタン**（サイバー調）を追加し研究室へ遷移（`onGoToConnect` を quant/shield/calendar の3箇所に配線）。文言も修正。

### 非メンバー初期表示の研究室化（`7a19f1a`）
- 新規セッションのデフォルト着地で非メンバーが会員限定ビュー（`month/week/day/quant/shield`）に来た場合、`CommunityLockScreen` ではなく研究室（`support`）へ遷移。ユーザーが自分で遷移した場合（`sessionStorage` に保存ビューあり）は尊重。
- `App.tsx`: マウント毎に一度だけ・`!authLoading && membershipResolved` 確定後に判定（`wasFreshLoad` + `initialMemberRedirectDone` ref）。
- `useCommunityAccess.ts`: `membershipResolved`（`checkedEmail === user.email`／logout 時 true）を追加。**非同期の会員判定が確定する前のリダイレクト誤発火（レース）を防止**（メンバーを誤って研究室へ飛ばさない）。
- 検証: tsc(app)=0 / lint=0 / vitest 167 pass。

### トレンド整合ゲートの要点（`1a1ec2b`）
- `tevCore.mjs`: `priceTrend` 逆行時のみ `tev_confidence` を最大 **55%** に抑制（`tev_counterTrend` 付与）。**シグナル方向（`tev_value` 符号）は変えない**保守設計＝極限が反転に先行する余地を残す。
- 供給元: backtest=週次close 8週前比 / 本番=`nkRegime`（MA20/60）。
- 背景: 52週検証で期間の日経 +76% の大相場に対しエンジンが逆張り bear を量産し負け（順張り56% vs 逆張り36%）。確信度の「反転」（高確信度ほど実勝率↓）の主因がこれ。第15-16の20年R&D（トレンドフィルター必須）と整合。

---

## ▶ 次にやること（候補）

### 2026-08-16(47) の確認待ち

V1. 🔴 **次の `fetch-data.yml`（平日 19:30 / 21:30 JST）が緑で終わるか**を見る。
   xlsx の取得元を npm → **作者のCDN**に変えたので、`npm ci` がそこで落ちる可能性がある。
   落ちたら `package.json` の `//xlsx` に書いた事情を読んでから対処すること。

V2. 🔵 **予約通知の実挙動を1回だけ通して確かめる**（申請→承認）。
   通知APIに認証を足したので、フロントが idToken を付け損ねていると
   **予約はできるがメールだけ来ない**という出方をする。ログは Vercel の Functions。
   🔵 メール送信は fire-and-forget なので、失敗しても予約自体は成立する。

### 2026-08-11 に持ち越したもの

N1. 🔵 **エンジン画面の左ビジュアル列に何を足すか**（ユーザー検討中）。
   PCのみ 500px の柱を立ててドット絵を置いたが、「さすがに横長すぎる」→ 入れるコンテンツを検討中で保留。
   応急処置として「面の中身に最大幅を設けて中央寄せ」を提案してある。

N2. 🔵 **実機（スマホ）での目視確認が残っている**もの＝周期の2タブ・サイバー調のフッター・
   セクターバナーから飛んだときの黒い覆い・エンジンの「板」撤去。開発環境では確認できていない。

N-1. 🔵 **戦略プレイブックの数字について**（2026-08-11 のやりとり）。
   「A. 現物を長期保有 年+15%」を私が日経平均（配当なし・26年 5.63%）と比べて
   「過大」と指摘したが、**これは誤りだった**。A は個別の高配当株・優良株を寄せ集める話で、
   高配当ETFの**配当込み**実績は 12〜15%（野村日本株高配当70 が13.4年で 14.68%、
   上場インデックス日本高配当が16.2年で 12.13%・配当だけで年3〜4%）。**スライドの数字は妥当**。
   🔴 物差しを間違えると結論が反転する。配当込みか否か、指数か個別か、を必ず確認すること。
   🔵 「あくまで例（イメージ）」と明記されているので、スライドは据え置きで合意。

N0. 🔴 **明日 8/12(水) 15:00 が新運用の初回**。判断の時刻も執行のしかたも変わったので、
   ①15:00に通知が来るか ②「本日の引成で執行」と書かれているか ③翌日の実行で
   その日の終値で約定が記録されるか、を順に確認する。
   🔴 運用側の約束＝**15:25までに引成（MOC）で発注**。

N3. 🔵 **プッシュ通知は 8/13(木) 12:30 に「明日はミニSQです」が届くかで確かめる**。
   🔴 8/11 に「通知が来ない」と相談があったが、**設定は正常で、送るべきイベントが無かっただけ**。
   レーダーの既定は **majorSq / miniSq だけ ON**（`POIROBO_ALERT_CONFIG_DEFAULT`）で、
   CPI・FOMC・日銀・雇用統計は既定 OFF。8月の 8/5 ADP・8/7 雇用統計・8/12 CPI はいずれも対象外だった。
   🔴 通知トグルの表示は **localStorage 由来**なので、サーバー側の登録（`pushSubscriptions/{uid}`）が
   切れていても ON に見える。8/13 に届かなければトークン切れを疑い、通知を OFF→ON する。


### セクター画面（2026-08-07 に新設・保留にした宿題）

S1. 🔵 **ダブルトップなどチャートパターンの有効性を検証していない**。
   ユーザーから「ダブルトップ確定しそう」という読みが出たが、**このアプリで一度も測っていない**ので
   賛成も反対もできなかった。定義（例：1時間足で2つの山が1%以内・間の谷を割ったら成立）を決めて
   過去データで測れば、形の読みに数字を付けられる。`analyze-sector-span.mjs` と同じ作りで書ける。

S2. 🔵 **ドル円の「水準」と業績修正の関係は未検証**。
   日次の相関では為替はほぼ効いていない（日経との相関 −0.05〜0／ナスダックは +0.56）が、
   🔴 **これは日々の値動きの話で、水準の効果を捉えていない**。157円→150円→145円と進むと
   想定為替レート割れ→通期見通しの下方修正、という形で四半期ごとに一気に出る。
   「日次相関が低い＝為替リスクは小さい」と読むのは**誤り**。ここは測り方を変えないと分からない。

S3. 🔵 個別銘柄の株価は**あえて持たない**方針で決着（Yahooから取得可能なことは実測で確認済み）。
   理由＝①コピー先が TradingView なので二重管理 ②数字を持つと正しさの責任が生じる
   ③非公式APIへの依存が増える。ファンダはAIに調べさせる分業のまま。
   🔴 無料で取れるのは**値動きだけ**（PER等は Yahoo quoteSummary で不安定・業績は実質不可）。

S4. ✅ **完了（2026-08-08）**。スマホで中列・右列が潰れていたのを修正（上の 2026-08-08(1) 参照）。
   残っているのは実機（実際のスマホ）での最終確認だけ。390/360/320px の実測では問題なし。

### 既存の宿題

0. ~~反転臨界モニターの日足モメンタム確認~~ → **削除（2026-08-08）**。機能ごと消えている。
   残骸は `src/utils/reversalWatch.ts` / `scripts/check-reversal-watch.ts` / そのテストの3ファイルのみで、
   UI からの参照はゼロ。**この3ファイルを消すかどうかだけ未判断**。
1. エンジン本番プロンプト側の「価格レジーム逆行時は確信度を引き下げ」文言と、数値側ゲートの一貫性を最終確認。
2. 残課題: 高確信度帯（順張り）も n が小さく正の識別力は未確証。勝率を上げる「逆張りシグナルの中立化」は**下落相場のデータが揃うまで保留**（現在は確信度抑制のみで方向は据え置き）。
3. ローンチ前法務（`docs/legal/` の専門家レビュー）— `~/.claude` メモリ「残タスク一覧」参照。
4. NT倍率データの堅牢化: ①✅ **2026-08-03 に確認完了＝CIで topix.json が更新できている**（7/31まで反映・鮮度チェック全15ファイル最新）。7/31のフォールバックが実測で効いた。②長期チャート/恒久安定を狙うなら **J-Quants（無料登録・TOPIX公式）** へ移行検討（残）。
   🔴 **鮮度を見る前に `git pull` すること**。public/data は CI が更新するので、pull しないと「止まっている」と誤診する（2026-08-03 に実際に誤診しかけた）。

---

## ✅ 直近の完了済み（履歴・新しい順）

- **第26 (2026-06-11)**: ①非メンバーの初期表示を会員限定ロック画面（`CommunityLockScreen`）→ 研究室（`support`）に変更 `7a19f1a`（`useCommunityAccess` に `membershipResolved` を追加し非同期会員判定のレースを回避）。②ロック画面のフッターを研究室と同じサイバー調に（`CalendarHeader` `forceNeon`）＋「右下のコネクト」不整合を解消し未ログイン時に「研究室でログイン →」ボタン追加 `2701880`。検証 tsc/lint/test=167pass。push 済み。
- **第25 後半 (2026-06-10)**: トレンド整合ゲート（逆張り確信度を最大55%に抑制・方向は不変）を本番反映 `1a1ec2b`。資料カード「基礎」のタイトルを「レジサポ・移動平均線」に短縮（折返し防止）`8ec8a23`。ファイルベース引継ぎ運用（`CLAUDE.md`/`handover.md`/`requirements.md`）導入 `d11f648`＋本番反映ルーティン「D」をメモリ登録。SpecView 第25節追記。検証 tsc/lint/test=167pass。最新 `aa7d20c` 以降に push 済み。
- **第25 (2026-06-10)**: バックテスト期間を 52週へ拡張（`fetch-jpx` の investor 取得上限 26→55 是正）。確信度キャリブレーション機能追加（10%刻みで予想確率 vs 実勝率）。発見=確信度が自信過剰かつ反転。`tevCore.mjs` で確信度を再キャリブレーション（傾き0.5→0.3・上限95→70・範囲50-70%）。push済 `640c563`。
- **第24**: タイムマシンに逸話/格言の2モード追加（相場格言23個）。push済 `ec31844`。
- **第23**: 管理者メールを `firestore.rules` の `isAdmin()` 1箇所に集約。TEV計算式を `tevCore.mjs` に単一情報源化。PWA SWキャッシュ固着の逃げ道 `forceUpdate.ts` 追加。セキュリティ横断点検。本番デプロイ済。
- それ以前（第1〜22）の詳細は `~/.claude` メモリ「ぽいロボ 作業ログ」を参照。

---

## メモ
- データ鮮度: `fetch-data.yml` が平日 19:30 / 21:30 JST に取得、土曜 09:00 に週次・バックテスト再計算。
- TEV ロジックバージョン: `v2-symmetric-restoring`（復元力 `tev_rResist` 両側化）。今回のゲートはこの上に乗る確信度の後処理。
