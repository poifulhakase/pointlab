{
  "name": "pointlab-site",
  "version": "1.0.0",
  "type": "module",
  "description": "ぽいんとらぼ - note風静的サイト",
  "scripts": {
    "dev": "npx serve pointlab -l 3000",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npx serve pointlab -l 3000"
  },
  "dependencies": {
    "@vercel/functions": "^3.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
