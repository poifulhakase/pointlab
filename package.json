{
  "name": "pointlab-site",
  "version": "1.0.0",
  "type": "module",
  "description": "ぽいんとらぼ - note風静的サイト",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npx serve . -l 3000",
    "optimize-images": "node scripts/optimize-images.js",
    "embed-sidebiz": "node scripts/embed-sidebiz-image.js",
    "remove-unused-images": "node scripts/remove-unused-images.js",
    "remove-unused-images:dry": "node scripts/remove-unused-images.js --dry-run",
    "compress-images": "node scripts/compress-images.js",
    "generate-icons": "node scripts/generate-apple-touch-icon.js",
    "generate-icons:sharp": "node scripts/generate-apple-touch-icon-sharp.js",
    "optimize-icons": "node scripts/optimize-icons.js",
    "make-gif-transparent": "node scripts/make-gif-transparent.js"
  },
  "dependencies": {
    "@vercel/functions": "^3.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "vite": "^5.0.0"
  }
}
