// ── グローバル z-index 階層定義 ──────────────────────
// UI が複雑化しても重ね順の衝突を防ぐために一元管理する。
// コンポーネント内部ローカルな値（グリッド行の 2/4/5 等）はここに含めない。
//
// 階層ルール（大きいほど前面）:
//   160-161 : アプリナビ（ヘッダー / フッター）
//   170     : コネクト確認モーダル（フッターより前面）
//   150     : フローティングサブバー
//   199-200 : サイドバー + オーバーレイ / ヘッダーツールチップ
//   299-300 : デイノートパネル
//   399-400 : 設定・認証モーダル
//   600-601 : フルスクリーン（チャート / エンジン）
//   900-901 : アラートモーダル
//  1200     : デルタ分析モーダル
//  9999     : ポップオーバー（ドロップダウン / トースト / バッジ）

export const Z = {
  // ── ナビ層 ──────────────────────────────────────
  calendarHeader:    160,
  footer:            161,
  connectConfirm:    170, // SupportView 接続確認（フッターを覆う）

  // ── フローティング UI ──────────────────────────
  floatSubBar:       150,

  // ── サイドバー層 ─────────────────────────────────
  sidebarOverlay:    199,
  sidebar:           200,
  headerTooltip:     200,

  // ── パネル層 ─────────────────────────────────────
  dayNoteOverlay:    299,
  dayNotePanel:      300,

  // ── モーダル層 ───────────────────────────────────
  settingsOverlay:   399,
  settingsPanel:     400,

  // ── フルスクリーン層 ─────────────────────────────
  chartFullscreen:   600,
  engineModal:       601,

  // ── アラート層 ───────────────────────────────────
  alertOverlay:      900,
  alertPanel:        901,

  // ── 分析モーダル ──────────────────────────────────
  deltaModal:       1200,

  // ── 最前面 ───────────────────────────────────────
  popover:          9999,
} as const
