# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# 秘密（Discord Webhook URL は実質パスワード・Anthropic API キー）
.env

# ローカル実行の産物（判断ログ・DB・価格キャッシュ）
# 🔴 リポジトリ直下の .gitignore に `logs` があるため robotrade/logs も元々追跡されない。
#    判断ログと成績DBは「ローカルだけの資産」＝バックアップは手動。
logs/
*.log
*.db
*.db-wal
*.db-shm
*.sqlite3
data_cache/
eda_out/
# JPXから取り直せる予定表（鮮度が落ちたら自動で取り直す）
data/earnings_schedule.json
scratch/
note_feed_state.json
feed_state.json
